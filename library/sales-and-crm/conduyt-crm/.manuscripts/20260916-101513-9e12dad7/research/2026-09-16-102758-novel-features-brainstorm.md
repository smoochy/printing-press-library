## Absorb Manifest (conduyt-crm reprint 2026-09-16, run 20260916-101513-9e12dad7)

### Absorbed (match or beat everything that exists)
| # | Feature | Best Source | Our Implementation | Added Value |
|---|---------|-----------|-------------------|-------------|
| 1 | Full endpoint mirror: contacts/companies/deals/pipelines/tasks/notes/tags/activities/appointments CRUD + search (826 ops, 572 paths, 95 families) | Conduyt OpenAPI 3.1 (first-party) + conduyt-mcp@4.59.0 (159 tools) + npm conduyt@1.45.0 | (generated endpoint) <resource> <endpoint> for every operation | typed exits, --select/--compact, MCP annotations, offline store, regenerated under press 4.32 |
| 2 | Reports: lead activity, speed to lead, stage funnel, appointments, calls, agent performance, attribution, dashboards, scheduled deliveries (39 ops) | Conduyt app reports (new since July) + conduyt-mcp report/scheduled tools | (generated endpoint) reports <endpoint>; promoted `conduyt-crm-pp-cli reports <name>` paths with --range/--group-by/--format | reports as commands for agents and cron; CSV/JSON out of the box |
| 3 | Smart Dialing: queues, dispositions → lead status, agent statuses, smart views, local presence (51 dialer ops) | Conduyt app (new since July) + conduyt-mcp | (generated endpoint) dialer <endpoint> | queue state and dispositions from the terminal |
| 4 | Automations ops: validate/import/dry-run/publish, step logs, node-level operating hours (runWindow) | prior conduyt-crm-pp-cli + conduyt-mcp workflow/publish tools | (generated endpoint) automations <endpoint> | agent workflow-builder loop intact incl. the new runWindow config |
| 5 | Imports: preflight (criticalWarnings), create (verifyLineType/reEnrollMode), job status, rows, resubmit, revert-preview | prior print (first-class) + conduyt-mcp conduyt_import_preflight | conduyt-crm-pp-cli imports preflight; (generated endpoint) imports <endpoint> | Kloudi-safe imports from the CLI |
| 6 | Line-type verification loop | prior print `contacts verify-line-type --until-done` + conduyt-mcp conduyt_verify_line_type | conduyt-crm-pp-cli contacts verify-line-type | drives the batched loop to done, tenant-paid disclosure |
| 7 | SMS delivery report incl. rejection reasons | prior print `reports sms-delivery` + conduyt-mcp | conduyt-crm-pp-cli reports sms-delivery | the Kloudi receipts from a terminal |
| 8 | Messaging channels: SMS/Twilio, WhatsApp templates, voice drop, FromK.ai, Project Blue, email sequences | conduyt-mcp send/whatsapp/fromk/sms tools + Conduyt app | (generated endpoint) messages/whatsapp/voice-drop/fromkai <endpoint> | parity |
| 9 | Bulk + batch operations: bulk tag/edit contacts, update deals, batch by filter, select-all-matching, delete by filter | conduyt-mcp bulk/batch tools + Conduyt app | (generated endpoint) contacts bulk*, batch-operations <endpoint> | parity; --dry-run on every mutation |
| 10 | AI features: chat/compose/insights/summarize/enrich/daily brief | prior print ai_* + conduyt-mcp ai tools | (generated endpoint) ai <endpoint> | parity |
| 11 | DNC / consent / compliance list ops | conduyt-mcp dnc tools | (generated endpoint) dnc <endpoint> (dnc scope documented) | parity |
| 12 | Settings, API keys, webhooks, integrations (Twilio, Zoom, calendar, Hyros), exports | prior print + conduyt-mcp (48 settings ops) | (generated endpoint) settings/api-keys/webhooks/... <endpoint> | parity |
| 13 | Local store: sync/search (FTS)/sql/stale/reconcile/analytics/tail | press 4.32 framework (prior 4.28) | (behavior in conduyt-crm-pp-cli sync/search/sql/analytics/tail) framework commands | offline queries over 200k contacts |
| 14 | Agent plumbing: --json/--agent/--select/--compact/--dry-run/agent-context/doctor | press 4.32 framework | (behavior in conduyt-crm-pp-cli doctor / agent-context) | bounded output for agents |
| 15 | MCP server | conduyt-mcp@4.59.0 (159 always-loaded tools, stdio) | conduyt-crm-pp-mcp with the Cloudflare pattern (stdio+http, code orchestration, hidden endpoint tools) | full 826-op surface in ~1K tokens, remote-capable |
| 16 | Prior novel commands (July): send-check, imports blame, imports watch, verify-line-type --estimate, drips audit, analytics, tail | prior print research.json (novel_features_built = 7) | re-scored by the novel-features subagent (candidate input, Pass 2(d)) | keep/reframe/drop with reasons |

### Transcendence (only possible with our approach)
| # | Feature | Command | Score | Persona | Buildability | Why Only We Can Do This | Long Description |
|---|---------|---------|-------|---------|--------------|------------------------|------------------|
| 1 | Send-safety check | `send-check --list <id> \| --tag <t> \| --contact <id>` | 10/10 | Agent, Paul | hand-code | Joins synced contacts (phone, lineType, valid) with GET /contacts/dnc-status into a go/no-go table with typed exits | Use this command for a go/no-go verdict on a send list. Do NOT use it to count unverified numbers or their Twilio cost; use 'contacts verify-line-type --estimate' instead. |
| 2 | Import delivery blame | `imports blame <jobId>` | 9/10 | Paul, Agent | hand-code | Joins GET /imports/{id}/rows with synced contacts and GET /reports/sms-delivery outcomes plus GET /imports/{id}/side-effects, grouped by reason; no endpoint takes an importId | Use this command after an import finished to see per-row delivery outcomes. Do NOT use it to wait for an import to finish; use 'imports watch' instead. |
| 3 | Import watcher | `imports watch <jobId> [--verify] [--interval 10s]` | 9/10 | MacGyver, Paul | hand-code | Polls GET /imports/{id} and /imports/{id}/launch-monitor to a terminal state, prints the recap, typed exits for cron | Use this command to block until an import job completes. Do NOT use it for a finished job's per-row outcomes; use 'imports blame' instead. |
| 4 | Verification cost estimate | `contacts verify-line-type --estimate [--smart-list <id>]` | 8/10 | Paul | hand-code | Counts unverified contacts in scope from the local store × the disclosed tenant-paid unit cost before driving POST /contacts/verify-line-type to done | Use this command to count unverified numbers and their cost, or to run the verification loop. Do NOT use it to decide whether a list is safe to text; use 'send-check' instead. |
| 5 | Targets scorecard | `reports scorecard --range 30d [--user <id>] --agent` | 8/10 | Paul, MacGyver | hand-code | Joins GET /reports/targets to GET /reports/agent-performance and computes attainment % and pace-to-period-end per user and metric | Use this command for target-vs-actual attainment per user. Do NOT use it for how a report changed versus the prior period; use 'reports compare' instead. |
| 6 | Period-over-period compare | `reports compare speed-to-lead --range 7d [--assigned-to <id>] --json` | 7/10 | Agent, Paul | hand-code | Calls one report route twice (window + equal prior window) and diffs every numeric leaf: prior, current, delta, delta % | Use this command for the change in a report between two equal periods. Do NOT use it for target attainment; use 'reports scorecard' instead. |
| 7 | Dial coverage | `dialer coverage [--limit 25] --agent` | 9/10 | Paul, Agent | hand-code | Joins GET /smart-views/dial-order × GET /dialer/queue?smartViewId per priority × GET /dialer/agents-status (Available); exit 2 when a priority is empty | Use this command for the account-wide picture of priorities, queue depth and available agents. Do NOT use it to fetch one agent's next leads; use 'dialer queue list' instead. |
| 8 | Operating-hours audit | `automations hours-audit [--published-only] --json` | 9/10 | Paul, Agent | hand-code | json_extracts every Send SMS / Send Email / Assign step from synced automation definitions and lists steps with and without runWindow, per automation | none |

Hand-code commitment: 8 of 8 rows are hand-code (4 carried from July: send-check, imports blame, imports watch, verify-line-type --estimate; 4 new: reports scorecard, reports compare, dialer coverage, automations hours-audit). 0 spec-emits. 0 stubs.

### Reprint verdicts (prior novel features)
keep: send-check, imports blame (+ side-effects), imports watch (+ launch-monitor), contacts verify-line-type --estimate · drop: drips audit (no weekly caller; server-side ledger + sms-delivery/sms-drip-conversion reports cover it), analytics (press 4.32 framework root command), tail (framework root command)

### Killed candidates (subagent Pass 3)
report digest (five mirrors, no computation → reports compare); queue driver (auth gap: queue next/skip/worked/release are human-session only); queue preview (thin rename of GET /dialer/queue); agent utilization (two mirrors divided; agent-performance already carries it); untouched contacts (speed-to-lead already buckets it server-side); stuck deals (one framework sql statement); delivery watchdog (three-hop walk, no ritual); call wrap-up read (thin rename of GET /dialer/call/{id}/post-call)

### Phase Gate 1.5 (recorded)
Approved 2026-09-16 by fable on Paul's delegation ("Sure if that's the correct approach", 10:11 AM ET): 16 absorbed rows + 8 transcendence rows, all hand-code, no stubs. Paul's standing brief for this run: keep both CLIs, this print is the marketing listing, the merge time is the measurement.

---
## Subagent audit trail (customer model, pre-cut candidates)
Personas: (1) Paul, owner-operator running BDA's lead machine (Monday reports ritual, AM import ritual, dial-order check; frustration: every report is a separate page/date picker, dialer state split over three screens). (2) Headless agent with a cdy_ key (natural-language asks: text a list without repeating Kloudi, verify an import, pull the numbers, explain a speed-to-lead move, check quiet hours before publishing; frustration: unbounded JSON, no go/no-go verdict, no typed exits). (3) MacGyver, the n8n/cron plumbing (poll imports, post Monday numbers, alert on breaks; frustration: polling loops, no exit codes).
Pre-cut candidates (19): send-check, imports blame, imports watch, verify-line-type --estimate, drips audit, analytics, tail, reports scorecard, reports compare, report digest, dialer coverage, queue driver, queue preview, agent utilization, contacts untouched, deals stuck, automations hours-audit, delivery watchdog, calls wrapup. Survivors 8, killed 11 (reasons in the manifest).
