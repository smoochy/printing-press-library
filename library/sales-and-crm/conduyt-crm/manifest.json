{
  "manifest_version": "0.3",
  "name": "conduyt-crm-pp-mcp",
  "display_name": "Conduyt CRM",
  "version": "1.0.0",
  "description": "Every Conduyt endpoint as a command, plus send-safety, import forensics, report deltas and dialer coverage no other Conduyt surface has.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/conduyt-crm-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/conduyt-crm-pp-mcp",
      "args": [],
      "env": {
        "CONDUYT_API_KEY": "${user_config.conduyt_api_key}",
        "CONDUYT_BEARER_AUTH": "${user_config.conduyt_bearer_auth}"
      }
    }
  },
  "user_config": {
    "conduyt_api_key": {
      "type": "string",
      "title": "CONDUYT_API_KEY",
      "description": "Optional. Sets CONDUYT_API_KEY for the Conduyt CRM MCP server.",
      "sensitive": true
    },
    "conduyt_bearer_auth": {
      "type": "string",
      "title": "CONDUYT_BEARER_AUTH",
      "description": "Optional. Sets CONDUYT_BEARER_AUTH for the Conduyt CRM MCP server.",
      "sensitive": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
