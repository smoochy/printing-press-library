// Copyright 2026 Kelvin Cushman and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"
	"strconv"

	"github.com/spf13/cobra"
)

func newGenerateUploadAndExtendAudioCmd(flags *rootFlags) *cobra.Command {
	var bodyAudioWeight float64
	var bodyCallBackUrl string
	var bodyContinueAt float64
	var bodyDefaultParamFlag string
	var bodyInstrumental bool
	var bodyModel string
	var bodyNegativeTags string
	var bodyPersonaId string
	var bodyPersonaModel string
	var bodyPrompt string
	var bodyStyle string
	var bodyStyleWeight float64
	var bodyTitle string
	var bodyUploadUrl string
	var bodyVocalGender string
	var bodyWeirdnessConstraint float64
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "upload-and-extend-audio",
		Short:       "This API extends audio while preserving the original style, generating a longer and seamless audio track.",
		Example:     "  kie-pp-cli generate upload-and-extend-audio --call-back-url https://api.example.com/callback",
		Annotations: map[string]string{"pp:endpoint": "generate.upload-and-extend-audio", "pp:method": "POST", "pp:path": "/api/v1/generate/upload-extend"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("call-back-url") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "call-back-url")
				}
				if !cmd.Flags().Changed("continue-at") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "continue-at")
				}
				if !cmd.Flags().Changed("default-param-flag") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "default-param-flag")
				}
				if !cmd.Flags().Changed("model") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "model")
				}
				if !cmd.Flags().Changed("upload-url") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "upload-url")
				}
			}
			path := "/api/v1/generate/upload-extend"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("audio-weight") || bodyAudioWeight != 0.0 {
					bodyMap["audioWeight"] = bodyAudioWeight
				}
				if cmd.Flags().Changed("call-back-url") || bodyCallBackUrl != "" {
					bodyMap["callBackUrl"] = bodyCallBackUrl
				}
				if cmd.Flags().Changed("continue-at") || bodyContinueAt != 0.0 {
					bodyMap["continueAt"] = bodyContinueAt
				}
				if cmd.Flags().Changed("default-param-flag") || bodyDefaultParamFlag != "" {
					parsedDefaultParamFlag, err := strconv.ParseBool(bodyDefaultParamFlag)
					if err != nil {
						return fmt.Errorf("parsing --default-param-flag as bool: %w", err)
					}
					bodyMap["defaultParamFlag"] = parsedDefaultParamFlag
				}
				if cmd.Flags().Changed("instrumental") {
					bodyMap["instrumental"] = bodyInstrumental
				}
				if cmd.Flags().Changed("model") || bodyModel != "" {
					bodyMap["model"] = bodyModel
				}
				if cmd.Flags().Changed("negative-tags") || bodyNegativeTags != "" {
					bodyMap["negativeTags"] = bodyNegativeTags
				}
				if cmd.Flags().Changed("persona-id") || bodyPersonaId != "" {
					bodyMap["personaId"] = bodyPersonaId
				}
				if cmd.Flags().Changed("persona-model") || bodyPersonaModel != "" {
					bodyMap["personaModel"] = bodyPersonaModel
				}
				if cmd.Flags().Changed("prompt") || bodyPrompt != "" {
					bodyMap["prompt"] = bodyPrompt
				}
				if cmd.Flags().Changed("style") || bodyStyle != "" {
					bodyMap["style"] = bodyStyle
				}
				if cmd.Flags().Changed("style-weight") || bodyStyleWeight != 0.0 {
					bodyMap["styleWeight"] = bodyStyleWeight
				}
				if cmd.Flags().Changed("title") || bodyTitle != "" {
					bodyMap["title"] = bodyTitle
				}
				if cmd.Flags().Changed("upload-url") || bodyUploadUrl != "" {
					bodyMap["uploadUrl"] = bodyUploadUrl
				}
				if cmd.Flags().Changed("vocal-gender") || bodyVocalGender != "" {
					bodyMap["vocalGender"] = bodyVocalGender
				}
				if cmd.Flags().Changed("weirdness-constraint") || bodyWeirdnessConstraint != 0.0 {
					bodyMap["weirdnessConstraint"] = bodyWeirdnessConstraint
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "generate", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "generate", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "generate", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "generate", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "generate", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "generate",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "generate", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "generate", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().Float64Var(&bodyAudioWeight, "audio-weight", 0.0, "Balance weight for audio features vs. other factors. Optional. Range 0–1, up to 2 decimal places.")
	cmd.Flags().StringVar(&bodyCallBackUrl, "call-back-url", "", "The URL to receive audio extension task completion updates. Required for all audio extension requests.")
	cmd.Flags().Float64Var(&bodyContinueAt, "continue-at", 0.0, "The time point (in seconds) from which to start extending the music. - Required when `defaultParamFlag` is `true`.")
	cmd.Flags().StringVar(&bodyDefaultParamFlag, "default-param-flag", "", "Enable custom mode for advanced audio generation settings.")
	cmd.Flags().BoolVar(&bodyInstrumental, "instrumental", false, "Determines whether the audio is instrumental (without lyrics).")
	cmd.Flags().StringVar(&bodyModel, "model", "", "The AI model version to use for generation. - Required for all requests.")
	cmd.Flags().StringVar(&bodyNegativeTags, "negative-tags", "", "Music styles to exclude from generation")
	cmd.Flags().StringVar(&bodyPersonaId, "persona-id", "", "Only available when Custom Mode (`defaultParamFlag: true`) is enabled.")
	cmd.Flags().StringVar(&bodyPersonaModel, "persona-model", "", "The persona model is only available for models version 5 and 5.5.")
	cmd.Flags().StringVar(&bodyPrompt, "prompt", "", "Describe how the music should be extended. Optional; if provided, it will be used as a prompt.")
	cmd.Flags().StringVar(&bodyStyle, "style", "", "Music style, e.g., Jazz, Classical, Electronic.")
	cmd.Flags().Float64Var(&bodyStyleWeight, "style-weight", 0.0, "Strength of adherence to the specified style. Optional. Range 0–1, up to 2 decimal places.")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "Music title.")
	cmd.Flags().StringVar(&bodyUploadUrl, "upload-url", "", "The URL for uploading audio files, required regardless of whether defaultParamFlag is true or false.")
	cmd.Flags().StringVar(&bodyVocalGender, "vocal-gender", "", "Vocal gender preference for the singing voice. Optional. Use 'm' for male and 'f' for female.")
	cmd.Flags().Float64Var(&bodyWeirdnessConstraint, "weirdness-constraint", 0.0, "Controls experimental/creative deviation. Optional. Range 0–1, up to 2 decimal places.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
