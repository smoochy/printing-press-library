// Copyright 2026 Farouk Umar and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newRunsListProjectTaskCmd(flags *rootFlags) *cobra.Command {
	var flagPageLimit int
	var flagCursor string
	var flagTaskId string
	var flagTaskRevisionId string
	var flagStatus string
	var flagStartDate string
	var flagEndDate string
	var flagIncludeEventTriggerId bool
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list-project-task <org_id> <project_id>",
		Short:       "List all task runs across a project. Returns a paginated list of task runs for all tasks in the project.",
		Example:     "  rightbrain-pp-cli runs list-project-task 123e4567-e89b-12d3-a456-426614174000 123e4567-e89b-12d3-a456-426614174001",
		Annotations: map[string]string{"pp:endpoint": "runs.list-project-task", "pp:method": "GET", "pp:path": "/org/{org_id}/project/{project_id}/task_run", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <org_id> <project_id>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <org_id> <project_id>"))
			}
			if cmd.Flags().Changed("status") {
				allowedStatus := []string{"success", "error"}
				validStatus := false
				for _, v := range allowedStatus {
					if flagStatus == v {
						validStatus = true
						break
					}
				}
				if !validStatus {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagStatus, "status", allowedStatus)
				}
			}
			path := "/org/{org_id}/project/{project_id}/task_run"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("org_id is required\nUsage: %s <%s>", cmd.CommandPath(), "org_id"))
			}
			path = replacePathParam(path, "org_id", args[0])
			if len(args) < 2 || args[1] == "" {
				return usageErr(fmt.Errorf("project_id is required\nUsage: %s <%s>", cmd.CommandPath(), "project_id"))
			}
			path = replacePathParam(path, "project_id", args[1])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "live", "runs", path, map[string]string{
				"page_limit":               formatCLIParamValue(flagPageLimit),
				"cursor":                   formatCLIParamValue(flagCursor),
				"task_id":                  formatCLIParamValue(flagTaskId),
				"task_revision_id":         formatCLIParamValue(flagTaskRevisionId),
				"status":                   formatCLIParamValue(flagStatus),
				"start_date":               formatCLIParamValue(flagStartDate),
				"end_date":                 formatCLIParamValue(flagEndDate),
				"include_event_trigger_id": formatCLIParamValue(flagIncludeEventTriggerId),
			}, nil, flagAll, "cursor", "cursor", "", 100, "pagination.next_cursor", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().IntVar(&flagPageLimit, "page-limit", 100, "The maximum number of items to return per page. Defaults to `100` if not specified.")
	cmd.Flags().StringVar(&flagCursor, "cursor", "", "A cursor for pagination. Use the `next_cursor` value from the previous response to get the next page of results.")
	cmd.Flags().StringVar(&flagTaskId, "task-id", "", "Filter to a specific task")
	cmd.Flags().StringVar(&flagTaskRevisionId, "task-revision-id", "", "Filter to a specific task revision")
	cmd.Flags().StringVar(&flagStatus, "status", "", "Filter by status (success or error) (one of: success, error)")
	cmd.Flags().StringVar(&flagStartDate, "start-date", "", "Start date for the listing (e.g., '2023-06-01', '24 hours ago', 'yesterday'). Defaults to 7 days before end_date or now.")
	cmd.Flags().StringVar(&flagEndDate, "end-date", "", "End date for the listing (e.g., '2023-06-30', 'now', 'today'). Defaults to now.")
	cmd.Flags().BoolVar(&flagIncludeEventTriggerId, "include-event-trigger-id", false, "If true, include event_trigger_id for each task run in results.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
