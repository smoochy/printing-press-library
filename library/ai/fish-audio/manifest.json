{
  "manifest_version": "0.3",
  "name": "fish-audio-pp-mcp",
  "display_name": "Fish Audio",
  "version": "4.31.1",
  "description": "Fish Audio API: text-to-speech, speech-to-text, voice design, voice models (clones), and account credit.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/fish-audio-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/fish-audio-pp-mcp",
      "args": [],
      "env": {
        "PRINTING_PRESS_CLIENT_PROFILE": "${user_config.printing_press_client_profile}"
      }
    }
  },
  "user_config": {
    "printing_press_client_profile": {
      "type": "string",
      "title": "Client profile",
      "description": "Binds the MCP server to an existing tenant-gated Printing Press client profile.",
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
