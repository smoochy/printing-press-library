// Copyright 2026 Jon Gouveia and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/ai/fish-audio/internal/cliutil"
	"github.com/spf13/cobra"
)

func newTtsCreateStreamCmd(flags *rootFlags) *cobra.Command {
	var flagModel string
	var bodyChunkLength int
	var bodyConditionOnPreviousChunks bool
	var bodyEarlyStopThreshold float64
	var bodyFeatures string
	var bodyFormat string
	var bodyLatency string
	var bodyMaxNewTokens int
	var bodyMinChunkLength int
	var bodyMp3Bitrate int
	var bodyNormalize bool
	var bodyOpusBitrate int
	var bodyProsody string
	var bodyReferenceId string
	var bodyReferences string
	var bodyRepetitionPenalty float64
	var bodySampleRate string
	var bodyTemperature float64
	var bodyText string
	var bodyTopP float64
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "create-stream",
		Short: "Text to Speech Stream with Timestamps",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  fish-audio-pp-cli tts create-stream --text example-value",
		Annotations: map[string]string{"pp:endpoint": "tts.create-stream", "pp:method": "POST", "pp:path": "/v1/tts/stream/with-timestamp"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if cmd.Flags().Changed("model") {
				allowedModel := []string{"s1", "s2-pro", "s2.1-pro", "s2.1-pro-free"}
				validModel := false
				for _, v := range allowedModel {
					if flagModel == v {
						validModel = true
						break
					}
				}
				if !validModel {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagModel, "model", allowedModel)
				}
			}
			if !stdinBody {
				if !cmd.Flags().Changed("text") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "text")
				}
			}
			path := "/v1/tts/stream/with-timestamp"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			headerOverrides := map[string]string{
				"Accept":                         "text/event-stream",
				"X-Printing-Press-Text-Response": "true",
			}

			if cmd.Flags().Changed("model") || flagModel != "" {
				headerOverrides["model"] = formatCLIParamValue(flagModel)
			}

			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("chunk-length") || bodyChunkLength != 0 {
					bodyMap["chunk_length"] = bodyChunkLength
				}
				if cmd.Flags().Changed("condition-on-previous-chunks") {
					bodyMap["condition_on_previous_chunks"] = bodyConditionOnPreviousChunks
				}
				if cmd.Flags().Changed("early-stop-threshold") || bodyEarlyStopThreshold != 0.0 {
					bodyMap["early_stop_threshold"] = bodyEarlyStopThreshold
				}
				if cmd.Flags().Changed("features") {
					parsedFeatures, parseErr := cliutil.ParseStringList(bodyFeatures)
					if parseErr != nil {
						return fmt.Errorf("parsing --features list: %w", parseErr)
					}
					bodyMap["features"] = parsedFeatures
				}
				if cmd.Flags().Changed("format") || bodyFormat != "" {
					bodyMap["format"] = bodyFormat
				}
				if cmd.Flags().Changed("latency") || bodyLatency != "" {
					bodyMap["latency"] = bodyLatency
				}
				if cmd.Flags().Changed("max-new-tokens") || bodyMaxNewTokens != 0 {
					bodyMap["max_new_tokens"] = bodyMaxNewTokens
				}
				if cmd.Flags().Changed("min-chunk-length") || bodyMinChunkLength != 0 {
					bodyMap["min_chunk_length"] = bodyMinChunkLength
				}
				if cmd.Flags().Changed("mp3-bitrate") || bodyMp3Bitrate != 0 {
					bodyMap["mp3_bitrate"] = bodyMp3Bitrate
				}
				if cmd.Flags().Changed("normalize") {
					bodyMap["normalize"] = bodyNormalize
				}
				if cmd.Flags().Changed("opus-bitrate") || bodyOpusBitrate != 0 {
					bodyMap["opus_bitrate"] = bodyOpusBitrate
				}
				if cmd.Flags().Changed("prosody") || bodyProsody != "" {
					if looksLikeJSONComposite(bodyProsody) {
						var parsedProsody any
						if err := json.Unmarshal([]byte(bodyProsody), &parsedProsody); err != nil {
							return fmt.Errorf("parsing --prosody JSON: %w", err)
						}
						bodyMap["prosody"] = parsedProsody
					} else {
						bodyMap["prosody"] = bodyProsody
					}
				}
				if cmd.Flags().Changed("reference-id") || bodyReferenceId != "" {
					if looksLikeJSONComposite(bodyReferenceId) {
						var parsedReferenceId any
						if err := json.Unmarshal([]byte(bodyReferenceId), &parsedReferenceId); err != nil {
							return fmt.Errorf("parsing --reference-id JSON: %w", err)
						}
						bodyMap["reference_id"] = parsedReferenceId
					} else {
						bodyMap["reference_id"] = bodyReferenceId
					}
				}
				if cmd.Flags().Changed("references") || bodyReferences != "" {
					if looksLikeJSONComposite(bodyReferences) {
						var parsedReferences any
						if err := json.Unmarshal([]byte(bodyReferences), &parsedReferences); err != nil {
							return fmt.Errorf("parsing --references JSON: %w", err)
						}
						bodyMap["references"] = parsedReferences
					} else {
						bodyMap["references"] = bodyReferences
					}
				}
				if cmd.Flags().Changed("repetition-penalty") || bodyRepetitionPenalty != 0.0 {
					bodyMap["repetition_penalty"] = bodyRepetitionPenalty
				}
				if cmd.Flags().Changed("sample-rate") || bodySampleRate != "" {
					bodyMap["sample_rate"] = bodySampleRate
				}
				if cmd.Flags().Changed("temperature") || bodyTemperature != 0.0 {
					bodyMap["temperature"] = bodyTemperature
				}
				if cmd.Flags().Changed("text") || bodyText != "" {
					bodyMap["text"] = bodyText
				}
				if cmd.Flags().Changed("top-p") || bodyTopP != 0.0 {
					bodyMap["top_p"] = bodyTopP
				}
			}
			data, _, err := c.PostWithParamsAndHeaders(cmd.Context(), path, params, body, headerOverrides)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			_ = os.Stderr
			var textEnvelope struct {
				Format string `json:"format"`
				Body   string `json:"body"`
			}
			if err := json.Unmarshal(data, &textEnvelope); err != nil {
				return fmt.Errorf("decoding text response envelope: %w", err)
			}
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.agent || flags.csv || flags.compact || flags.selectFields != "" {
				if flags.csv {
					data = append(append(json.RawMessage{'['}, data...), ']')
				}
				return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"}, map[string]bool{"alignment": true, "audio_base64": true, "chunk_audio_offset_sec": true, "chunk_seq": true, "content": true})
			}
			_, err = cmd.OutOrStdout().Write([]byte(textEnvelope.Body))
			return err
		},
	}
	cmd.Flags().StringVar(&flagModel, "model", "s2.1-pro", "Specify which TTS model to use. Use `s2.1-pro-free` for the free developer tier. (one of: s1, s2-pro, s2.1-pro, s2.1-pro-free)")
	cmd.Flags().IntVar(&bodyChunkLength, "chunk-length", 300, "Text segment size for processing.")
	cmd.Flags().BoolVar(&bodyConditionOnPreviousChunks, "condition-on-previous-chunks", true, "Use previous audio as context for voice consistency.")
	cmd.Flags().Float64Var(&bodyEarlyStopThreshold, "early-stop-threshold", 1.000000, "Early stopping threshold for batch processing.")
	cmd.Flags().StringVar(&bodyFeatures, "features", "[]", "Optional request-scoped TTS feature flags forwarded verbatim to the inference backend.")
	cmd.Flags().StringVar(&bodyFormat, "format", "mp3", "Output audio container (one of: mp3, wav, pcm, opus)")
	cmd.Flags().StringVar(&bodyLatency, "latency", "normal", "Latency-quality trade-off. normal: best quality, balanced: reduced latency, low: lowest latency.")
	cmd.Flags().IntVar(&bodyMaxNewTokens, "max-new-tokens", 1024, "Maximum audio tokens to generate per text chunk.")
	cmd.Flags().IntVar(&bodyMinChunkLength, "min-chunk-length", 50, "Minimum characters before splitting into a new chunk.")
	cmd.Flags().IntVar(&bodyMp3Bitrate, "mp3-bitrate", 128, "MP3 bitrate in kbps. Only applies when format is mp3.")
	cmd.Flags().BoolVar(&bodyNormalize, "normalize", true, "Normalizes text for English and Chinese, improving stability for numbers.")
	cmd.Flags().IntVar(&bodyOpusBitrate, "opus-bitrate", -1000, "Opus bitrate in bps. -1000 for automatic. Only applies when format is opus.")
	cmd.Flags().StringVar(&bodyProsody, "prosody", "", "Speed and volume adjustments for the output.")
	cmd.Flags().StringVar(&bodyReferenceId, "reference-id", "", "Voice model ID(s) from Fish Audio library or your custom models. For single-speaker synthesis, provide a string.")
	cmd.Flags().StringVar(&bodyReferences, "references", "", "Inline voice references for zero-shot cloning. Requires MessagePack (not JSON).")
	cmd.Flags().Float64Var(&bodyRepetitionPenalty, "repetition-penalty", 1.200000, "Penalty for repeating audio patterns. Values above 1.0 reduce repetition.")
	cmd.Flags().StringVar(&bodySampleRate, "sample-rate", "", "Audio sample rate in Hz. When null, uses the format's default (44100 Hz for most formats, 48000 Hz for opus).")
	cmd.Flags().Float64Var(&bodyTemperature, "temperature", 0.700000, "Controls expressiveness. Higher is more varied, lower is more consistent.")
	cmd.Flags().StringVar(&bodyText, "text", "", "Text to convert to speech.")
	cmd.Flags().Float64Var(&bodyTopP, "top-p", 0.700000, "Controls diversity via nucleus sampling.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
