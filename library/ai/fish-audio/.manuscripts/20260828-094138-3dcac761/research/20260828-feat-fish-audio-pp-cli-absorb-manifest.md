# Fish Audio Printing Press CLI — Absorb Manifest

Scope: TTS, ASR, voice design, voice models (clones), wallet/credit. Agents/phone product out of scope for `fish-audio-pp-cli`; ElevenLabs Agents/convai features below are catalogued for completeness of the incumbent survey but most map to `(stub) out of scope`.

## Absorbed

| # | Feature | Best Source | Our Implementation | Added Value |
|---|---|---|---|---|
| 1 | TTS render to bytes/file (single call) | fish-audio-python `fishaudio` SDK | `fish-audio-pp-cli tts render` | Writes audio file + JSON manifest (model, bytes, cost) in one call; SDK only returns bytes. |
| 2 | TTS chunked HTTP streaming | fish-audio-python `client.tts.stream()` | `(generated endpoint) tts POST /v1/tts` | Direct pass-through, matches SDK. |
| 3 | SSE streaming with word-level timestamps | OpenAPI spec `/v1/tts/stream/with-timestamp` (no SDK covers this — spec-only endpoint) | `(generated endpoint) tts POST /v1/tts/stream/with-timestamp` | We're first to wrap this; no official SDK exposes it. |
| 4 | WebSocket real-time TTS streaming | fish-audio-python `stream_websocket()`, fish-audio-go `TTS.StreamWebSocket`, fish-audio-typescript `convertRealtime()` | `(stub) deferred — Pearl's use case is static pre-rendered audio, not live playback; build priority 7` | N/A until a live-voice use case exists. |
| 5 | `model` HTTP header selection (s1/s2-pro/s2.1-pro/s2.1-pro-free) | OpenAPI spec + all 3 official SDKs | `fish-audio-pp-cli tts render --model <id>` | Client-side enum validation before send, since the API silently falls back to `s2.1-pro` on an unrecognized value instead of erroring. |
| 6 | msgpack content-type for inline zero-shot voice references | OpenAPI spec `TTSRequest.references` + fish-audio-go `ConvertParams` | `(behavior in fish-audio-pp-cli tts render) --reference-audio <file> --reference-text "<transcript>" auto-switches request encoding to application/msgpack` | Hides the JSON-can't-carry-binary constraint from the caller; raw API requires manually picking msgpack. |
| 7 | Multi-speaker dialogue via `<|speaker:N|>` tags | OpenAPI spec `TTSRequest` description + fish-audio-typescript README | `fish-audio-pp-cli tts batch --dialogue` (maps speaker labels to `reference_id` array positions) | Avoids hand-building the speaker-tag string and matching `reference_id` array order manually. |
| 8 | Multi-speaker gated to S2 family, rejects `s1` | OpenAPI spec (`TTSRequest`/`TTSStreamWithTimestampRequest` description) | `(behavior in fish-audio-pp-cli tts batch --dialogue)` client-side pre-check rejects `model=s1` before the call | Fails fast locally instead of spending a call on a request the API would reject. |
| 9 | Bracket-tag emotion/prosody control (`[happy]`, `[whispering]`, free-text) | docs.fish.audio `emotions.md` | `(behavior in fish-audio-pp-cli tts render)` pass-through, documented in `--help` with example tags | Docs note tags aren't a closed enum; CLI help surfaces examples without falsely enumerating a fixed list. |
| 10 | Prosody speed/volume/loudness-normalize controls | OpenAPI spec `ProsodyControl` | `fish-audio-pp-cli tts render --speed --volume --normalize-loudness` | Flags map 1:1 to spec fields; documents the `s1`-has-no-effect caveat on `normalize_loudness` in help text. |
| 11 | Output format + bitrate controls (mp3/wav/pcm/opus, mp3_bitrate, opus_bitrate) | OpenAPI spec `TTSRequest` | `fish-audio-pp-cli tts render --format --mp3-bitrate --opus-bitrate` | Direct pass-through with enum validation client-side. |
| 12 | WAV output missing frame-count header (breaks pygame/some players) | GitHub issue [fishaudio/fish-audio-python#26](https://github.com/fishaudio/fish-audio-python/issues/26) | `(stub) documented gotcha in --help for --format wav; no client-side re-encode (would require bundling ffmpeg)` | Known pain point flagged so users don't debug it blind; fix deferred as out of scope for a thin CLI. |
| 13 | Latency/quality tradeoff (`low`/`balanced`/`normal`) | OpenAPI spec `TTSRequest.latency` | `fish-audio-pp-cli tts render --latency` | Direct pass-through. |
| 14 | ASR transcribe (multipart or msgpack, language hint, timestamps toggle) | fish-audio-python `client.asr.transcribe()`, fish-audio-go `ASR.Transcribe()` | `fish-audio-pp-cli asr transcribe --audio <file> [--language] [--timestamps]` | Adds `--timestamps` as an opt-in flag (API defaults `ignore_timestamps=true`, i.e. timestamps OFF by default) rather than leaving the inverted-default flag as-is. |
| 15 | Intermittent ASR 503s under load | GitHub issue [fishaudio/fish-audio-python#119](https://github.com/fishaudio/fish-audio-python/issues/119) | `(behavior in fish-audio-pp-cli asr transcribe)` retry-with-backoff on 503, documented in --help | Community-observed instability the raw endpoint doesn't self-mitigate. |
| 16 | Voice design: instruction-based candidate generation | OpenAPI spec `/v1/voice-design`, fish-audio-python (no dedicated resource — spec-only) | `fish-audio-pp-cli voice design --instruction "..." [--n --speed --num-step --guidance-scale --seed]` | Direct pass-through; we're ahead of the SDKs, which don't wrap this endpoint yet. |
| 17 | Voice-design candidate → permanent model promotion via `voice_design_signatures` | OpenAPI spec `POST /model` (`voice_design_signatures` field, order-matched to `voices[]`) | `fish-audio-pp-cli voice design-save --candidate-id <id>` | Chains what's a two-step, order-sensitive raw API sequence (design, then re-upload with matched signatures) into one command. |
| 18 | Voice clone from uploaded audio (`POST /model`, multipart) | fish-audio-python `client.voices.create()`, elevenlabs-pp-cli `voices add` (IVC equivalent) | `fish-audio-pp-cli voice clone --audio <file>... [--title --tags --train-mode fast]` | Defaults `train_mode=fast` (instant availability) rather than the API's entity-level `full` default, matching the actual Pearl use case (fast iteration, not archival quality). |
| 19 | Model list with filters (title, tag, self, author_id, language, title_language, licensed, sort_by, pagination) | OpenAPI spec `GET /model`, all 3 official SDKs | `fish-audio-pp-cli model list [--title --tag --self --author-id --language --licensed --sort-by]` | Direct pass-through; `--sort-by created_at` recommended in --help for stable pagination during a full sync (default `score` reorders over time). |
| 20 | Model get/update/delete | OpenAPI spec, all 3 official SDKs | `fish-audio-pp-cli model get/update/delete <id>` | Generated CRUD, 1:1. |
| 21 | Model list local cache for offline/fast lookup | elevenlabs-pp-cli SQLite-backed offline store + `voice discover` | `fish-audio-pp-cli voice discover --query "<text>" [--source self|public|all]` | Fish Audio has no server-side full-text voice search; this is a client-side cache + fuzzy match over the synced `/model` list, same pattern as ElevenLabs pp-cli's `voice discover`. |
| 22 | Pre-flight validation before spending a render call | elevenlabs-pp-cli `tts resolve` | `fish-audio-pp-cli tts resolve --voice <id> --model <id>` | Resolves voice existence, model validity, and estimated cost before an actual render call. |
| 23 | Wallet / dev API credit balance | OpenAPI spec `GET /wallet/{user_id}/api-credit`, fish-audio-python `client.account.get_credits()` | `fish-audio-pp-cli wallet credit [--check-free-credit] [--team-id]` | Direct pass-through, `user_id` defaults to `self` so no ID lookup needed. |
| 24 | Wallet / subscription package balance | OpenAPI spec `GET /wallet/{user_id}/package`, fish-audio-python `client.account.get_package()` | `fish-audio-pp-cli wallet package` | Direct pass-through. |
| 25 | Combined two-ledger balance view (credit + package in one call) | Community pain point: dual billing model is confusing (smallest.ai review) | `fish-audio-pp-cli wallet balance` | New command not present in any SDK — calls both endpoints, labels dev-API-per-byte vs subscription-credit-pool explicitly so the two ledgers aren't conflated. |
| 26 | Per-render cost computation ($15/M UTF-8 bytes paid, $0/M free tier) | docs.fish.audio `pricing-and-rate-limits.md` | `(behavior in fish-audio-pp-cli tts render)` manifest includes computed `cost_usd` field | No SDK computes this client-side; users currently have to do the math themselves. Directly serves Jon's "cost tracking vs ElevenLabs" ask. |
| 27 | ASR cost computation ($0.36/audio-hour, rounded to nearest second) | docs.fish.audio `pricing-and-rate-limits.md` | `(behavior in fish-audio-pp-cli asr transcribe)` manifest includes computed `cost_usd` field | Same rationale as #26. |
| 28 | Model-string deprecation warning | fish-audio-go `Model` enum `// Deprecated` comments, fish-audio-python `warn_if_deprecated_model()` | `(behavior in fish-audio-pp-cli tts render)` warns to stderr if `--model` matches a known-deprecated string (speech-1.5, speech-1.6) | Surfaces vendor-side deprecations proactively instead of silent fallback-to-default. |
| 29 | Rate-limit-aware retry (concurrency-tier based, not flat RPM) | docs.fish.audio `pricing-and-rate-limits.md` (5/15/50 concurrent slots by spend tier) | `(behavior in fish-audio-pp-cli tts batch)` bounded-concurrency worker pool + 429 backoff, configurable `--concurrency` | Batch command respects the documented concurrency-slot model instead of firing unbounded parallel requests. |
| 30 | Multi-line/file batch TTS to a directory of numbered outputs | elevenlabs-pp-cli has no direct equivalent (closest: `dialogue cast`); new for Fish Audio | `fish-audio-pp-cli tts batch --input <file|dir> --out-dir <dir>` | Directly serves Jon's "batch TTS to files" ask; aggregate cost summary printed at end. |
| 31 | Official hosted MCP server parity (voice browse, TTS, ASR, credit check) | fishaudio/docs `overview/mcp.mdx` (`https://api.fish.audio/mcp`) | `(stub) not absorbed — official MCP bills against plan/package credits (web-app ledger), a different accounting path than our dev-API-key CLI; noting for awareness, not building` | Avoids conflating two separate billing paths under one CLI. |
| 32 | Community MCP tool: `fish_audio_tts` (text, reference selection, streaming/WS/realtime-play, output-path, auto-play) | da-okazaki/mcp-fish-audio-server `src/tools/tts.ts` | `fish-audio-pp-cli tts render` (covers text/reference/format/output-path); `--play` local playback flag | Absorbs the local-playback convenience (`auto_play`) that neither official SDK provides as a CLI-level flag. |
| 33 | Community MCP tool: `fish_audio_list_references` (env-var-configured voice aliases, not live lookup) | da-okazaki/mcp-fish-audio-server `src/tools/listReferences.ts` | `(behavior in fish-audio-pp-cli voice discover)` supersedes with a live `/model` lookup instead of static env-var config | We fix the gap the community tool leaves open: live voice-library browsing, not a config-only alias list. |
| 34 | `--agent` compact-output flag (json+compact+no-input+no-color+yes) | elevenlabs-pp-cli | `fish-audio-pp-cli --agent` (global flag, all commands) | Direct pattern reuse for agent-loop ergonomics. |
| 35 | `--select field1,field2` output field filter | elevenlabs-pp-cli | `fish-audio-pp-cli --select <fields>` (global flag) | Direct pattern reuse. |
| 36 | `--dry-run` preview without sending | elevenlabs-pp-cli | `fish-audio-pp-cli --dry-run` (global flag) | Direct pattern reuse. |
| 37 | `--deliver <sink>` (stdout/file/webhook) output routing | elevenlabs-pp-cli | `fish-audio-pp-cli --deliver stdout\|file:<path>\|webhook:<url>` (global flag) | Direct pattern reuse. |
| 38 | Named config profiles (`profile save/list/show/delete`) | elevenlabs-pp-cli | `fish-audio-pp-cli profile save/list/show/delete <name>` | Direct pattern reuse for scheduled/repeated jobs. |
| 39 | Local friction log (`feedback`, never auto-sent) | elevenlabs-pp-cli | `fish-audio-pp-cli feedback [--stdin] / feedback list` | Direct pattern reuse. |
| 40 | `auth setup [--launch]`, `doctor` setup verification | elevenlabs-pp-cli | `fish-audio-pp-cli auth setup [--launch]`, `fish-audio-pp-cli doctor` | Direct pattern reuse; `doctor` also checks `FISH_AUDIO_API_KEY` is set and the wallet endpoint is reachable. |
| 41 | Natural-language command resolver (`which "<capability>"`) | elevenlabs-pp-cli | `fish-audio-pp-cli which "<capability in plain words>"` | Direct pattern reuse over our smaller command index. |
| 42 | Standard exit-code contract (0/2/3/4/5/7/10) | elevenlabs-pp-cli | `fish-audio-pp-cli` — same exit code table | Direct pattern reuse for scripting consistency across the Printing Press library. |
| 43 | ElevenLabs Agents/Conversational-AI platform (agent CRUD, phone numbers, batch calling, knowledge base, tools, conversations) | elevenlabs-pp-cli `convai` (~100 subcommands) | `(stub) out of scope — Agents/phone product explicitly excluded per task scope` | N/A |
| 44 | ElevenLabs Dubbing (create/dub/translate/render, multi-language) | elevenlabs-pp-cli `dubbing` | `(stub) out of scope — Fish Audio has no dubbing endpoint in the in-scope spec` | N/A |
| 45 | ElevenLabs Studio (Projects/Chapters, podcast auto-convert) | elevenlabs-pp-cli `studio` | `(stub) out of scope — no Fish Audio equivalent endpoint` | N/A |
| 46 | ElevenLabs Music generation | elevenlabs-pp-cli `music` | `(stub) out of scope — no Fish Audio equivalent endpoint` | N/A |
| 47 | ElevenLabs Sound generation (text-to-sound-effects) | elevenlabs-pp-cli `sound-generation` | `(stub) out of scope — no Fish Audio equivalent endpoint` | N/A |
| 48 | ElevenLabs Pronunciation dictionaries | elevenlabs-pp-cli `pronunciation-dictionaries` | `(stub) out of scope — no Fish Audio equivalent endpoint` | N/A |
| 49 | ElevenLabs speech-to-speech (voice conversion) | elevenlabs-pp-cli `speech-to-speech` | `(stub) out of scope — no Fish Audio equivalent endpoint in scope` | N/A |
| 50 | ElevenLabs Forced Alignment (word/char timing to transcript) | elevenlabs-pp-cli `forced-alignment` | `(stub) partially covered — Fish Audio's SSE-with-timestamp endpoint (#3) provides alignment at generation time, not for arbitrary pre-existing audio+transcript pairs; no direct equivalent` | N/A |
| 51 | ElevenLabs generation history (list/download/delete past speech items) | elevenlabs-pp-cli `history` | `(stub) no Fish Audio server-side history resource exists; local SQLite render-log (see Data Layer §FTS) is the closest we can offer` | Fish Audio doesn't persist generations server-side, so this is CLI-owned state, not a generated endpoint wrap. |
| 52 | ElevenLabs Workspace admin (members, webhooks, audit logs, usage-by-product) | elevenlabs-pp-cli `workspace` | `(stub) out of scope — no Fish Audio equivalent endpoint in the in-scope spec (wallet endpoints are the only account-level surface)` | N/A |

## Transcendence (only possible with our approach)
| # | Feature | Command | Score | Buildability | How It Works | Evidence | Long Description |
|---|---|---|---|---|---|---|---|
| 1 | Local render history | `render log` | 10/10 | hand-code | Reads local SQLite render-log (populated by every tts render/batch) listing text, model, voice, bytes, cost. | No GET /tts/history endpoint exists; User Vision cost-tracking ask | Use this command to list individual past TTS renders with their text, model, voice, bytes, and cost. Do NOT use this command for aggregated spend totals; use 'render spend' instead. |
| 2 | Skip identical re-renders | `tts render --skip-if-rendered` | 8/10 | hand-code | Hashes text+voice+model+params and checks render-log before POST /v1/tts. | Render-log stores request hash; Build Priority #3 | none |
| 3 | Grouped spend report | `render spend --group-by voice\|model\|day` | 9/10 | hand-code | GROUP BY over local render-log with cost totals. | User Vision cost tracking vs ElevenLabs | Use this command to view aggregated Fish Audio spend grouped by voice, model, or day. Do NOT use this command to see individual render records; use 'render log' instead. |
| 4 | Compare two renders | `render diff <id1> <id2>` | 6/10 | hand-code | Reads two render-log rows and computes cost/model/byte deltas. | Batch-iteration A/B across takes | Use this command to compare two specific past renders by log ID. Do NOT use this command for a full list or totals; use 'render log' or 'render spend' instead. |
| 5 | Clone fidelity check | `voice verify <model_id>` | 8/10 | hand-code | Renders a reference phrase via POST /v1/tts with the model, transcribes via POST /v1/asr, computes WER vs known text. | Top Workflow #2; need to trust clone before replacing live ElevenLabs voice | Use this command to test a cloned voice's fidelity by rendering a reference phrase and checking its ASR transcription. Do NOT use this command to create a clone; use 'voice clone' instead. |
| 6 | Pre-batch budget guard | `tts batch --budget-guard` | 7/10 | hand-code | Sums estimated batch cost, checks GET /wallet/self/api-credit, refuses if insufficient. | Top Workflow #5; dual-ledger confusion | none |



## Sources

| Name | GitHub URL | Language | Stars | Feature Count Contributed |
|---|---|---|---|---|
| fish-audio-python (`fishaudio`/`fish_audio_sdk`) | https://github.com/fishaudio/fish-audio-python | Python | 211 | 9 (rows 1,2,4,5,6,14,16,18,19,20,23,24,28) |
| fish-audio-typescript | https://github.com/fishaudio/fish-audio-typescript | TypeScript | 14 | 3 (rows 4,7,18) |
| fish-audio-go | https://github.com/fishaudio/fish-audio-go | Go | 7 | 4 (rows 4,14,19,28) |
| Fish Audio OpenAPI spec (docs.fish.audio) | https://docs.fish.audio/api-reference/openapi.json | — | — | 12 (rows 3,5,6,7,8,9,10,11,13,16,17,23,24) |
| Fish Audio pricing/rate-limits docs | https://docs.fish.audio/developer-guide/models-pricing/pricing-and-rate-limits.md | — | — | 3 (rows 26,27,29) |
| Fish Audio official MCP server docs | https://github.com/fishaudio/docs (overview/mcp.mdx) | — | — | 1 (row 31) |
| da-okazaki/mcp-fish-audio-server | https://github.com/da-okazaki/mcp-fish-audio-server | TypeScript | 13 | 2 (rows 32,33) |
| elevenlabs-pp-cli (mvanhorn/printing-press-library) | https://github.com/mvanhorn/printing-press-library | Go | 1,965 | 19 (rows 18,21,22,34-42,43-52) |
| elevenlabs/cli (official) | https://github.com/elevenlabs/cli | Rust | 80 | referenced for scope contrast only, no rows absorbed directly (agents-as-code workflow out of scope) |
| elevenlabs-python (official SDK) | https://github.com/elevenlabs/elevenlabs-python | Python | — | referenced for Studio/Dubbing/Speech-Engine scope contrast (rows 43-52), no rows absorbed directly |
| GitHub issue: WAV frame-count bug | https://github.com/fishaudio/fish-audio-python/issues/26 | — | — | 1 (row 12) |
| GitHub issue: intermittent ASR 503 | https://github.com/fishaudio/fish-audio-python/issues/119 | — | — | 1 (row 15) |
| smallest.ai Fish Audio pricing review | https://smallest.ai/blog/fish-audio-pricing-plans-api-billing-commercial-use-in-2026 | — | — | 1 (row 25) |
