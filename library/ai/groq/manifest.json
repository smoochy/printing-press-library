{
  "manifest_version": "0.3",
  "name": "groq-pp-mcp",
  "display_name": "Groq Cloud",
  "version": "4.31.1",
  "description": "Every Groq endpoint in your terminal, plus a local ledger that tracks token cost and rate-limit budget.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/groq-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/groq-pp-mcp",
      "args": [],
      "env": {
        "PRINTING_PRESS_CLIENT_PROFILE": "${user_config.printing_press_client_profile}",
        "GROQ_API_KEY": "${user_config.groq_api_key}"
      }
    }
  },
  "user_config": {
    "printing_press_client_profile": {
      "type": "string",
      "title": "Client profile",
      "description": "Binds the MCP server to an existing tenant-gated Printing Press client profile.",
      "required": true
    },
    "groq_api_key": {
      "type": "string",
      "title": "Groq API Key",
      "description": "Groq API key (create one at console.groq.com/keys).",
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}