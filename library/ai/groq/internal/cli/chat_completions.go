// Copyright 2026 Som Samantray and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/ai/groq/internal/cliutil"
	"github.com/spf13/cobra"
)

func newChatCompletionsCmd(flags *rootFlags) *cobra.Command {
	var bodyCitationOptions string
	var bodyCompoundCustomModelsAnsweringModel string
	var bodyCompoundCustomModelsReasoningModel string
	var bodyCompoundCustomToolsEnabledTools string
	var bodyCustomTemplatePreserveSystemMessageOrder bool
	var bodyCustomTemplateRelaxedMessageOrdering bool
	var bodyCustomTemplateTemplate string
	var bodyDebug bool
	var bodyDisableToolValidation bool
	var bodyDocuments string
	var bodyExcludeDomains string
	var bodyExcludeInstanceIds string
	var bodyFrequencyPenalty float64
	var bodyFunctionCall string
	var bodyFunctions string
	var bodyIncludeDomains string
	var bodyIncludeReasoning bool
	var bodyLogitBias string
	var bodyLogprobs bool
	var bodyMaxCompletionTokens int
	var bodyMaxTokens int
	var bodyMessages string
	var bodyMetadata string
	var bodyModel string
	var bodyN int
	var bodyParallelToolCalls bool
	var bodyPresencePenalty float64
	var bodyReasoningEffort string
	var bodyReasoningFormat string
	var bodyResponseFormat string
	var bodySearchSettingsCountry string
	var bodySearchSettingsExcludeDomains string
	var bodySearchSettingsIncludeDomains string
	var bodySearchSettingsIncludeImages bool
	var bodySeed int
	var bodyServiceTier string
	var bodyStop string
	var bodyStore bool
	var bodyStream bool
	var bodyStreamOptionsIncludeUsage bool
	var bodyTemperature float64
	var bodyToolChoice string
	var bodyTools string
	var bodyTopLogprobs int
	var bodyTopP float64
	var bodyUser string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "completions",
		Aliases:     []string{"create"},
		Short:       "Creates a model response for the given chat conversation.",
		Example:     "  groq-pp-cli chat completions --model openai/gpt-oss-20b --messages '[{\"role\":\"user\",\"content\":\"Explain fast inference in one sentence\"}]'",
		Annotations: map[string]string{"pp:endpoint": "chat.completions", "pp:method": "POST", "pp:path": "/openai/v1/chat/completions"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if cmd.Flags().Changed("custom-template-preserve-system-message-order") || cmd.Flags().Changed("custom-template-relaxed-message-ordering") || (cmd.Flags().Changed("custom-template-template") || bodyCustomTemplateTemplate != "") {
					if !cmd.Flags().Changed("custom-template-template") && !flags.dryRun {
						return fmt.Errorf("required flag \"%s\" not set", "custom-template-template")
					}
				}
				if !cmd.Flags().Changed("messages") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "messages")
				}
				if !cmd.Flags().Changed("model") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "model")
				}
			}
			path := "/openai/v1/chat/completions"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("citation-options") || bodyCitationOptions != "" {
					bodyMap["citation_options"] = bodyCitationOptions
				}
				{
					nestedCompoundCustom := map[string]any{}
					{
						nestedCompoundCustomModels := map[string]any{}
						if cmd.Flags().Changed("compound-custom-models-answering-model") || bodyCompoundCustomModelsAnsweringModel != "" {
							nestedCompoundCustomModels["answering_model"] = bodyCompoundCustomModelsAnsweringModel
						}
						if cmd.Flags().Changed("compound-custom-models-reasoning-model") || bodyCompoundCustomModelsReasoningModel != "" {
							nestedCompoundCustomModels["reasoning_model"] = bodyCompoundCustomModelsReasoningModel
						}
						if len(nestedCompoundCustomModels) > 0 {
							nestedCompoundCustom["models"] = nestedCompoundCustomModels
						}
					}
					{
						nestedCompoundCustomTools := map[string]any{}
						if cmd.Flags().Changed("compound-custom-tools-enabled-tools") {
							parsedCompoundCustomToolsEnabledTools, parseErr := cliutil.ParseStringList(bodyCompoundCustomToolsEnabledTools)
							if parseErr != nil {
								return fmt.Errorf("parsing --compound-custom-tools-enabled-tools list: %w", parseErr)
							}
							nestedCompoundCustomTools["enabled_tools"] = parsedCompoundCustomToolsEnabledTools
						}
						if len(nestedCompoundCustomTools) > 0 {
							nestedCompoundCustom["tools"] = nestedCompoundCustomTools
						}
					}
					if len(nestedCompoundCustom) > 0 {
						bodyMap["compound_custom"] = nestedCompoundCustom
					}
				}
				{
					nestedCustomTemplate := map[string]any{}
					if cmd.Flags().Changed("custom-template-preserve-system-message-order") {
						nestedCustomTemplate["preserve_system_message_order"] = bodyCustomTemplatePreserveSystemMessageOrder
					}
					if cmd.Flags().Changed("custom-template-relaxed-message-ordering") {
						nestedCustomTemplate["relaxed_message_ordering"] = bodyCustomTemplateRelaxedMessageOrdering
					}
					if cmd.Flags().Changed("custom-template-template") || bodyCustomTemplateTemplate != "" {
						nestedCustomTemplate["template"] = bodyCustomTemplateTemplate
					}
					if len(nestedCustomTemplate) > 0 {
						bodyMap["custom_template"] = nestedCustomTemplate
					}
				}
				if cmd.Flags().Changed("debug") {
					bodyMap["debug"] = bodyDebug
				}
				if cmd.Flags().Changed("disable-tool-validation") {
					bodyMap["disable_tool_validation"] = bodyDisableToolValidation
				}
				if cmd.Flags().Changed("documents") || bodyDocuments != "" {
					var parsedDocuments any
					if err := json.Unmarshal([]byte(bodyDocuments), &parsedDocuments); err != nil {
						return fmt.Errorf("parsing --documents JSON: %w", err)
					}
					asArray, ok := parsedDocuments.([]any)
					if !ok {
						return fmt.Errorf("--documents must be a JSON array, got JSON %T", parsedDocuments)
					}
					bodyMap["documents"] = asArray
				}
				if cmd.Flags().Changed("exclude-domains") {
					parsedExcludeDomains, parseErr := cliutil.ParseStringList(bodyExcludeDomains)
					if parseErr != nil {
						return fmt.Errorf("parsing --exclude-domains list: %w", parseErr)
					}
					bodyMap["exclude_domains"] = parsedExcludeDomains
				}
				if cmd.Flags().Changed("exclude-instance-ids") {
					parsedExcludeInstanceIds, parseErr := cliutil.ParseStringList(bodyExcludeInstanceIds)
					if parseErr != nil {
						return fmt.Errorf("parsing --exclude-instance-ids list: %w", parseErr)
					}
					bodyMap["exclude_instance_ids"] = parsedExcludeInstanceIds
				}
				if cmd.Flags().Changed("frequency-penalty") || bodyFrequencyPenalty != 0.0 {
					bodyMap["frequency_penalty"] = bodyFrequencyPenalty
				}
				if cmd.Flags().Changed("function-call") || bodyFunctionCall != "" {
					if looksLikeJSONComposite(bodyFunctionCall) {
						var parsedFunctionCall any
						if err := json.Unmarshal([]byte(bodyFunctionCall), &parsedFunctionCall); err != nil {
							return fmt.Errorf("parsing --function-call JSON: %w", err)
						}
						bodyMap["function_call"] = parsedFunctionCall
					} else {
						bodyMap["function_call"] = bodyFunctionCall
					}
				}
				if cmd.Flags().Changed("functions") || bodyFunctions != "" {
					var parsedFunctions any
					if err := json.Unmarshal([]byte(bodyFunctions), &parsedFunctions); err != nil {
						return fmt.Errorf("parsing --functions JSON: %w", err)
					}
					asArray, ok := parsedFunctions.([]any)
					if !ok {
						return fmt.Errorf("--functions must be a JSON array, got JSON %T", parsedFunctions)
					}
					bodyMap["functions"] = asArray
				}
				if cmd.Flags().Changed("include-domains") {
					parsedIncludeDomains, parseErr := cliutil.ParseStringList(bodyIncludeDomains)
					if parseErr != nil {
						return fmt.Errorf("parsing --include-domains list: %w", parseErr)
					}
					bodyMap["include_domains"] = parsedIncludeDomains
				}
				if cmd.Flags().Changed("include-reasoning") {
					bodyMap["include_reasoning"] = bodyIncludeReasoning
				}
				if cmd.Flags().Changed("logit-bias") || bodyLogitBias != "" {
					var parsedLogitBias any
					if err := json.Unmarshal([]byte(bodyLogitBias), &parsedLogitBias); err != nil {
						return fmt.Errorf("parsing --logit-bias JSON: %w", err)
					}
					asMap, ok := parsedLogitBias.(map[string]any)
					if !ok {
						return fmt.Errorf("--logit-bias must be a JSON object, got JSON %T", parsedLogitBias)
					}
					bodyMap["logit_bias"] = asMap
				}
				if cmd.Flags().Changed("logprobs") {
					bodyMap["logprobs"] = bodyLogprobs
				}
				if cmd.Flags().Changed("max-completion-tokens") || bodyMaxCompletionTokens != 0 {
					bodyMap["max_completion_tokens"] = bodyMaxCompletionTokens
				}
				if cmd.Flags().Changed("max-tokens") || bodyMaxTokens != 0 {
					bodyMap["max_tokens"] = bodyMaxTokens
				}
				if cmd.Flags().Changed("messages") || bodyMessages != "" {
					var parsedMessages any
					if err := json.Unmarshal([]byte(bodyMessages), &parsedMessages); err != nil {
						return fmt.Errorf("parsing --messages JSON: %w", err)
					}
					asArray, ok := parsedMessages.([]any)
					if !ok {
						return fmt.Errorf("--messages must be a JSON array, got JSON %T", parsedMessages)
					}
					bodyMap["messages"] = asArray
				}
				if cmd.Flags().Changed("metadata") || bodyMetadata != "" {
					var parsedMetadata any
					if err := json.Unmarshal([]byte(bodyMetadata), &parsedMetadata); err != nil {
						return fmt.Errorf("parsing --metadata JSON: %w", err)
					}
					asMap, ok := parsedMetadata.(map[string]any)
					if !ok {
						return fmt.Errorf("--metadata must be a JSON object, got JSON %T", parsedMetadata)
					}
					bodyMap["metadata"] = asMap
				}
				if cmd.Flags().Changed("model") || bodyModel != "" {
					bodyMap["model"] = bodyModel
				}
				if cmd.Flags().Changed("n") || bodyN != 0 {
					bodyMap["n"] = bodyN
				}
				if cmd.Flags().Changed("parallel-tool-calls") {
					bodyMap["parallel_tool_calls"] = bodyParallelToolCalls
				}
				if cmd.Flags().Changed("presence-penalty") || bodyPresencePenalty != 0.0 {
					bodyMap["presence_penalty"] = bodyPresencePenalty
				}
				if cmd.Flags().Changed("reasoning-effort") || bodyReasoningEffort != "" {
					bodyMap["reasoning_effort"] = bodyReasoningEffort
				}
				if cmd.Flags().Changed("reasoning-format") || bodyReasoningFormat != "" {
					bodyMap["reasoning_format"] = bodyReasoningFormat
				}
				if cmd.Flags().Changed("response-format") || bodyResponseFormat != "" {
					if looksLikeJSONComposite(bodyResponseFormat) {
						var parsedResponseFormat any
						if err := json.Unmarshal([]byte(bodyResponseFormat), &parsedResponseFormat); err != nil {
							return fmt.Errorf("parsing --response-format JSON: %w", err)
						}
						bodyMap["response_format"] = parsedResponseFormat
					} else {
						bodyMap["response_format"] = bodyResponseFormat
					}
				}
				{
					nestedSearchSettings := map[string]any{}
					if cmd.Flags().Changed("search-settings-country") || bodySearchSettingsCountry != "" {
						nestedSearchSettings["country"] = bodySearchSettingsCountry
					}
					if cmd.Flags().Changed("search-settings-exclude-domains") {
						parsedSearchSettingsExcludeDomains, parseErr := cliutil.ParseStringList(bodySearchSettingsExcludeDomains)
						if parseErr != nil {
							return fmt.Errorf("parsing --search-settings-exclude-domains list: %w", parseErr)
						}
						nestedSearchSettings["exclude_domains"] = parsedSearchSettingsExcludeDomains
					}
					if cmd.Flags().Changed("search-settings-include-domains") {
						parsedSearchSettingsIncludeDomains, parseErr := cliutil.ParseStringList(bodySearchSettingsIncludeDomains)
						if parseErr != nil {
							return fmt.Errorf("parsing --search-settings-include-domains list: %w", parseErr)
						}
						nestedSearchSettings["include_domains"] = parsedSearchSettingsIncludeDomains
					}
					if cmd.Flags().Changed("search-settings-include-images") {
						nestedSearchSettings["include_images"] = bodySearchSettingsIncludeImages
					}
					if len(nestedSearchSettings) > 0 {
						bodyMap["search_settings"] = nestedSearchSettings
					}
				}
				if cmd.Flags().Changed("seed") || bodySeed != 0 {
					bodyMap["seed"] = bodySeed
				}
				if cmd.Flags().Changed("service-tier") || bodyServiceTier != "" {
					bodyMap["service_tier"] = bodyServiceTier
				}
				if cmd.Flags().Changed("stop") || bodyStop != "" {
					if looksLikeJSONComposite(bodyStop) {
						var parsedStop any
						if err := json.Unmarshal([]byte(bodyStop), &parsedStop); err != nil {
							return fmt.Errorf("parsing --stop JSON: %w", err)
						}
						bodyMap["stop"] = parsedStop
					} else {
						bodyMap["stop"] = bodyStop
					}
				}
				if cmd.Flags().Changed("store") {
					bodyMap["store"] = bodyStore
				}
				if cmd.Flags().Changed("stream") {
					bodyMap["stream"] = bodyStream
				}
				{
					nestedStreamOptions := map[string]any{}
					if cmd.Flags().Changed("stream-options-include-usage") {
						nestedStreamOptions["include_usage"] = bodyStreamOptionsIncludeUsage
					}
					if len(nestedStreamOptions) > 0 {
						bodyMap["stream_options"] = nestedStreamOptions
					}
				}
				if cmd.Flags().Changed("temperature") || bodyTemperature != 0.0 {
					bodyMap["temperature"] = bodyTemperature
				}
				if cmd.Flags().Changed("tool-choice") || bodyToolChoice != "" {
					if looksLikeJSONComposite(bodyToolChoice) {
						var parsedToolChoice any
						if err := json.Unmarshal([]byte(bodyToolChoice), &parsedToolChoice); err != nil {
							return fmt.Errorf("parsing --tool-choice JSON: %w", err)
						}
						bodyMap["tool_choice"] = parsedToolChoice
					} else {
						bodyMap["tool_choice"] = bodyToolChoice
					}
				}
				if cmd.Flags().Changed("tools") || bodyTools != "" {
					var parsedTools any
					if err := json.Unmarshal([]byte(bodyTools), &parsedTools); err != nil {
						return fmt.Errorf("parsing --tools JSON: %w", err)
					}
					asArray, ok := parsedTools.([]any)
					if !ok {
						return fmt.Errorf("--tools must be a JSON array, got JSON %T", parsedTools)
					}
					bodyMap["tools"] = asArray
				}
				if cmd.Flags().Changed("top-logprobs") || bodyTopLogprobs != 0 {
					bodyMap["top_logprobs"] = bodyTopLogprobs
				}
				if cmd.Flags().Changed("top-p") || bodyTopP != 0.0 {
					bodyMap["top_p"] = bodyTopP
				}
				if cmd.Flags().Changed("user") || bodyUser != "" {
					bodyMap["user"] = bodyUser
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "chat", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "chat", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "chat", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "chat", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "chat", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "chat",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"choices": true, "created": true, "id": true, "mcp_list_tools": true, "model": true, "object": true, "service_tier": true, "system_fingerprint": true, "usage": true, "usage_breakdown": true, "x_groq": true})
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "chat", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "chat", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyCitationOptions, "citation-options", "", "Whether to enable citations in the response (only supported on models with compound features).")
	cmd.Flags().StringVar(&bodyCompoundCustomModelsAnsweringModel, "compound-custom-models-answering-model", "", "Custom model to use for answering.")
	cmd.Flags().StringVar(&bodyCompoundCustomModelsReasoningModel, "compound-custom-models-reasoning-model", "", "Custom model to use for reasoning.")
	cmd.Flags().StringVar(&bodyCompoundCustomToolsEnabledTools, "compound-custom-tools-enabled-tools", "", "A list of tool names that are enabled for the request.")
	cmd.Flags().BoolVar(&bodyCustomTemplatePreserveSystemMessageOrder, "custom-template-preserve-system-message-order", false, "Whether to preserve the order of system messages.")
	cmd.Flags().BoolVar(&bodyCustomTemplateRelaxedMessageOrdering, "custom-template-relaxed-message-ordering", false, "Whether to allow relaxed message ordering in the conversation.")
	cmd.Flags().StringVar(&bodyCustomTemplateTemplate, "custom-template-template", "", "The custom template string to use for formatting messages.")
	cmd.Flags().BoolVar(&bodyDebug, "debug", false, "output input and output token IDs in xgroq debug field")
	cmd.Flags().BoolVar(&bodyDisableToolValidation, "disable-tool-validation", false, "If set to true, groq will return called tools without validating that the tool is present in request.tools.")
	cmd.Flags().StringVar(&bodyDocuments, "documents", "", "A list of documents to provide context for the conversation.")
	cmd.Flags().StringVar(&bodyExcludeDomains, "exclude-domains", "", "Deprecated: Use search_settings.exclude_domains instead.")
	cmd.Flags().StringVar(&bodyExcludeInstanceIds, "exclude-instance-ids", "", "For internal use only")
	cmd.Flags().Float64Var(&bodyFrequencyPenalty, "frequency-penalty", 0.000000, "This is not yet supported by any of our models. Number between -2.0 and 2.0.")
	cmd.Flags().StringVar(&bodyFunctionCall, "function-call", "", "Deprecated in favor of `tool_choice`. Controls which (if any) function is called by the model.")
	cmd.Flags().StringVar(&bodyFunctions, "functions", "", "Deprecated in favor of `tools`. A list of functions the model may generate JSON inputs for.")
	cmd.Flags().StringVar(&bodyIncludeDomains, "include-domains", "", "Deprecated: Use search_settings.include_domains instead.")
	cmd.Flags().BoolVar(&bodyIncludeReasoning, "include-reasoning", false, "Whether to include reasoning in the response. If true, the response will include a `reasoning` field.")
	cmd.Flags().StringVar(&bodyLogitBias, "logit-bias", "", "This is not yet supported by any of our models. Modify the likelihood of specified tokens appearing in the completion.")
	cmd.Flags().BoolVar(&bodyLogprobs, "logprobs", false, "This is not yet supported by any of our models. Whether to return log probabilities of the output tokens or not.")
	cmd.Flags().IntVar(&bodyMaxCompletionTokens, "max-completion-tokens", 0, "The maximum number of tokens that can be generated in the chat completion.")
	cmd.Flags().IntVar(&bodyMaxTokens, "max-tokens", 0, "Deprecated in favor of `max_completion_tokens`.")
	cmd.Flags().StringVar(&bodyMessages, "messages", "", "A list of messages comprising the conversation so far.")
	cmd.Flags().StringVar(&bodyMetadata, "metadata", "", "This parameter is not currently supported.")
	cmd.Flags().StringVar(&bodyModel, "model", "", "ID of the model to use.")
	cmd.Flags().IntVar(&bodyN, "n", 1, "How many chat completion choices to generate for each input message.")
	cmd.Flags().BoolVar(&bodyParallelToolCalls, "parallel-tool-calls", true, "Whether to enable parallel function calling during tool use.")
	cmd.Flags().Float64Var(&bodyPresencePenalty, "presence-penalty", 0.000000, "This is not yet supported by any of our models. Number between -2.0 and 2.0.")
	cmd.Flags().StringVar(&bodyReasoningEffort, "reasoning-effort", "", "qwen3 models support `none` to disable reasoning and `default` or null to use the model default. qwen/qwen3.")
	cmd.Flags().StringVar(&bodyReasoningFormat, "reasoning-format", "", "Specifies how to output reasoning tokens This field is mutually exclusive with `include_reasoning`.")
	cmd.Flags().StringVar(&bodyResponseFormat, "response-format", "", "An object specifying the format that the model must output. Setting to `{ 'type': 'json_schema', 'json_schema': {...")
	cmd.Flags().StringVar(&bodySearchSettingsCountry, "search-settings-country", "", "Name of country to prioritize search results from (e.g., 'united states', 'germany', 'france').")
	cmd.Flags().StringVar(&bodySearchSettingsExcludeDomains, "search-settings-exclude-domains", "", "A list of domains to exclude from the search results.")
	cmd.Flags().StringVar(&bodySearchSettingsIncludeDomains, "search-settings-include-domains", "", "A list of domains to include in the search results.")
	cmd.Flags().BoolVar(&bodySearchSettingsIncludeImages, "search-settings-include-images", false, "Whether to include images in the search results.")
	cmd.Flags().IntVar(&bodySeed, "seed", 0, "If specified, our system will make a best effort to sample deterministically")
	cmd.Flags().StringVar(&bodyServiceTier, "service-tier", "", "The service tier to use for the request. Defaults to `on_demand`.")
	cmd.Flags().StringVar(&bodyStop, "stop", "", "Up to 4 sequences where the API will stop generating further tokens.")
	cmd.Flags().BoolVar(&bodyStore, "store", false, "This parameter is not currently supported.")
	cmd.Flags().BoolVar(&bodyStream, "stream", false, "If set, partial message deltas will be sent. Tokens will be sent as data-only [server-sent events](https://developer.")
	cmd.Flags().BoolVar(&bodyStreamOptionsIncludeUsage, "stream-options-include-usage", false, "If set, an additional chunk will be streamed before the `data: [DONE]` message.")
	cmd.Flags().Float64Var(&bodyTemperature, "temperature", 1.000000, "What sampling temperature to use, between 0 and 2. Higher values like 0.")
	cmd.Flags().StringVar(&bodyToolChoice, "tool-choice", "", "Controls which (if any) tool is called by the model.")
	cmd.Flags().StringVar(&bodyTools, "tools", "", "A list of tools the model may call. Currently, only functions are supported as a tool.")
	cmd.Flags().IntVar(&bodyTopLogprobs, "top-logprobs", 0, "This is not yet supported by any of our models.")
	cmd.Flags().Float64Var(&bodyTopP, "top-p", 1.000000, "An alternative to sampling with temperature, called nucleus sampling")
	cmd.Flags().StringVar(&bodyUser, "user", "", "A unique identifier representing your end-user, which can help us monitor and detect abuse.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin (use this for deeply nested fields not exposed as flags)")

	return cmd
}
