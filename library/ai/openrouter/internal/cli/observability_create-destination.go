// Copyright 2026 Rick van de Laar and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/ai/openrouter/internal/cliutil"
	"github.com/spf13/cobra"
)

func newObservabilityCreateDestinationCmd(flags *rootFlags) *cobra.Command {
	var flagHTTPReferer string
	var flagXOpenRouterTitle string
	var flagXOpenRouterCategories string
	var bodyApiKeyHashes string
	var bodyBroadcastGenerationCost bool
	var bodyBroadcastGenerationIdentity bool
	var bodyBroadcastGenerationRequestContext bool
	var bodyConfig string
	var bodyEnabled bool
	var bodyFilterRulesEnabled bool
	var bodyFilterRulesGroups string
	var bodyName string
	var bodyPrivacyMode bool
	var bodySamplingRate float64
	var bodyType string
	var bodyWorkspaceId string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-destination",
		Aliases:     []string{"create"},
		Short:       "Create a new observability destination. A maximum of 5 destinations per type is allowed.",
		Example:     "  openrouter-pp-cli observability create-destination --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "observability.create-destination", "pp:method": "POST", "pp:path": "/observability/destinations"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("config") && bodyConfig == "" && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "config")
				}
				if cmd.Flags().Changed("filter-rules-enabled") || (cmd.Flags().Changed("filter-rules-groups") || bodyFilterRulesGroups != "") {
					if !cmd.Flags().Changed("filter-rules-groups") && bodyFilterRulesGroups == "" && !flags.dryRun {
						return fmt.Errorf("required flag \"%s\" not set", "filter-rules-groups")
					}
				}
				if !cmd.Flags().Changed("name") && bodyName == "" && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
				if !cmd.Flags().Changed("type") && bodyType == "" && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "type")
				}
			}
			path := "/observability/destinations"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			headerOverrides := map[string]string{}

			if cmd.Flags().Changed("http-referer") || flagHTTPReferer != "" {
				headerOverrides["HTTP-Referer"] = formatCLIParamValue(flagHTTPReferer)
			}

			if cmd.Flags().Changed("x-open-router-title") || flagXOpenRouterTitle != "" {
				headerOverrides["X-OpenRouter-Title"] = formatCLIParamValue(flagXOpenRouterTitle)
			}

			if cmd.Flags().Changed("x-open-router-categories") || flagXOpenRouterCategories != "" {
				headerOverrides["X-OpenRouter-Categories"] = formatCLIParamValue(flagXOpenRouterCategories)
			}

			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("api-key-hashes") {
					parsedApiKeyHashes, parseErr := cliutil.ParseStringList(bodyApiKeyHashes)
					if parseErr != nil {
						return fmt.Errorf("parsing --api-key-hashes list: %w", parseErr)
					}
					bodyMap["api_key_hashes"] = parsedApiKeyHashes
				}
				if cmd.Flags().Changed("broadcast-generation-cost") {
					bodyMap["broadcast_generation_cost"] = bodyBroadcastGenerationCost
				}
				if cmd.Flags().Changed("broadcast-generation-identity") {
					bodyMap["broadcast_generation_identity"] = bodyBroadcastGenerationIdentity
				}
				if cmd.Flags().Changed("broadcast-generation-request-context") {
					bodyMap["broadcast_generation_request_context"] = bodyBroadcastGenerationRequestContext
				}
				if cmd.Flags().Changed("config") || bodyConfig != "" {
					var parsedConfig any
					if err := json.Unmarshal([]byte(bodyConfig), &parsedConfig); err != nil {
						return fmt.Errorf("parsing --config JSON: %w", err)
					}
					asMap, ok := parsedConfig.(map[string]any)
					if !ok {
						return fmt.Errorf("--config must be a JSON object, got JSON %T", parsedConfig)
					}
					bodyMap["config"] = asMap
				}
				if cmd.Flags().Changed("enabled") {
					bodyMap["enabled"] = bodyEnabled
				}
				{
					nestedFilterRules := map[string]any{}
					if cmd.Flags().Changed("filter-rules-enabled") {
						nestedFilterRules["enabled"] = bodyFilterRulesEnabled
					}
					if cmd.Flags().Changed("filter-rules-groups") || bodyFilterRulesGroups != "" {
						var parsedFilterRulesGroups any
						if err := json.Unmarshal([]byte(bodyFilterRulesGroups), &parsedFilterRulesGroups); err != nil {
							return fmt.Errorf("parsing --filter-rules-groups JSON: %w", err)
						}
						asArray, ok := parsedFilterRulesGroups.([]any)
						if !ok {
							return fmt.Errorf("--filter-rules-groups must be a JSON array, got JSON %T", parsedFilterRulesGroups)
						}
						nestedFilterRules["groups"] = asArray
					}
					if len(nestedFilterRules) > 0 {
						bodyMap["filter_rules"] = nestedFilterRules
					}
				}
				if cmd.Flags().Changed("name") || bodyName != "" {
					bodyMap["name"] = bodyName
				}
				if cmd.Flags().Changed("privacy-mode") {
					bodyMap["privacy_mode"] = bodyPrivacyMode
				}
				if cmd.Flags().Changed("sampling-rate") || bodySamplingRate != 0.0 {
					bodyMap["sampling_rate"] = bodySamplingRate
				}
				if cmd.Flags().Changed("type") || bodyType != "" {
					bodyMap["type"] = bodyType
				}
				if cmd.Flags().Changed("workspace-id") || bodyWorkspaceId != "" {
					bodyMap["workspace_id"] = bodyWorkspaceId
				}
			}
			data, statusCode, err := c.PostWithParamsAndHeaders(cmd.Context(), path, params, body, headerOverrides)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "observability", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "observability", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "observability", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "observability", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "observability", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "observability",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Mutation-riding reads (POST search, RPC-over-POST lists) return
				// the same single-key collection envelopes as GET reads. Unwrap
				// before filtering so rows nest once under the result key and
				// --select filters rows, not envelope keys; plain created-object
				// responses pass through unwrapSingleKeyArray untouched.
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := unwrapSingleKeyArray(data)
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"data": true})
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "observability", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "observability", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagHTTPReferer, "http-referer", "", "The app identifier should be your app's URL and is used as the primary identifier for rankings.")
	cmd.Flags().StringVar(&flagXOpenRouterTitle, "x-open-router-title", "", "The app display name allows you to customize how your app appears in OpenRouter's dashboard.")
	cmd.Flags().StringVar(&flagXOpenRouterCategories, "x-open-router-categories", "", "Comma-separated list of app categories (e.g. 'cli-agent,cloud-agent'). Used for marketplace rankings.")
	cmd.Flags().StringVar(&bodyApiKeyHashes, "api-key-hashes", "", "Optional allowlist of OpenRouter API key hashes whose traffic is forwarded. `null` or omitted means all keys.")
	cmd.Flags().BoolVar(&bodyBroadcastGenerationCost, "broadcast-generation-cost", false, "When true, include cost and billing generation metadata.")
	cmd.Flags().BoolVar(&bodyBroadcastGenerationIdentity, "broadcast-generation-identity", false, "When true, include identity generation metadata.")
	cmd.Flags().BoolVar(&bodyBroadcastGenerationRequestContext, "broadcast-generation-request-context", false, "When true, include request-context generation metadata.")
	cmd.Flags().StringVar(&bodyConfig, "config", "", "Provider-specific configuration. The shape depends on `type` and is validated server-side.")
	cmd.Flags().BoolVar(&bodyEnabled, "enabled", true, "Whether this destination should be enabled immediately.")
	cmd.Flags().BoolVar(&bodyFilterRulesEnabled, "filter-rules-enabled", false, "Enabled")
	cmd.Flags().StringVar(&bodyFilterRulesGroups, "filter-rules-groups", "", "Groups")
	cmd.Flags().StringVar(&bodyName, "name", "", "Human-readable name for the destination.")
	cmd.Flags().BoolVar(&bodyPrivacyMode, "privacy-mode", false, "When true, request/response bodies are not forwarded — only metadata.")
	cmd.Flags().Float64Var(&bodySamplingRate, "sampling-rate", 0.0, "Sampling rate between 0.0001 and 1 (1 = 100%).")
	cmd.Flags().StringVar(&bodyType, "type", "", "The destination type. Only stable destination types are accepted.")
	cmd.Flags().StringVar(&bodyWorkspaceId, "workspace-id", "", "Optional workspace ID. Defaults to the authenticated entity's default workspace.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
