// Copyright 2026 Rick van de Laar and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/ai/openrouter/internal/cliutil"
	"github.com/spf13/cobra"
)

func newGuardrailsCreateCmd(flags *rootFlags) *cobra.Command {
	var flagHTTPReferer string
	var flagXOpenRouterTitle string
	var flagXOpenRouterCategories string
	var bodyAllowedModels string
	var bodyAllowedProviders string
	var bodyContentFilterBuiltins string
	var bodyContentFilters string
	var bodyDescription string
	var bodyEnableFreeModelPublication bool
	var bodyEnableFreeModelTraining bool
	var bodyEnablePaidModelTraining bool
	var bodyEnforceZdr bool
	var bodyEnforceZdrAnthropic bool
	var bodyEnforceZdrGoogle bool
	var bodyEnforceZdrOpenai bool
	var bodyEnforceZdrOther bool
	var bodyEnforceZdrXai bool
	var bodyIgnoredModels string
	var bodyIgnoredProviders string
	var bodyIncludeByokInBudgets bool
	var bodyLimitUsd float64
	var bodyName string
	var bodyResetInterval string
	var bodyWorkspaceId string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create a new guardrail for the authenticated user.",
		Example:     "  openrouter-pp-cli guardrails create --name example-resource",
		Annotations: map[string]string{"pp:endpoint": "guardrails.create", "pp:method": "POST", "pp:path": "/guardrails"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("name") && bodyName == "" && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "name")
				}
			}
			path := "/guardrails"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			headerOverrides := map[string]string{}

			if cmd.Flags().Changed("http-referer") || flagHTTPReferer != "" {
				headerOverrides["HTTP-Referer"] = formatCLIParamValue(flagHTTPReferer)
			}

			if cmd.Flags().Changed("x-open-router-title") || flagXOpenRouterTitle != "" {
				headerOverrides["X-OpenRouter-Title"] = formatCLIParamValue(flagXOpenRouterTitle)
			}

			if cmd.Flags().Changed("x-open-router-categories") || flagXOpenRouterCategories != "" {
				headerOverrides["X-OpenRouter-Categories"] = formatCLIParamValue(flagXOpenRouterCategories)
			}

			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("allowed-models") {
					parsedAllowedModels, parseErr := cliutil.ParseStringList(bodyAllowedModels)
					if parseErr != nil {
						return fmt.Errorf("parsing --allowed-models list: %w", parseErr)
					}
					bodyMap["allowed_models"] = parsedAllowedModels
				}
				if cmd.Flags().Changed("allowed-providers") {
					parsedAllowedProviders, parseErr := cliutil.ParseStringList(bodyAllowedProviders)
					if parseErr != nil {
						return fmt.Errorf("parsing --allowed-providers list: %w", parseErr)
					}
					bodyMap["allowed_providers"] = parsedAllowedProviders
				}
				if cmd.Flags().Changed("content-filter-builtins") || bodyContentFilterBuiltins != "" {
					var parsedContentFilterBuiltins any
					if err := json.Unmarshal([]byte(bodyContentFilterBuiltins), &parsedContentFilterBuiltins); err != nil {
						return fmt.Errorf("parsing --content-filter-builtins JSON: %w", err)
					}
					asArray, ok := parsedContentFilterBuiltins.([]any)
					if !ok {
						return fmt.Errorf("--content-filter-builtins must be a JSON array, got JSON %T", parsedContentFilterBuiltins)
					}
					bodyMap["content_filter_builtins"] = asArray
				}
				if cmd.Flags().Changed("content-filters") || bodyContentFilters != "" {
					var parsedContentFilters any
					if err := json.Unmarshal([]byte(bodyContentFilters), &parsedContentFilters); err != nil {
						return fmt.Errorf("parsing --content-filters JSON: %w", err)
					}
					asArray, ok := parsedContentFilters.([]any)
					if !ok {
						return fmt.Errorf("--content-filters must be a JSON array, got JSON %T", parsedContentFilters)
					}
					bodyMap["content_filters"] = asArray
				}
				if cmd.Flags().Changed("description") || bodyDescription != "" {
					bodyMap["description"] = bodyDescription
				}
				if cmd.Flags().Changed("enable-free-model-publication") {
					bodyMap["enable_free_model_publication"] = bodyEnableFreeModelPublication
				}
				if cmd.Flags().Changed("enable-free-model-training") {
					bodyMap["enable_free_model_training"] = bodyEnableFreeModelTraining
				}
				if cmd.Flags().Changed("enable-paid-model-training") {
					bodyMap["enable_paid_model_training"] = bodyEnablePaidModelTraining
				}
				if cmd.Flags().Changed("enforce-zdr") {
					bodyMap["enforce_zdr"] = bodyEnforceZdr
				}
				if cmd.Flags().Changed("enforce-zdr-anthropic") {
					bodyMap["enforce_zdr_anthropic"] = bodyEnforceZdrAnthropic
				}
				if cmd.Flags().Changed("enforce-zdr-google") {
					bodyMap["enforce_zdr_google"] = bodyEnforceZdrGoogle
				}
				if cmd.Flags().Changed("enforce-zdr-openai") {
					bodyMap["enforce_zdr_openai"] = bodyEnforceZdrOpenai
				}
				if cmd.Flags().Changed("enforce-zdr-other") {
					bodyMap["enforce_zdr_other"] = bodyEnforceZdrOther
				}
				if cmd.Flags().Changed("enforce-zdr-xai") {
					bodyMap["enforce_zdr_xai"] = bodyEnforceZdrXai
				}
				if cmd.Flags().Changed("ignored-models") {
					parsedIgnoredModels, parseErr := cliutil.ParseStringList(bodyIgnoredModels)
					if parseErr != nil {
						return fmt.Errorf("parsing --ignored-models list: %w", parseErr)
					}
					bodyMap["ignored_models"] = parsedIgnoredModels
				}
				if cmd.Flags().Changed("ignored-providers") {
					parsedIgnoredProviders, parseErr := cliutil.ParseStringList(bodyIgnoredProviders)
					if parseErr != nil {
						return fmt.Errorf("parsing --ignored-providers list: %w", parseErr)
					}
					bodyMap["ignored_providers"] = parsedIgnoredProviders
				}
				if cmd.Flags().Changed("include-byok-in-budgets") {
					bodyMap["include_byok_in_budgets"] = bodyIncludeByokInBudgets
				}
				if cmd.Flags().Changed("limit-usd") || bodyLimitUsd != 0.0 {
					bodyMap["limit_usd"] = bodyLimitUsd
				}
				if cmd.Flags().Changed("name") || bodyName != "" {
					bodyMap["name"] = bodyName
				}
				if cmd.Flags().Changed("reset-interval") || bodyResetInterval != "" {
					bodyMap["reset_interval"] = bodyResetInterval
				}
				if cmd.Flags().Changed("workspace-id") || bodyWorkspaceId != "" {
					bodyMap["workspace_id"] = bodyWorkspaceId
				}
			}
			data, statusCode, err := c.PostWithParamsAndHeaders(cmd.Context(), path, params, body, headerOverrides)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "guardrails", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "guardrails", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "guardrails", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "guardrails", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "guardrails", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "guardrails",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Mutation-riding reads (POST search, RPC-over-POST lists) return
				// the same single-key collection envelopes as GET reads. Unwrap
				// before filtering so rows nest once under the result key and
				// --select filters rows, not envelope keys; plain created-object
				// responses pass through unwrapSingleKeyArray untouched.
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := unwrapSingleKeyArray(data)
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"data": true})
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "guardrails", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "guardrails", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagHTTPReferer, "http-referer", "", "The app identifier should be your app's URL and is used as the primary identifier for rankings.")
	cmd.Flags().StringVar(&flagXOpenRouterTitle, "x-open-router-title", "", "The app display name allows you to customize how your app appears in OpenRouter's dashboard.")
	cmd.Flags().StringVar(&flagXOpenRouterCategories, "x-open-router-categories", "", "Comma-separated list of app categories (e.g. 'cli-agent,cloud-agent'). Used for marketplace rankings.")
	cmd.Flags().StringVar(&bodyAllowedModels, "allowed-models", "", "Array of model identifiers (slug or canonical_slug accepted)")
	cmd.Flags().StringVar(&bodyAllowedProviders, "allowed-providers", "", "List of allowed provider IDs")
	cmd.Flags().StringVar(&bodyContentFilterBuiltins, "content-filter-builtins", "", "Builtin content filters to apply. Every builtin slug supports 'block', 'redact', and the detect-only 'flag' action.")
	cmd.Flags().StringVar(&bodyContentFilters, "content-filters", "", "Custom regex content filters to apply to request messages")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "Description of the guardrail")
	cmd.Flags().BoolVar(&bodyEnableFreeModelPublication, "enable-free-model-publication", false, "Whether this guardrail allows free endpoints that publish prompts.")
	cmd.Flags().BoolVar(&bodyEnableFreeModelTraining, "enable-free-model-training", false, "Whether this guardrail allows free endpoints that train on request data.")
	cmd.Flags().BoolVar(&bodyEnablePaidModelTraining, "enable-paid-model-training", false, "Whether this guardrail allows paid endpoints that train on request data.")
	cmd.Flags().BoolVar(&bodyEnforceZdr, "enforce-zdr", false, "Deprecated.")
	cmd.Flags().BoolVar(&bodyEnforceZdrAnthropic, "enforce-zdr-anthropic", false, "Whether to enforce zero data retention for Anthropic models. Falls back to enforce_zdr when not provided.")
	cmd.Flags().BoolVar(&bodyEnforceZdrGoogle, "enforce-zdr-google", false, "Whether to enforce zero data retention for Google models. Falls back to enforce_zdr when not provided.")
	cmd.Flags().BoolVar(&bodyEnforceZdrOpenai, "enforce-zdr-openai", false, "Whether to enforce zero data retention for OpenAI models. Falls back to enforce_zdr when not provided.")
	cmd.Flags().BoolVar(&bodyEnforceZdrOther, "enforce-zdr-other", false, "Whether to enforce zero data retention for models that are not from Anthropic, OpenAI, Google, or xAI.")
	cmd.Flags().BoolVar(&bodyEnforceZdrXai, "enforce-zdr-xai", false, "Whether to enforce zero data retention for xAI models. Falls back to enforce_zdr when not provided.")
	cmd.Flags().StringVar(&bodyIgnoredModels, "ignored-models", "", "Array of model identifiers to exclude from routing (slug or canonical_slug accepted)")
	cmd.Flags().StringVar(&bodyIgnoredProviders, "ignored-providers", "", "List of provider IDs to exclude from routing")
	cmd.Flags().BoolVar(&bodyIncludeByokInBudgets, "include-byok-in-budgets", false, "Whether BYOK (bring-your-own-key) inference spend counts toward this guardrail's limit_usd")
	cmd.Flags().Float64Var(&bodyLimitUsd, "limit-usd", 0.0, "Spending limit in USD.")
	cmd.Flags().StringVar(&bodyName, "name", "", "Name for the new guardrail")
	cmd.Flags().StringVar(&bodyResetInterval, "reset-interval", "", "Interval at which the limit resets (daily, weekly, monthly)")
	cmd.Flags().StringVar(&bodyWorkspaceId, "workspace-id", "", "The workspace to create the guardrail in.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
