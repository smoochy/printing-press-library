// Copyright 2026 Rick van de Laar and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newDatasetsGetSessionCostCmd(flags *rootFlags) *cobra.Command {
	var flagHTTPReferer string
	var flagXOpenRouterTitle string
	var flagXOpenRouterCategories string
	var flagAppSlug string
	var flagModel string
	var flagTurnRange string
	var flagLimit int
	var flagOffset string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-session-cost",
		Short:       "Returns weekly refreshed, aggregated cost-per-session cells for the published harnesses.",
		Example:     "  openrouter-pp-cli datasets get-session-cost",
		Annotations: map[string]string{"pp:endpoint": "datasets.get-session-cost", "pp:method": "GET", "pp:path": "/datasets/session-cost", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("turn-range") {
				allowedTurnRange := []string{"1-turn", "2-9-turns", "10-49-turns", "50-plus-turns"}
				validTurnRange := false
				for _, v := range allowedTurnRange {
					if flagTurnRange == v {
						validTurnRange = true
						break
					}
				}
				if !validTurnRange {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagTurnRange, "turn-range", allowedTurnRange)
				}
			}
			path := "/datasets/session-cost"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			headerOverrides := map[string]string{}

			if cmd.Flags().Changed("http-referer") || flagHTTPReferer != "" {
				headerOverrides["HTTP-Referer"] = formatCLIParamValue(flagHTTPReferer)
			}

			if cmd.Flags().Changed("x-open-router-title") || flagXOpenRouterTitle != "" {
				headerOverrides["X-OpenRouter-Title"] = formatCLIParamValue(flagXOpenRouterTitle)
			}

			if cmd.Flags().Changed("x-open-router-categories") || flagXOpenRouterCategories != "" {
				headerOverrides["X-OpenRouter-Categories"] = formatCLIParamValue(flagXOpenRouterCategories)
			}

			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "datasets", path, map[string]string{
				"app_slug":   formatCLIParamValue(flagAppSlug),
				"model":      formatCLIParamValue(flagModel),
				"turn_range": formatCLIParamValue(flagTurnRange),
				"limit":      formatCLIParamValue(flagLimit),
				"offset":     formatCLIParamValue(flagOffset),
			}, headerOverrides, flagAll, "offset", "offset", "limit", 100, "", "", "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			outputData := collectionItemsForOutput(data, path)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"app_name": true, "app_slug": true, "median_session_cost_usd": true, "model_permaslug": true, "turn_range": true})
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"app_name": true, "app_slug": true, "median_session_cost_usd": true, "model_permaslug": true, "turn_range": true})
		},
	}
	cmd.Flags().StringVar(&flagHTTPReferer, "http-referer", "", "The app identifier should be your app's URL and is used as the primary identifier for rankings.")
	cmd.Flags().StringVar(&flagXOpenRouterTitle, "x-open-router-title", "", "The app display name allows you to customize how your app appears in OpenRouter's dashboard.")
	cmd.Flags().StringVar(&flagXOpenRouterCategories, "x-open-router-categories", "", "Comma-separated list of app categories (e.g. 'cli-agent,cloud-agent'). Used for marketplace rankings.")
	cmd.Flags().StringVar(&flagAppSlug, "app-slug", "", "Filter to one published harness slug.")
	cmd.Flags().StringVar(&flagModel, "model", "", "Exact model permaslug filter. Works across all harness apps.")
	cmd.Flags().StringVar(&flagTurnRange, "turn-range", "", "Filter by the inclusive number of turns in a session. (one of: 1-turn, 2-9-turns, 10-49-turns, 50-plus-turns)")
	cmd.Flags().IntVar(&flagLimit, "limit", 100, "Maximum number of cells to return (1-500). Defaults to 100.")
	cmd.Flags().StringVar(&flagOffset, "offset", "0", "Number of sorted cells to skip (0-5000). Defaults to 0.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
