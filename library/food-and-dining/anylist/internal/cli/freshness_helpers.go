// Copyright 2026 Jeeves and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"context"

	"github.com/mvanhorn/printing-press-library/library/food-and-dining/anylist/internal/cliutil"
)

// attachFreshness sets flags.freshnessMeta from a DataProvenance and returns
// the resulting FreshnessMeta. Call this in command handlers immediately after
// data is obtained so the freshness source is recorded before output is written.
//
// Usage (live API call):
//
//	return data, attachFreshness(cliutil.DataProvenance{Source: "live"}, flags), nil
//
// Usage (local cache read):
//
//	return data, attachFreshness(cliutil.DataProvenance{Source: "cache", CachedAt: lastSync}, flags), nil
func attachFreshness(p cliutil.DataProvenance, flags *rootFlags) *cliutil.FreshnessMeta {
	meta := cliutil.NewFreshnessMeta(p)
	flags.freshnessMeta = meta
	return meta
}

// ensureFreshForCommand looks up a registered command path in
// readCommandResources and applies the same freshness hook used by root
// pre-run. commandPath must match cobra.CommandPath(), including the binary
// name. It returns skipped metadata for unregistered commands.
func ensureFreshForCommand(ctx context.Context, flags *rootFlags, commandPath string) cliutil.FreshnessMeta {
	resources, ok := readCommandResources[commandPath]
	if !ok {
		meta := cliutil.FreshnessMeta{
			Decision: "skipped",
			Reason:   "unregistered_command",
			Source:   flags.dataSource,
		}
		flags.freshnessMeta = meta
		return meta
	}
	return ensureFreshForResources(ctx, flags, resources...)
}
