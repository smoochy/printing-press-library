// Copyright 2026 Som Samantray and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"
)

func newAirportsCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:         "airports",
		Short:       "Airport status, delays, weather, and flight boards",
		Annotations: map[string]string{"mcp:read-only": "true", "pp:parent-group": "true", "pp:api-resource": "true", "pp:typed-exit-codes": "0,2"},
		RunE:        parentNoSubcommandRunE(flags),
	}

	cmd.AddCommand(newAirportsArrivalsCmd(flags))
	cmd.AddCommand(newAirportsDeparturesCmd(flags))
	cmd.AddCommand(newAirportsListCmd(flags))
	cmd.AddCommand(newAirportsShowCmd(flags))
	cmd.AddCommand(newAirportsTvCmd(flags))
	addNovelCommandIfAbsent(cmd, newNovelAirportsAirlineCmd(flags))
	addNovelCommandIfAbsent(cmd, newNovelAirportsCompareCmd(flags))
	addNovelCommandIfAbsent(cmd, newNovelAirportsDiffCmd(flags))
	addNovelCommandIfAbsent(cmd, newNovelAirportsFindFlightCmd(flags))
	addNovelCommandIfAbsent(cmd, newNovelAirportsNearbyCmd(flags))
	addNovelCommandIfAbsent(cmd, newNovelAirportsRouteCmd(flags))
	addNovelCommandIfAbsent(cmd, newNovelAirportsWorstCmd(flags))
	return cmd
}
