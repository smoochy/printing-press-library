// Copyright 2026 zjsng and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"fmt"
	"sort"
	"strings"

	"github.com/spf13/cobra"
)

// whichEntry is one row of the curated capability index. The index is
// seeded at generation time from the same NovelFeature list that drives
// the SKILL.md feature section, so the command a `which` query returns
// is guaranteed to exist and to match what the skill advertises.
type whichEntry struct {
	Command      string   `json:"command"`
	Description  string   `json:"description"`
	Group        string   `json:"group,omitempty"`
	WhyItMatters string   `json:"why_it_matters,omitempty"`
	Aliases      []string `json:"aliases,omitempty"`
}

// whichIndex is the curated list of capabilities this CLI advertises as
// its hero features. Endpoint-level commands are discoverable via
// `--help`; `which` exists to resolve a natural-language capability
// query to one of the commands the skill says matter most.
var whichIndex = []whichEntry{
	{Command: "plan clone", Description: "Create a new Wanderlog trip from a shared or public source plan, then fill it with the source plan template.", Group: "Plan cloning and fill", WhyItMatters: "Use this when the user wants a private editable copy of a shared plan. A `/plan/<16-char-key>/...` URL is often already editable — do not clone just to edit it."},
	{Command: "plan fill", Description: "Fill an existing Wanderlog trip from a shared or public source plan with dry-run and force safeguards.", Group: "Plan cloning and fill", WhyItMatters: "Use this when the user already created a target trip and wants to populate it from a shared template. Substitute YOUR_TRIP_KEY with the 16-character key of a trip you own, from `trips home`."},
	{Command: "plan preview", Description: "Inspect a shared plan and report dates, sections, blocks, resources, and clone warnings before any write.", Group: "Plan cloning and fill", WhyItMatters: "Use this before clone/fill to confirm what will be copied and whether credentials are needed. The report is ~1KB, so it is safe to run on any pasted link."},
	{Command: "trips home", Description: "List the authenticated account's home trips together with their 16-character plan `key`s. Also: list my trips, own itineraries.", Group: "Plan reading and inspection", WhyItMatters: "Run this before any outline or edit: the numeric `tripPlan.id` is not a key, and every editing command wants the 16-char key."},
	{Command: "plan outline", Description: "Show a slim itinerary outline: days, section headings, and stop names, optionally for a single day. Also: show days in my plan, slim day list.", Group: "Plan reading and inspection", WhyItMatters: "Use this instead of a fat `trips get` dump when an agent needs the shape of the itinerary cheaply; pass `--day` to narrow to one day."},
	{Command: "plan inspect", Description: "Inspect a slim itinerary outline and, with `--check`, report counts, unformatted notes, lodging coverage, closed places, and text-vs-schedule mismatches. Also: verify an itinerary after edits.", Group: "Plan reading and inspection", WhyItMatters: "Run `--check` after a batch of writes to confirm the plan is still consistent before handing it back to the user."},
	{Command: "plan votes", Description: "List place and hotel block upvote counts for a Wanderlog plan. Also: who upvoted places, upvotedBy counts.", Group: "Plan reading and inspection", WhyItMatters: "Use this to see what collaborators actually voted for; the comments list is not a vote tally."},
	{Command: "lodging search", Description: "Search Wanderlog's lodging aggregator across Airbnb, Expedia, Google, and Kayak for a geo and date range, and return compact candidates carrying price, rating, coordinates, and booking URL. Also: search hotels, find a place to stay, compare stays.", Group: "Plan reading and inspection", WhyItMatters: "Use this to find candidates before committing to one — it answers “where could we stay?”, which `plan reservation add` does not. Feed a chosen offer straight into `plan reservation add --kind lodging --lodging-offer-json '<offer>'`, which is how stays with no Google place id become native lodging blocks. `lodging` is hidden from `--help` but callable by name. The example narrows to one source and a single night so it returns quickly; --sources defaults to airbnb,expedia,google,kayak when you omit it."},
	{Command: "plan sections", Description: "List editable section indexes, day numbers, section ids, dates, and block counts for a plan.", Group: "Agentic plan editing", WhyItMatters: "Use this before editing so an agent targets the right day or section."},
	{Command: "plan note add", Description: "Add a note block to a selected day or section through ShareDB.", Group: "Agentic plan editing", WhyItMatters: "Use this to add reminders, reservations, constraints, or planning notes."},
	{Command: "plan place add", Description: "Add a real place block to a selected day or section from a Google/Wanderlog place id, or from a query with location bias. Also: geocode a place by query, verify an address.", Group: "Agentic plan editing", WhyItMatters: "Use this to build an itinerary stop by stop."},
	{Command: "plan place replace", Description: "Replace only the nested place on an existing itinerary block, keeping its times and notes.", Group: "Agentic plan editing", WhyItMatters: "Use this when a stop was geocoded to the wrong venue and you do not want to lose the schedule or note already attached to it."},
	{Command: "plan fill-day", Description: "Insert a batch of place stops into one Wanderlog day from a JSON array of stops with optional times and notes.", Group: "Agentic plan editing", WhyItMatters: "Use this to lay down a whole day in one apply instead of one `plan place add` per stop; `--closed-place-policy` blocks stops that are closed on that date."},
	{Command: "plan block move", Description: "Move a note or place block within or across days.", Group: "Agentic plan editing", WhyItMatters: "Use this to reorder an itinerary after adding candidate stops."},
	{Command: "plan block delete", Description: "Delete a note or place block from a selected day or section.", Group: "Agentic plan editing", WhyItMatters: "Use this to clean up test blocks or remove rejected candidates."},
	{Command: "plan block edit-text", Description: "Replace the rich-text note attached to an existing block, as plain text or compiled markdown with `--markdown`. Also: replace a stop note as plain text.", Group: "Agentic plan editing", WhyItMatters: "Use this to revise stop notes, reservation notes, or reminders. Without `--markdown` a bulleted string becomes one flat insert; with it, `**bold**`, `-`/`*` bullets, and `#` label lines survive."},
	{Command: "plan block edit-text --markdown", Description: "Compile a markdown note into Wanderlog rich text: **bold**, `-`/`*` bullets, and `#` label lines. Also: format notes with bullets, bold text, rich text on a stop.", Group: "Agentic plan editing", WhyItMatters: "Without `--markdown` a bulleted string lands as one flat insert; with it the bullets and bold runs survive."},
	{Command: "plan block rename", Description: "Rename the display name on an existing place or lodging block. Also: rename a place or hotel, change a display name.", Group: "Agentic plan editing", WhyItMatters: "Use this after a bad geocode instead of `plan block set-field place.name`, which does not update the rendered stop label."},
	{Command: "plan block set-field", Description: "Set or remove a non-protected field on a block, including newly observed fields from live plan data.", Group: "Agentic plan editing", WhyItMatters: "Use this for schedule or metadata fields once you have inspected the plan shape; use `--json-value` for numbers, booleans, arrays, or objects."},
	{Command: "plan block schedule", Description: "Set or clear first-class schedule fields on an existing block. Also: set stop times, duration.", Group: "Agentic plan editing", WhyItMatters: "Use this to turn loose stops into a timed itinerary."},
	{Command: "plan block attachment", Description: "List, add, or remove attachment metadata on a block. Also: tickets, booking links, PDFs.", Group: "Agentic plan editing", WhyItMatters: "Use this for tickets, booking links, PDFs, and other planning artifacts."},
	{Command: "plan reservation add", Description: "Add a flight, lodging, rental car, restaurant, transit, cruise, or standalone attachment block with dates, times, and confirmation details. Also: record a hotel stay, check-in and check-out dates.", Group: "Agentic plan editing", WhyItMatters: "Use `--kind lodging` to record a stay: `--span-nights` is on by default when the dates span more than one night, and `--display-name` sets the rendered place name after geocoding."},
	{Command: "plan checklist", Description: "Add checklist blocks and add, check, or remove checklist items. Also: packing lists, todo items.", Group: "Agentic plan editing", WhyItMatters: "Use this for packing lists, booking tasks, and shared planning todos."},
	{Command: "plan comments", Description: "List, add, edit, delete, or vote on Wanderlog plan comments using the confirmed comments API. Also: friend discussion, replies, comment votes.", Group: "Agentic plan editing", WhyItMatters: "Use this to read friend discussion, ask questions, and explain agent-made edits."},
	{Command: "plan collaborators", Description: "Inspect collaborator/share metadata, list pending invites, send email/user invites, add or remove collaborators by user id, and create share keys. Also: invite a friend, share links, share keys, pending invites, permissions.", Group: "Agentic plan editing", WhyItMatters: "Use this for account-level collaboration tasks around the shared plan. Keep invite sends on `--dry-run` until the recipient list is explicit. Related invocations: `plan collaborators invites --plan-url URL --agent` lists pending invites, and `plan collaborators invite --plan-url URL --email friend@example.com --dry-run --agent` sends one."},
	{Command: "plan budget", Description: "Summarize, export, and edit Wanderlog trip budget expenses and settlement payments. Also: add an expense, costs, splits, payers, record a payment or settlement, CSV export.", Group: "Agentic plan editing", WhyItMatters: "Use this to set the trip budget, add costs with categories/splits/payers, link expenses to itinerary blocks, and record settlement payments. Mutating budget commands use ShareDB and are covered by `plan undo`/`plan redo`."},
	{Command: "plan route", Description: "Build route optimization request bodies or call Wanderlog's optimizeRoute endpoint. Also: optimize a route, better travel order.", Group: "Agentic plan editing", WhyItMatters: "Use this to compute a better order/travel path, then apply block moves deliberately."},
	{Command: "plan history", Description: "List, preview, undo, and redo local-journaled ShareDB itinerary edits (`plan history`, `plan undo`, `plan redo`). Also: undo my last edit, redo my last edit, revert an itinerary change.", Group: "Agentic plan editing", WhyItMatters: "Use this as the safety net after applied itinerary edits. Undo/redo defaults to preview; pass `--apply` to mutate the plan. Related invocations: `plan undo --plan-url URL --apply --agent` and `plan redo --plan-url URL --apply --agent`."},
	{Command: "plan section add-day", Description: "Insert a new day section and update trip day/date bounds.", Group: "Agentic plan editing", WhyItMatters: "Use this when the itinerary needs another travel day."},
	{Command: "plan section set-field", Description: "Set or remove a field on a section, including day headings or rich text.", Group: "Agentic plan editing", WhyItMatters: "Use this to label days or adjust section-level notes."},
	{Command: "plan section delete", Description: "Delete an empty section and update trip day/date bounds.", Group: "Agentic plan editing", WhyItMatters: "Use this to remove unused days or empty sections after reviewing the target."},
	{Command: "plan section swap-days", Description: "Swap the block arrays of two day sections in one JSON0 batch.", Group: "Agentic plan editing", WhyItMatters: "Use this to reorder a trip by whole days without moving stops one at a time; it is a single apply."},
	{Command: "plan block apply", Description: "Preview or apply a JSON0 operation array read from disk with --ops-file. Also: apply an ops-file batch of plan edits.", Group: "Agentic plan editing", WhyItMatters: "Use this to replay a reviewed batch of operations in one apply; `--dry-run` alone short-circuits without reading the file. A real run is `plan block apply --plan-url URL --ops-file ./ops.json --apply --agent`; the global `--dry-run` returns before the file is read, so it is safe as a smoke probe."},
	{Command: "plan raw op", Description: "Preview or apply an explicit ShareDB JSON0 operation array.", Group: "Agentic plan editing", WhyItMatters: "Use this only as an escape hatch for fields not yet covered by a named command, after inspecting live plan shape and dry-run output."},
}

// whichMatch pairs an index entry with its ranking score for a query.
// Higher score means stronger match. The ranker is naive (exact token
// then substring then group tag) because 20-40 entries do not need
// semantic retrieval - a ranker upgrade is a future change that would
// not break this contract.
type whichMatch struct {
	Entry whichEntry `json:"entry"`
	Score int        `json:"score"`
}

// rankWhich returns up to `limit` best matches for `query` against the
// index, sorted by descending score. Score breakdown:
//
//	+3  exact token match on the command's leaf or full path
//	+2  substring match on the command (any part)
//	+2  substring match on the description
//	+4  description contains the full query string (in addition to +2)
//	+5  query or token hits an Aliases entry
//	+1  group tag contains the query as a word
//
// Ties break on declaration order in the index. An empty query returns
// every entry at score 0 in declaration order - this is the "list all"
// behavior the skill documents for broad agent discovery.
func rankWhich(index []whichEntry, query string, limit int) []whichMatch {
	if limit <= 0 {
		limit = 3
	}
	q := strings.ToLower(strings.TrimSpace(query))
	if q == "" {
		out := make([]whichMatch, 0, len(index))
		for _, e := range index {
			out = append(out, whichMatch{Entry: e, Score: 0})
		}
		return out
	}
	qTokens := strings.Fields(q)

	scored := make([]whichMatch, 0, len(index))
	for i, e := range index {
		score := whichScoreEntry(e, q, qTokens)
		scored = append(scored, whichMatch{Entry: e, Score: score})
		_ = i
	}

	sort.SliceStable(scored, func(i, j int) bool {
		return scored[i].Score > scored[j].Score
	})
	// Drop zero-score matches when the query was non-empty; agents
	// branching on exit code rely on "no match" meaning no confidence.
	filtered := scored[:0]
	for _, m := range scored {
		if m.Score > 0 {
			filtered = append(filtered, m)
		}
	}
	if len(filtered) > limit {
		filtered = filtered[:limit]
	}
	return filtered
}

func whichTokens(s string) []string {
	s = strings.NewReplacer("-", " ", "_", " ", "/", " ").Replace(s)
	return strings.Fields(s)
}

func whichScoreEntry(e whichEntry, query string, qTokens []string) int {
	score := 0
	cmd := strings.ToLower(e.Command)
	cmdTokens := whichTokens(cmd)
	desc := strings.ToLower(e.Description)
	group := strings.ToLower(e.Group)

	// Exact token match on the command path (any token).
	for _, qt := range qTokens {
		for _, ct := range cmdTokens {
			if qt == ct {
				score += 3
				break
			}
		}
	}
	// Substring match on the full command (covers hyphenated leaves).
	if strings.Contains(cmd, query) {
		score += 2
	}
	// Substring match on the description.
	if strings.Contains(desc, query) {
		score += 2
		// PATCH(amend-2026-08-23: full-query description bonus)
		score += 4
	}
	descTokens := whichTokens(desc)
	for _, qt := range qTokens {
		for _, dt := range descTokens {
			if qt == dt {
				score++
				break
			}
		}
	}
	// PATCH(amend-2026-08-23: alias ranking)
	if whichAliasHits(e.Aliases, query, qTokens) {
		score += 5
	}
	// Group tag match.
	if group != "" {
		for _, qt := range qTokens {
			if strings.Contains(group, qt) {
				score += 1
				break
			}
		}
	}
	return score
}

func whichAliasHits(aliases []string, query string, qTokens []string) bool {
	for _, alias := range aliases {
		a := strings.ToLower(strings.TrimSpace(alias))
		if a == "" {
			continue
		}
		if a == query || strings.Contains(query, a) || strings.Contains(a, query) {
			return true
		}
		aTokens := whichTokens(a)
		for _, qt := range qTokens {
			for _, at := range aTokens {
				if qt == at {
					return true
				}
			}
		}
	}
	return false
}

func newWhichCmd(flags *rootFlags) *cobra.Command {
	var limit int
	cmd := &cobra.Command{
		Use:   "which [query]",
		Short: "Find the command that implements a capability",
		Annotations: map[string]string{
			"mcp:read-only":       "true",
			"pp:typed-exit-codes": "0,2",
		},
		Long: `which resolves a natural-language capability query (for example, "search messages" or "stale tickets") to the best matching command from this CLI's curated feature index.

Exit codes:
  0  at least one match found
  2  no confident match - the query did not score against any indexed capability; fall back to '--help' or 'search' if this CLI has one`,
		Example: `  wanderlog-pp-cli which "stale tickets"
  wanderlog-pp-cli which "bottleneck"
  wanderlog-pp-cli which --limit 1 "send message"
  wanderlog-pp-cli which                                # list the full capability index`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(whichIndex) == 0 {
				return usageErr(fmt.Errorf("this CLI has no curated capability index; run '--help' to see every command"))
			}
			query := strings.Join(args, " ")
			matches := rankWhich(whichIndex, query, limit)

			// Empty query returns the whole index at score 0 (listing mode).
			if strings.TrimSpace(query) == "" {
				return renderWhich(cmd, flags, rankWhichAll(whichIndex))
			}

			if len(matches) == 0 {
				// Under --json, return an empty matches envelope at exit 0
				// so agents can branch on `matches.length == 0` instead of
				// parsing a usage error message. Non-JSON keeps the typed
				// exit-2 path so terminal users see the help hint.
				if flags.asJSON {
					return printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"matches": []whichMatch{},
					}, flags)
				}
				return usageErr(fmt.Errorf("no match for %q; try '%s --help' for the full command list", query, cmd.Root().Name()))
			}
			return renderWhich(cmd, flags, matches)
		},
	}
	cmd.Flags().IntVar(&limit, "limit", 3, "Maximum number of matches to return")
	return cmd
}

// rankWhichAll is a narrow helper used by the "empty query lists the
// index" path. It returns every entry in declaration order at score 0
// so the render path treats them uniformly.
func rankWhichAll(index []whichEntry) []whichMatch {
	out := make([]whichMatch, 0, len(index))
	for _, e := range index {
		out = append(out, whichMatch{Entry: e, Score: 0})
	}
	return out
}

func renderWhich(cmd *cobra.Command, flags *rootFlags, matches []whichMatch) error {
	w := cmd.OutOrStdout()
	// Output shape follows the same rule as every other generated
	// command: JSON when the caller asked for it OR when stdout is not
	// a terminal; table when a human is looking.
	asJSON := flags.asJSON
	if !asJSON && !isTerminal(w) {
		asJSON = true
	}
	if asJSON {
		// JSON envelope: {matches: [...]}. The wrap is critical:
		// printJSONFiltered's --compact path uses compactListFields
		// (allowlist) for top-level arrays, which would strip
		// entry/score keys; routing through compactObjectFields
		// (blocklist) via an object envelope preserves them.
		if matches == nil {
			matches = []whichMatch{}
		}
		return printJSONFiltered(w, map[string]any{"matches": matches}, flags)
	}
	fmt.Fprintf(w, "%-24s  %-8s  %s\n", "COMMAND", "SCORE", "DESCRIPTION")
	for _, m := range matches {
		fmt.Fprintf(w, "%-24s  %-8d  %s\n", m.Entry.Command, m.Score, m.Entry.Description)
	}
	return nil
}
