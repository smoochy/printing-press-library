{
  "manifest_version": "0.3",
  "name": "agoda-pp-mcp",
  "display_name": "Agoda",
  "version": "0.1.0",
  "description": "The only Agoda tool that shows what you will actually pay, then re-ranks the results by that number.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/agoda-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/agoda-pp-mcp",
      "args": [],
      "env": {
        "PRINTING_PRESS_CLIENT_PROFILE": "${user_config.printing_press_client_profile}"
      }
    }
  },
  "user_config": {
    "printing_press_client_profile": {
      "type": "string",
      "title": "Client profile",
      "description": "Binds the MCP server to an existing tenant-gated Printing Press client profile.",
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
