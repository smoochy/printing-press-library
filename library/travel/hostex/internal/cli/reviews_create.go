// Copyright 2026 bust011r and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newReviewsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyCategoryRatingsCleanliness float64
	var bodyCategoryRatingsCommunication float64
	var bodyCategoryRatingsOverallRating float64
	var bodyCategoryRatingsRecommend float64
	var bodyCategoryRatingsRespectOfHouseRules float64
	var bodyHostReplyContent string
	var bodyHostReviewContent string
	var bodyHostReviewScore float64
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "create <reservation_code>",
		Short: "Create review or reply for a reservation.",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  hostex-pp-cli reviews create example-value",
		Annotations: map[string]string{"pp:endpoint": "reviews.create", "pp:method": "POST", "pp:path": "/reviews/{reservation_code}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			path := "/reviews/{reservation_code}"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("reservation_code is required\nUsage: %s <%s>", cmd.CommandPath(), "reservation_code"))
			}
			path = replacePathParam(path, "reservation_code", args[0])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				{
					nestedCategoryRatings := map[string]any{}
					if bodyCategoryRatingsCleanliness != 0.0 {
						nestedCategoryRatings["cleanliness"] = bodyCategoryRatingsCleanliness
					}
					if bodyCategoryRatingsCommunication != 0.0 {
						nestedCategoryRatings["communication"] = bodyCategoryRatingsCommunication
					}
					if bodyCategoryRatingsOverallRating != 0.0 {
						nestedCategoryRatings["overall_rating"] = bodyCategoryRatingsOverallRating
					}
					if bodyCategoryRatingsRecommend != 0.0 {
						nestedCategoryRatings["recommend"] = bodyCategoryRatingsRecommend
					}
					if bodyCategoryRatingsRespectOfHouseRules != 0.0 {
						nestedCategoryRatings["respect_of_house_rules"] = bodyCategoryRatingsRespectOfHouseRules
					}
					if len(nestedCategoryRatings) > 0 {
						body["category_ratings"] = nestedCategoryRatings
					}
				}
				if bodyHostReplyContent != "" {
					body["host_reply_content"] = bodyHostReplyContent
				}
				if bodyHostReviewContent != "" {
					body["host_review_content"] = bodyHostReviewContent
				}
				if bodyHostReviewScore != 0.0 {
					body["host_review_score"] = bodyHostReviewScore
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "reviews", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "reviews", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "reviews", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "reviews", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "reviews", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "reviews",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "reviews", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "reviews", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().Float64Var(&bodyCategoryRatingsCleanliness, "category-ratings-cleanliness", 0.0, "Cleanliness rating. The value is between 0 and 5.")
	cmd.Flags().Float64Var(&bodyCategoryRatingsCommunication, "category-ratings-communication", 0.0, "Communication rating. The value is between 0 and 5.")
	cmd.Flags().Float64Var(&bodyCategoryRatingsOverallRating, "category-ratings-overall-rating", 0.0, "Overall rating of the guest. The value is between 0 and 5.")
	cmd.Flags().Float64Var(&bodyCategoryRatingsRecommend, "category-ratings-recommend", 0.0, "Whether the host recommends the guest. 1 for yes, 0 for no (Airbnb only).")
	cmd.Flags().Float64Var(&bodyCategoryRatingsRespectOfHouseRules, "category-ratings-respect-of-house-rules", 0.0, "Respect of house rules rating. The value is between 0 and 5.")
	cmd.Flags().StringVar(&bodyHostReplyContent, "host-reply-content", "", "The reply of the host.")
	cmd.Flags().StringVar(&bodyHostReviewContent, "host-review-content", "", "The comment of the host.")
	cmd.Flags().Float64Var(&bodyHostReviewScore, "host-review-score", 0.0, "The rating score of the host review. The value is between 0 and 5.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
