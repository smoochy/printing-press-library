# seats-aero reprint — final shipcheck after the peer-review fix wave (2026-09-06)

Run against library commit `5601f71` (Tier 2/3 fix wave T10–T17 + Tier-1 re-review round; T17 = completeness gate for partially re-synced legacy stores, Greptile r5 P1). A completeness cache (T18/T19) was tried to avoid the two covering-index counts per bounded local read and then REVERTED: count equality cannot prove typed/generic set identity (a failed typed projection plus a reconcile deletion of a different row restores the count), so every bounded local read re-derives completeness — two covering-index counts, measured ~10–15 ms at 300k rows — rather than trusting a cached claim, XDG-isolated, read-only API key.

| Gate | Result |
|---|---|
| go build / vet / test | PASS (every codex task gated; `go test -count=1 ./...` green at each commit) |
| verify (live, `--api-key`) | **PASS 43/43** (100%), critical 0. A first pass scored 43/44 because the verifier treats the legacy `SEATS_AERO_PARTNER_PARTNER_AUTHORIZATION` env var — which the CLI now honours as a fallback — as a required variable; with it set the run is clean, which also proves the legacy credential path live. |
| scorecard (`--spec`, `--live-check`) | **93/100, Grade A** (1 of 26 dimensions unverified: live_api_verification — fed by shipcheck's live gate, not the standalone scorecard). Dimensions below 10: terminal_ux 9, mcp_token_efficiency 7, mcp_tool_design 7, mcp_surface_strategy 0, agent_workflow_readiness 9, data_pipeline_integrity 7, type_fidelity 5, dead_code 4, live_api_verification 0. Unchanged from the pre-review 93; the fix wave did not trade score for correctness. |
| Phase 5 live dogfood (full, `--write-acceptance`) | **PASS 104/104**, 94 structural skips, no hollow features. Marker `phase5-acceptance.json` fingerprints this tree. The first re-run after the alias landed failed 1/102 on the help contract (the alias lacked an Examples section) and was fixed before the passing run. |
| Peer review | see `2026-09-06-100500-fix-seats-aero-pp-cli-peer-reviews.md` |

Regression caught by the AQL harness between 2.0.1 and 2.0.2 and fixed here: a store synced before 2.0.x has empty typed tables, and the bounded local list treated the empty typed table as authoritative → `routes --data-source local` returned nothing with exit 0. 2.0.2 falls back to the legacy `resources` rows with a stderr hint; typed read-back was then verified end-to-end by the independent AQL harness on a synced COPY of a pre-2.0 store: `routes --source united --data-source local` returned 8,800 rows, exactly matching `select count(*) from routes where source='united'`, with no legacy hint, while the same command on the un-synced store returns the legacy rows and prints the hint. (Also measured there: a `routes` sync is a single request returning the full route set.)
