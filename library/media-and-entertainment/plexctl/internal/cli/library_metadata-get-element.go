// Copyright 2026 keithah and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newLibraryMetadataGetElementCmd(flags *rootFlags) *cobra.Command {
	var flagElement string

	cmd := &cobra.Command{
		Use:   "metadata-get-element <ids> <timestamp>",
		Short: "Get the artwork, thumb, element for a metadata item",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  plexctl-pp-cli library metadata-get-element example-value 42 --element thumb",
		Annotations: map[string]string{"pp:endpoint": "library.metadata-get-element", "pp:method": "GET", "pp:path": "/library/metadata/{ids}/{element}/{timestamp}", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <ids> <timestamp>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <ids> <timestamp>"))
			}
			if cmd.Flags().Changed("element") {
				allowedElement := []string{"thumb", "art", "clearLogo", "squareArt", "banner", "poster", "theme"}
				validElement := false
				for _, v := range allowedElement {
					if flagElement == v {
						validElement = true
						break
					}
				}
				if !validElement {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagElement, "element", allowedElement)
				}
			}
			path := "/library/metadata/{ids}/{element}/{timestamp}"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("ids is required\nUsage: %s <%s>", cmd.CommandPath(), "ids"))
			}
			path = replacePathParam(path, "ids", args[0])
			if len(args) < 2 || args[1] == "" {
				return usageErr(fmt.Errorf("timestamp is required\nUsage: %s <%s>", cmd.CommandPath(), "timestamp"))
			}
			path = replacePathParam(path, "timestamp", args[1])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			path = replacePathParam(path, "element", formatCLIParamValue(flagElement))
			headerOverrides := map[string]string{
				"Accept":                           "audio/mpeg3",
				"X-Printing-Press-Binary-Response": "true",
			}

			params := map[string]string{}
			data, prov, err := resolveReadWithStrategyResponsePathAndJSONGuard(cmd.Context(), c, flags, "live", "library", false, path, params, headerOverrides, "", false, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			_ = json.Valid
			_ = os.Stderr
			_ = prov
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.csv || flags.compact || flags.plain || flags.selectFields != "" {
				return usageErr(fmt.Errorf("binary response cannot be rendered as structured output; redirect stdout or use --deliver file:<path>"))
			}
			_, err = cmd.OutOrStdout().Write(data)
			return err
		},
	}
	cmd.Flags().StringVar(&flagElement, "element", "thumb", "Element (one of: thumb, art, clearLogo, squareArt, banner, poster, theme)")

	return cmd
}
