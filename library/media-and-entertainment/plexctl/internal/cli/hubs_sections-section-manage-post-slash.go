// Copyright 2026 keithah and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newHubsSectionsSectionManagePostSlashCmd(flags *rootFlags) *cobra.Command {
	var flagMetadataItemId string
	var flagPromotedToRecommended int
	var flagPromotedToOwnHome int
	var flagPromotedToSharedHome int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "sections-section-manage-post-slash <sectionId>",
		Aliases:     []string{"create"},
		Short:       "Create a custom hub based on a metadata item",
		Example:     "  plexctl-pp-cli hubs sections-section-manage-post-slash 42 --metadata-item-id 42",
		Annotations: map[string]string{"pp:endpoint": "hubs.sections-section-manage-post-slash", "pp:method": "POST", "pp:path": "/hubs/sections/{sectionId}/manage"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <sectionId>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <sectionId>"))
			}
			if !cmd.Flags().Changed("metadata-item-id") && flagMetadataItemId == "" && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "metadata-item-id")
			}
			if !stdinBody {
			}
			path := "/hubs/sections/{sectionId}/manage"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("sectionId is required\nUsage: %s <%s>", cmd.CommandPath(), "sectionId"))
			}
			path = replacePathParam(path, "sectionId", args[0])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			headerOverrides := map[string]string{
				"Accept":                         "text/html",
				"X-Printing-Press-Text-Response": "true",
			}

			params := map[string]string{}
			if true {
				params["metadataItemId"] = formatCLIParamValue(flagMetadataItemId)
			}
			if cmd.Flags().Changed("promoted-to-recommended") || flagPromotedToRecommended != 0 {
				params["promotedToRecommended"] = formatCLIParamValue(flagPromotedToRecommended)
			}
			if cmd.Flags().Changed("promoted-to-own-home") || flagPromotedToOwnHome != 0 {
				params["promotedToOwnHome"] = formatCLIParamValue(flagPromotedToOwnHome)
			}
			if cmd.Flags().Changed("promoted-to-shared-home") || flagPromotedToSharedHome != 0 {
				params["promotedToSharedHome"] = formatCLIParamValue(flagPromotedToSharedHome)
			}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
			}
			data, statusCode, err := c.PostWithParamsAndHeaders(cmd.Context(), path, params, body, headerOverrides)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			_ = statusCode
			_ = os.Stderr
			var textEnvelope struct {
				Format string `json:"format"`
				Body   string `json:"body"`
			}
			if err := json.Unmarshal(data, &textEnvelope); err != nil {
				return fmt.Errorf("decoding text response envelope: %w", err)
			}
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.agent || flags.csv || flags.compact || flags.selectFields != "" {
				if flags.csv {
					data = append(append(json.RawMessage{'['}, data...), ']')
				}
				return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"}, nil)
			}
			_, err = cmd.OutOrStdout().Write([]byte(textEnvelope.Body))
			return err
		},
	}
	cmd.Flags().StringVar(&flagMetadataItemId, "metadata-item-id", "", "The metadata item on which to base this hub. This must currently be a collection")
	cmd.Flags().IntVar(&flagPromotedToRecommended, "promoted-to-recommended", 0, "Whether this hub should be displayed in recommended (one of: 0, 1)")
	cmd.Flags().IntVar(&flagPromotedToOwnHome, "promoted-to-own-home", 0, "Whether this hub should be displayed in admin's home (one of: 0, 1)")
	cmd.Flags().IntVar(&flagPromotedToSharedHome, "promoted-to-shared-home", 0, "Whether this hub should be displayed in shared user's home (one of: 0, 1)")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
