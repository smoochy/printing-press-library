// Copyright 2026 keithah and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newPhotoPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagUrl string
	var flagFormat string
	var flagWidth int
	var flagHeight int
	var flagQuality int
	var flagBackground string
	var flagUpscale int
	var flagMinSize int
	var flagRotate int
	var flagBlur int
	var flagSaturation int
	var flagOpacity int
	var flagChromaSubsampling int
	var flagBlendColor string

	cmd := &cobra.Command{
		Use:         "photo",
		Short:       "Transcode an image, possibly changing format or size",
		Long:        "Transcode an image, possibly changing format or size",
		Example:     "  plexctl-pp-cli photo",
		Annotations: map[string]string{"pp:endpoint": "photo.image-transcode", "pp:method": "GET", "pp:path": "/photo/:/transcode", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("format") {
				allowedFormat := []string{"jpg", "jpeg", "png", "ppm"}
				validFormat := false
				for _, v := range allowedFormat {
					if flagFormat == v {
						validFormat = true
						break
					}
				}
				if !validFormat {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagFormat, "format", allowedFormat)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/photo/:/transcode"
			headerOverrides := map[string]string{
				"Accept":                           "image/jpeg",
				"X-Printing-Press-Binary-Response": "true",
			}

			params := map[string]string{}
			if flagUrl != "" {
				params["url"] = formatCLIParamValue(flagUrl)
			}
			if flagFormat != "" {
				params["format"] = formatCLIParamValue(flagFormat)
			}
			if flagWidth != 0 {
				params["width"] = formatCLIParamValue(flagWidth)
			}
			if flagHeight != 0 {
				params["height"] = formatCLIParamValue(flagHeight)
			}
			if flagQuality != 0 {
				params["quality"] = formatCLIParamValue(flagQuality)
			}
			if flagBackground != "" {
				params["background"] = formatCLIParamValue(flagBackground)
			}
			if flagUpscale != 0 {
				params["upscale"] = formatCLIParamValue(flagUpscale)
			}
			if flagMinSize != 0 {
				params["minSize"] = formatCLIParamValue(flagMinSize)
			}
			if flagRotate != 0 {
				params["rotate"] = formatCLIParamValue(flagRotate)
			}
			if flagBlur != 0 {
				params["blur"] = formatCLIParamValue(flagBlur)
			}
			if flagSaturation != 0 {
				params["saturation"] = formatCLIParamValue(flagSaturation)
			}
			if flagOpacity != 0 {
				params["opacity"] = formatCLIParamValue(flagOpacity)
			}
			if flagChromaSubsampling != 0 {
				params["chromaSubsampling"] = formatCLIParamValue(flagChromaSubsampling)
			}
			if flagBlendColor != "" {
				params["blendColor"] = formatCLIParamValue(flagBlendColor)
			}
			data, prov, err := resolveReadWithStrategyResponsePathAndJSONGuard(cmd.Context(), c, flags, "auto", "photo", false, path, params, headerOverrides, "", false, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			_ = os.Stderr
			_ = prov
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.csv || flags.compact || flags.plain || flags.selectFields != "" {
				var envelope struct {
					PPBinary bool   `json:"_pp_binary"`
					Encoding string `json:"encoding"`
				}
				if err := json.Unmarshal(data, &envelope); err != nil || !envelope.PPBinary || envelope.Encoding != "base64" {
					return fmt.Errorf("binary response cannot be rendered as structured output; redirect stdout or use --deliver file:<path>")
				}
				if flags.csv {
					data = append(append(json.RawMessage{'['}, data...), ']')
				}
				return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"}, nil)
			}
			_, err = cmd.OutOrStdout().Write(data)
			return err
		},
	}
	cmd.Flags().StringVar(&flagUrl, "url", "", "The source URL for the image to transcode.")
	cmd.Flags().StringVar(&flagFormat, "format", "", "The output format for the image; defaults to jpg (one of: jpg, jpeg, png, ppm)")
	cmd.Flags().IntVar(&flagWidth, "width", 0, "The desired width of the output image")
	cmd.Flags().IntVar(&flagHeight, "height", 0, "The desired height of the output image")
	cmd.Flags().IntVar(&flagQuality, "quality", 0, "The desired quality of the output. -1 means the highest quality. Defaults to -1")
	cmd.Flags().StringVar(&flagBackground, "background", "", "The background color to apply before painting the image. Only really applicable if image has transparency.")
	cmd.Flags().IntVar(&flagUpscale, "upscale", 0, "Indicates if image should be upscaled to the desired width/height. Defaults to false (one of: 0, 1)")
	cmd.Flags().IntVar(&flagMinSize, "min-size", 0, "Indicates if image should be scaled to fit the smaller dimension. (one of: 0, 1)")
	cmd.Flags().IntVar(&flagRotate, "rotate", 0, "Obey the rotation values specified in EXIF data. Defaults to true. (one of: 0, 1)")
	cmd.Flags().IntVar(&flagBlur, "blur", 0, "Apply a blur to the image, Defaults to 0 (none)")
	cmd.Flags().IntVar(&flagSaturation, "saturation", 0, "Scale the image saturation by the specified percentage. Defaults to 100")
	cmd.Flags().IntVar(&flagOpacity, "opacity", 0, "Render the image at the specified opacity percentage. Defaults to 100")
	cmd.Flags().IntVar(&flagChromaSubsampling, "chroma-subsampling", 0, "Use the specified chroma subsambling. - 0: 411 - 1: 420 - 2: 422 - 3: 444 Defaults to 3 (444) (one of: 0, 1, 2, 3)")
	cmd.Flags().StringVar(&flagBlendColor, "blend-color", "", "The color to blend with the image. Defaults to none")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
