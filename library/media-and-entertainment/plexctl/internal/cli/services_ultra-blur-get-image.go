// Copyright 2026 keithah and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newServicesUltraBlurGetImageCmd(flags *rootFlags) *cobra.Command {
	var flagTopLeft string
	var flagTopRight string
	var flagBottomRight string
	var flagBottomLeft string
	var flagWidth int
	var flagHeight int
	var flagNoise int

	cmd := &cobra.Command{
		Use:         "ultra-blur-get-image",
		Short:       "Retrieves a server-side generated UltraBlur image based on the provided color inputs.",
		Example:     "  plexctl-pp-cli services ultra-blur-get-image",
		Annotations: map[string]string{"pp:endpoint": "services.ultra-blur-get-image", "pp:method": "GET", "pp:path": "/services/ultrablur/image", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			path := "/services/ultrablur/image"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			headerOverrides := map[string]string{
				"Accept":                           "image/png",
				"X-Printing-Press-Binary-Response": "true",
			}

			params := map[string]string{}
			if flagTopLeft != "" {
				params["topLeft"] = formatCLIParamValue(flagTopLeft)
			}
			if flagTopRight != "" {
				params["topRight"] = formatCLIParamValue(flagTopRight)
			}
			if flagBottomRight != "" {
				params["bottomRight"] = formatCLIParamValue(flagBottomRight)
			}
			if flagBottomLeft != "" {
				params["bottomLeft"] = formatCLIParamValue(flagBottomLeft)
			}
			if flagWidth != 0 {
				params["width"] = formatCLIParamValue(flagWidth)
			}
			if flagHeight != 0 {
				params["height"] = formatCLIParamValue(flagHeight)
			}
			if flagNoise != 0 {
				params["noise"] = formatCLIParamValue(flagNoise)
			}
			data, prov, err := resolveReadWithStrategyResponsePathAndJSONGuard(cmd.Context(), c, flags, "auto", "services", false, path, params, headerOverrides, "", false, cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			_ = json.Valid
			_ = os.Stderr
			_ = prov
			if flags.quiet {
				return nil
			}
			if flags.asJSON || flags.csv || flags.compact || flags.plain || flags.selectFields != "" {
				return usageErr(fmt.Errorf("binary response cannot be rendered as structured output; redirect stdout or use --deliver file:<path>"))
			}
			_, err = cmd.OutOrStdout().Write(data)
			return err
		},
	}
	cmd.Flags().StringVar(&flagTopLeft, "top-left", "", "The base color (hex) for the top left quadrant.")
	cmd.Flags().StringVar(&flagTopRight, "top-right", "", "The base color (hex) for the top right quadrant.")
	cmd.Flags().StringVar(&flagBottomRight, "bottom-right", "", "The base color (hex) for the bottom right quadrant.")
	cmd.Flags().StringVar(&flagBottomLeft, "bottom-left", "", "The base color (hex) for the bottom left quadrant.")
	cmd.Flags().IntVar(&flagWidth, "width", 0, "Width in pixels for the image.")
	cmd.Flags().IntVar(&flagHeight, "height", 0, "Height in pixels for the image.")
	cmd.Flags().IntVar(&flagNoise, "noise", 0, "Whether to add noise to the ouput image. Noise can reduce color banding with the gradients. (one of: 0, 1)")

	return cmd
}
