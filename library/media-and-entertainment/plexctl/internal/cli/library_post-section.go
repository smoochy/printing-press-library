// Copyright 2026 keithah and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newLibraryPostSectionCmd(flags *rootFlags) *cobra.Command {
	var flagName string
	var flagType int
	var flagScanner string
	var flagAgent string
	var flagMetadataAgentProviderGroupId string
	var flagLanguage string
	var flagLocations string
	var flagPrefs string
	var flagRelative int
	var flagImportFromiTunes int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "post-section",
		Short: "Add a new library section to the server",
		// TODO: replace placeholder example values before relying on this for live dogfood.
		Example:     "  plexctl-pp-cli library post-section --name example-resource --type 42 --agent example-value --language example-value",
		Annotations: map[string]string{"pp:endpoint": "library.post-section", "pp:method": "POST", "pp:path": "/library/sections/all"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !cmd.Flags().Changed("name") && flagName == "" && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "name")
			}
			if !cmd.Flags().Changed("type") && flagType == 0 && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "type")
			}
			if !cmd.Flags().Changed("agent") && flagAgent == "" && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "agent")
			}
			if !cmd.Flags().Changed("language") && flagLanguage == "" && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "language")
			}
			if !stdinBody {
			}
			path := "/library/sections/all"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			if flagLocations != "" {
				path = appendArrayQueryParam(path, "locations", flagLocations, "form", true)
			}
			if flagPrefs != "" {
				path, err = appendDeepObjectQueryParam(path, "prefs", flagPrefs)
				if err != nil {
					return err
				}
			}
			params := map[string]string{}
			if cmd.Flags().Changed("name") || flagName != "" {
				params["name"] = formatCLIParamValue(flagName)
			}
			if true {
				params["type"] = formatCLIParamValue(flagType)
			}
			if cmd.Flags().Changed("scanner") || flagScanner != "" {
				params["scanner"] = formatCLIParamValue(flagScanner)
			}
			if cmd.Flags().Changed("agent") || flagAgent != "" {
				params["agent"] = formatCLIParamValue(flagAgent)
			}
			if cmd.Flags().Changed("metadata-agent-provider-group-id") || flagMetadataAgentProviderGroupId != "" {
				params["metadataAgentProviderGroupId"] = formatCLIParamValue(flagMetadataAgentProviderGroupId)
			}
			if cmd.Flags().Changed("language") || flagLanguage != "" {
				params["language"] = formatCLIParamValue(flagLanguage)
			}
			if cmd.Flags().Changed("relative") || flagRelative != 0 {
				params["relative"] = formatCLIParamValue(flagRelative)
			}
			if cmd.Flags().Changed("import-fromi-tunes") || flagImportFromiTunes != 0 {
				params["importFromiTunes"] = formatCLIParamValue(flagImportFromiTunes)
			}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "library", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "library", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "library", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "library", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "library", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "library",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Mutation-riding reads (POST search, RPC-over-POST lists) return
				// the same single-key collection envelopes as GET reads. Unwrap
				// before filtering so rows nest once under the result key and
				// --select filters rows, not envelope keys; plain created-object
				// responses pass through unwrapSingleKeyArray untouched.
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := unwrapSingleKeyArray(data)
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"MediaContainer": true})
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "library", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "library", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagName, "name", "", "The name of the new section")
	cmd.Flags().IntVar(&flagType, "type", 0, "The type of library section")
	cmd.Flags().StringVar(&flagScanner, "scanner", "", "The scanner this section should use")
	cmd.Flags().StringVar(&flagAgent, "agent", "", "The agent this section should use for metadata")
	cmd.Flags().StringVar(&flagMetadataAgentProviderGroupId, "metadata-agent-provider-group-id", "", "The agent group id for this section")
	cmd.Flags().StringVar(&flagLanguage, "language", "", "The language of this section")
	cmd.Flags().StringVar(&flagLocations, "locations", "", "The locations on disk to add to this section")
	cmd.Flags().StringVar(&flagPrefs, "prefs", "", "The preferences for this section")
	cmd.Flags().IntVar(&flagRelative, "relative", 0, "If set, paths are relative to `Media Upload` path (one of: 0, 1)")
	cmd.Flags().IntVar(&flagImportFromiTunes, "import-fromi-tunes", 0, "If set, import media from iTunes. (one of: 0, 1)")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
