// Copyright 2026 Som Samantray and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newProductsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagQ string
	var flagLat float64
	var flagLon float64
	var flagPlatform string
	var flagXGeolocationPincode string

	cmd := &cobra.Command{
		Use:         "products",
		Short:       "Search products on one platform by keyword and location.",
		Long:        "Search products on one platform by keyword and location.",
		Example:     "  quickcommerce-pp-cli products --query milk --latitude 12.9021 --longitude 77.6639 --platform BlinkIt",
		Annotations: map[string]string{"pp:endpoint": "products.search", "pp:method": "GET", "pp:path": "/v1/search", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("platform") {
				allowedPlatform := []string{"BlinkIt", "Zepto", "Swiggy", "BigBasket", "DMart", "JioMart", "Minutes", "Amazon", "Nykaa", "Myntra", "Flipkart"}
				validPlatform := false
				for _, v := range allowedPlatform {
					if flagPlatform == v {
						validPlatform = true
						break
					}
				}
				if !validPlatform {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagPlatform, "platform", allowedPlatform)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/search"
			headerOverrides := map[string]string{}

			if (cmd.Flags().Changed("pincode") || cmd.Flags().Changed("geolocation-pincode")) || flagXGeolocationPincode != "" {
				headerOverrides["x-geolocation-pincode"] = formatCLIParamValue(flagXGeolocationPincode)
			}

			params := map[string]string{}
			if flagQ != "" {
				params["q"] = formatCLIParamValue(flagQ)
			}
			if flagLat != 0.0 {
				params["lat"] = formatCLIParamValue(flagLat)
			}
			if flagLon != 0.0 {
				params["lon"] = formatCLIParamValue(flagLon)
			}
			if flagPlatform != "" {
				params["platform"] = formatCLIParamValue(flagPlatform)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "products", true, path, params, headerOverrides, "data.products", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			outputData := data
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(outputData, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{outputData}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"id": true, "name": true, "brand": true, "available": true, "images": true, "mrp": true, "offer_price": true, "quantity": true, "deeplink": true, "rating": true, "rating_count": true, "inventory": true, "platform": true})
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"id": true, "name": true, "brand": true, "available": true, "images": true, "mrp": true, "offer_price": true, "quantity": true, "deeplink": true, "rating": true, "rating_count": true, "inventory": true, "platform": true})
		},
	}
	cmd.Flags().StringVar(&flagQ, "query", "milk", "Product keyword such as milk or atta 5kg.")
	cmd.Flags().StringVar(&flagQ, "q", "milk", "Product keyword such as milk or atta 5kg.")
	_ = cmd.Flags().MarkHidden("q")
	cmd.Flags().Float64Var(&flagLat, "latitude", 12.902100, "Delivery latitude in decimal degrees.")
	cmd.Flags().Float64Var(&flagLat, "lat", 12.902100, "Delivery latitude in decimal degrees.")
	_ = cmd.Flags().MarkHidden("lat")
	cmd.Flags().Float64Var(&flagLon, "longitude", 77.663900, "Delivery longitude in decimal degrees.")
	cmd.Flags().Float64Var(&flagLon, "lon", 77.663900, "Delivery longitude in decimal degrees.")
	_ = cmd.Flags().MarkHidden("lon")
	cmd.Flags().StringVar(&flagPlatform, "platform", "BlinkIt", "Platform to search, for example BlinkIt or Zepto. (one of: BlinkIt, Zepto, Swiggy, BigBasket, DMart, JioMart, Minutes, Amazon, Nykaa, Myntra, Flipkart)")
	cmd.Flags().StringVar(&flagXGeolocationPincode, "pincode", "", "Six-digit pincode required by DMart, JioMart, and Minutes.")
	cmd.Flags().StringVar(&flagXGeolocationPincode, "geolocation-pincode", "", "Six-digit pincode required by DMart, JioMart, and Minutes.")
	_ = cmd.Flags().MarkHidden("geolocation-pincode")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
