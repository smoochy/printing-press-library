// Copyright 2026 Som Samantray and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newDeliveryCompareCmd(flags *rootFlags) *cobra.Command {
	var flagPlatforms string
	var flagLat float64
	var flagLon float64
	var flagXGeolocationPincode string

	cmd := &cobra.Command{
		Use:         "compare",
		Short:       "Compare delivery ETAs across quick-commerce platforms.",
		Example:     "  quickcommerce-pp-cli delivery compare --platforms BlinkIt,Zepto,Swiggy --latitude 12.9021 --longitude 77.6639",
		Annotations: map[string]string{"pp:endpoint": "delivery.compare", "pp:method": "GET", "pp:path": "/v1/groupeta", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !cmd.Flags().Changed("platforms") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "platforms")
			}
			if !(cmd.Flags().Changed("latitude") || cmd.Flags().Changed("lat")) && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "latitude")
			}
			if !(cmd.Flags().Changed("longitude") || cmd.Flags().Changed("lon")) && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "longitude")
			}
			path := "/v1/groupeta"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			headerOverrides := map[string]string{}

			if (cmd.Flags().Changed("pincode") || cmd.Flags().Changed("geolocation-pincode")) || flagXGeolocationPincode != "" {
				headerOverrides["x-geolocation-pincode"] = formatCLIParamValue(flagXGeolocationPincode)
			}

			params := map[string]string{}
			if flagPlatforms != "" {
				params["platforms"] = formatCLIParamValue(flagPlatforms)
			}
			if flagLat != 0.0 {
				params["lat"] = formatCLIParamValue(flagLat)
			}
			if flagLon != 0.0 {
				params["lon"] = formatCLIParamValue(flagLon)
			}
			data, prov, err := resolveReadWithStrategyAndResponsePath(cmd.Context(), c, flags, "auto", "delivery", false, path, params, headerOverrides, "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			outputData := data
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"platforms": true, "lat": true, "lon": true, "credit_cost": true, "results": true})
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"platforms": true, "lat": true, "lon": true, "credit_cost": true, "results": true})
		},
	}
	cmd.Flags().StringVar(&flagPlatforms, "platforms", "", "Comma-separated quick-commerce platform names.")
	cmd.Flags().Float64Var(&flagLat, "latitude", 0.0, "Delivery latitude in decimal degrees.")
	cmd.Flags().Float64Var(&flagLat, "lat", 0.0, "Delivery latitude in decimal degrees.")
	_ = cmd.Flags().MarkHidden("lat")
	cmd.Flags().Float64Var(&flagLon, "longitude", 0.0, "Delivery longitude in decimal degrees.")
	cmd.Flags().Float64Var(&flagLon, "lon", 0.0, "Delivery longitude in decimal degrees.")
	_ = cmd.Flags().MarkHidden("lon")
	cmd.Flags().StringVar(&flagXGeolocationPincode, "pincode", "", "Six-digit pincode required by DMart, JioMart, and Minutes.")
	cmd.Flags().StringVar(&flagXGeolocationPincode, "geolocation-pincode", "", "Six-digit pincode required by DMart, JioMart, and Minutes.")
	_ = cmd.Flags().MarkHidden("geolocation-pincode")

	return cmd
}
