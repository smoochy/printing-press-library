# SKILL semantic review
PASS after fixes. Sequential main-thread review per user Task mapping; not an independent subagent review.

Seven trigger/capability checks: current SendFox operating triggers match typed API and local workflow behavior; seven unique capabilities exactly match seven novel_features_built; each help page resolves; snapshot and native-export limits are disclosed; bearer auth uses canonical SENDFOX_API_TOKEN and stdin-only set-token; nine recipes match output intent; unsupported exclusivity marketing removed after final dogfood sync.

The prior 11-operation campaign-create gap was removed. Webhook absence remains explicit. Normalized Kit input is required; no raw-export parser, actual pilot delivery, automatic cutover or automatic rollback is promised. No remaining semantic findings.
