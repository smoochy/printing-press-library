# README / SKILL / AGENTS audit
PASS after fixes, against 139-command recursive help, generated sources, approved manifest and research.

Removed placeholder executable examples and token-echo guidance. Canonical bearer auth is distinguished from legacy alias; set-token uses stdin. Corrected HTTP MCP documentation: caller-token authentication exists and TLS is required off loopback. Published installer links are explicitly identified as prior release links; local build instructions select this upgrade. Replaced unsupported exclusivity claim with concrete local-evidence behavior. README includes Known Gaps and SKILL includes anti-triggers. All seven built features remain aligned with research; generation-owned description text was synchronized by dogfood.

Known limits are explicit at use: local readiness does not authorize sending, actual domain/DNS/delivery behavior is unverified, native Kit exports need normalization, unsupported event/progress capabilities are never inferred. Final mechanical verify-skill and narrative checks recorded separately.
