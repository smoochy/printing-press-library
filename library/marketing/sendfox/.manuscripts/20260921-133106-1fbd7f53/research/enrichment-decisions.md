# Authorized generation enrichments
Keep original live YAML immutable. Derived spec selects documented Personal Access Token bearer auth while retaining original OAuth2 declaration; interactive OAuth app setup is not exercised in this no-auth run. Established auth env name SENDFOX_API_TOKEN, historical SENDFOX_BEARER_AUTH compatibility. MCP code search/execute, hidden endpoints, stdio + loopback HTTP. Explicit intents added before generation. 0.9 requests/second default leaves shared-account headroom; multi-process coordination still requires operator budgeting. No automatic cache refresh on a 60/minute API: manual sync and freshness evidence. Learn seeds use documented finite engagement/type vocabulary. Operation grouping changes only local resource names. Add page inputs only where vendor response schema proves pagination; this inferred request convention must be mock tested and remains live-unverified. All 60 HTTP method/path pairs retained.
