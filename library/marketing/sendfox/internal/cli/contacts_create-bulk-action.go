// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newContactsCreateBulkActionCmd(flags *rootFlags) *cobra.Command {
	var bodyAction string
	var bodyDryRun bool
	var bodyFilterClickedCampaignId int
	var bodyFilterCreatedAfter string
	var bodyFilterCreatedBefore string
	var bodyFilterInListIds string
	var bodyFilterLastClickedAfter string
	var bodyFilterLastClickedBefore string
	var bodyFilterLastOpenedAfter string
	var bodyFilterLastOpenedBefore string
	var bodyFilterLastSentAfter string
	var bodyFilterLastSentBefore string
	var bodyFilterNeverClicked bool
	var bodyFilterNeverOpened bool
	var bodyFilterNeverSent bool
	var bodyFilterNotInListIds string
	var bodyFilterNotOpenedCampaignId int
	var bodyFilterNotTagIds string
	var bodyFilterOpenedCampaignId int
	var bodyFilterStatus string
	var bodyFilterTagIds string
	var bodyTargetId int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-bulk-action",
		Example:     "  sendfox-pp-cli contacts create-bulk-action --action apply_tag --target-id 1 --dry-run",
		Short:       "Queues one action against every contact the filter matches, applied in chunks in the background.",
		Annotations: map[string]string{"pp:happy-args": "--action=apply_tag;--target-id=1", "pp:endpoint": "contacts.create-bulk-action", "pp:method": "POST", "pp:path": "/contacts/bulk-actions", "pp:requires-input": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("action") && bodyAction == "" && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "action")
				}
				if !cmd.Flags().Changed("target-id") && bodyTargetId == 0 && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "target-id")
				}
			}
			path := "/contacts/bulk-actions"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("action") || bodyAction != "" {
					bodyMap["action"] = bodyAction
				}
				if cmd.Flags().Changed("preview-count") {
					bodyMap["dry_run"] = bodyDryRun
				}
				{
					nestedFilter := map[string]any{}
					if cmd.Flags().Changed("filter-clicked-campaign-id") || bodyFilterClickedCampaignId != 0 {
						nestedFilter["clicked_campaign_id"] = bodyFilterClickedCampaignId
					}
					if cmd.Flags().Changed("filter-created-after") || bodyFilterCreatedAfter != "" {
						nestedFilter["created_after"] = bodyFilterCreatedAfter
					}
					if cmd.Flags().Changed("filter-created-before") || bodyFilterCreatedBefore != "" {
						nestedFilter["created_before"] = bodyFilterCreatedBefore
					}
					if cmd.Flags().Changed("filter-in-list-ids") || bodyFilterInListIds != "" {
						var parsedFilterInListIds any
						if err := json.Unmarshal([]byte(bodyFilterInListIds), &parsedFilterInListIds); err != nil {
							return fmt.Errorf("parsing --filter-in-list-ids JSON: %w", err)
						}
						asArray, ok := parsedFilterInListIds.([]any)
						if !ok {
							return fmt.Errorf("--filter-in-list-ids must be a JSON array, got JSON %T", parsedFilterInListIds)
						}
						nestedFilter["in_list_ids"] = asArray
					}
					if cmd.Flags().Changed("filter-last-clicked-after") || bodyFilterLastClickedAfter != "" {
						nestedFilter["last_clicked_after"] = bodyFilterLastClickedAfter
					}
					if cmd.Flags().Changed("filter-last-clicked-before") || bodyFilterLastClickedBefore != "" {
						nestedFilter["last_clicked_before"] = bodyFilterLastClickedBefore
					}
					if cmd.Flags().Changed("filter-last-opened-after") || bodyFilterLastOpenedAfter != "" {
						nestedFilter["last_opened_after"] = bodyFilterLastOpenedAfter
					}
					if cmd.Flags().Changed("filter-last-opened-before") || bodyFilterLastOpenedBefore != "" {
						nestedFilter["last_opened_before"] = bodyFilterLastOpenedBefore
					}
					if cmd.Flags().Changed("filter-last-sent-after") || bodyFilterLastSentAfter != "" {
						nestedFilter["last_sent_after"] = bodyFilterLastSentAfter
					}
					if cmd.Flags().Changed("filter-last-sent-before") || bodyFilterLastSentBefore != "" {
						nestedFilter["last_sent_before"] = bodyFilterLastSentBefore
					}
					if cmd.Flags().Changed("filter-never-clicked") {
						nestedFilter["never_clicked"] = bodyFilterNeverClicked
					}
					if cmd.Flags().Changed("filter-never-opened") {
						nestedFilter["never_opened"] = bodyFilterNeverOpened
					}
					if cmd.Flags().Changed("filter-never-sent") {
						nestedFilter["never_sent"] = bodyFilterNeverSent
					}
					if cmd.Flags().Changed("filter-not-in-list-ids") || bodyFilterNotInListIds != "" {
						var parsedFilterNotInListIds any
						if err := json.Unmarshal([]byte(bodyFilterNotInListIds), &parsedFilterNotInListIds); err != nil {
							return fmt.Errorf("parsing --filter-not-in-list-ids JSON: %w", err)
						}
						asArray, ok := parsedFilterNotInListIds.([]any)
						if !ok {
							return fmt.Errorf("--filter-not-in-list-ids must be a JSON array, got JSON %T", parsedFilterNotInListIds)
						}
						nestedFilter["not_in_list_ids"] = asArray
					}
					if cmd.Flags().Changed("filter-not-opened-campaign-id") || bodyFilterNotOpenedCampaignId != 0 {
						nestedFilter["not_opened_campaign_id"] = bodyFilterNotOpenedCampaignId
					}
					if cmd.Flags().Changed("filter-not-tag-ids") || bodyFilterNotTagIds != "" {
						var parsedFilterNotTagIds any
						if err := json.Unmarshal([]byte(bodyFilterNotTagIds), &parsedFilterNotTagIds); err != nil {
							return fmt.Errorf("parsing --filter-not-tag-ids JSON: %w", err)
						}
						asArray, ok := parsedFilterNotTagIds.([]any)
						if !ok {
							return fmt.Errorf("--filter-not-tag-ids must be a JSON array, got JSON %T", parsedFilterNotTagIds)
						}
						nestedFilter["not_tag_ids"] = asArray
					}
					if cmd.Flags().Changed("filter-opened-campaign-id") || bodyFilterOpenedCampaignId != 0 {
						nestedFilter["opened_campaign_id"] = bodyFilterOpenedCampaignId
					}
					if cmd.Flags().Changed("filter-status") || bodyFilterStatus != "" {
						nestedFilter["status"] = bodyFilterStatus
					}
					if cmd.Flags().Changed("filter-tag-ids") || bodyFilterTagIds != "" {
						var parsedFilterTagIds any
						if err := json.Unmarshal([]byte(bodyFilterTagIds), &parsedFilterTagIds); err != nil {
							return fmt.Errorf("parsing --filter-tag-ids JSON: %w", err)
						}
						asArray, ok := parsedFilterTagIds.([]any)
						if !ok {
							return fmt.Errorf("--filter-tag-ids must be a JSON array, got JSON %T", parsedFilterTagIds)
						}
						nestedFilter["tag_ids"] = asArray
					}
					if len(nestedFilter) > 0 {
						bodyMap["filter"] = nestedFilter
					}
				}
				if cmd.Flags().Changed("target-id") || bodyTargetId != 0 {
					bodyMap["target_id"] = bodyTargetId
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "contacts", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "contacts", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "contacts", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "contacts", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "contacts", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "contacts",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Mutation-riding reads (POST search, RPC-over-POST lists) return
				// the same single-key collection envelopes as GET reads. Unwrap
				// before filtering so rows nest once under the result key and
				// --select filters rows, not envelope keys; plain created-object
				// responses pass through unwrapSingleKeyArray untouched.
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				var selectErr error
				filtered := unwrapSingleKeyArray(data)
				if flags.selectFields != "" {
					filtered, selectErr = filterFieldsChecked(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"completed_at": true, "id": true, "matched_count": true, "processed_count": true, "started_at": true, "status": true, "target_id": true})
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "contacts", partialFailure.Message))
				}
				return selectErr
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlagsMeta renders the body with live
			// provenance, then the typed partial-failure exit fires unless
			// --allow-partial-failure downgrades it. Without this guard a
			// partial failure would exit 0 for these output modes — the exact
			// silent-swallow regression the surrounding patch is preventing
			// for asJSON / piped output.
			printErr := printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"}, map[string]bool{"completed_at": true, "id": true, "matched_count": true, "processed_count": true, "started_at": true, "status": true, "target_id": true})
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "contacts", partialFailure.Message))
			}
			return printErr
		},
	}
	cmd.Flags().StringVar(&bodyAction, "action", "", "Action")
	cmd.Flags().BoolVar(&bodyDryRun, "preview-count", true, "When true, only count the matches — nothing is modified")
	cmd.Flags().IntVar(&bodyFilterClickedCampaignId, "filter-clicked-campaign-id", 0, "Bulk actions only — not available on GET /contacts")
	cmd.Flags().StringVar(&bodyFilterCreatedAfter, "filter-created-after", "", "Created after")
	cmd.Flags().StringVar(&bodyFilterCreatedBefore, "filter-created-before", "", "Created before")
	cmd.Flags().StringVar(&bodyFilterInListIds, "filter-in-list-ids", "", "In list ids")
	cmd.Flags().StringVar(&bodyFilterLastClickedAfter, "filter-last-clicked-after", "", "Last clicked after")
	cmd.Flags().StringVar(&bodyFilterLastClickedBefore, "filter-last-clicked-before", "", "Last clicked before")
	cmd.Flags().StringVar(&bodyFilterLastOpenedAfter, "filter-last-opened-after", "", "Last opened after")
	cmd.Flags().StringVar(&bodyFilterLastOpenedBefore, "filter-last-opened-before", "", "Excludes contacts who never opened — use never_opened for those")
	cmd.Flags().StringVar(&bodyFilterLastSentAfter, "filter-last-sent-after", "", "Last sent after")
	cmd.Flags().StringVar(&bodyFilterLastSentBefore, "filter-last-sent-before", "", "Last sent before")
	cmd.Flags().BoolVar(&bodyFilterNeverClicked, "filter-never-clicked", false, "Never clicked")
	cmd.Flags().BoolVar(&bodyFilterNeverOpened, "filter-never-opened", false, "Never opened")
	cmd.Flags().BoolVar(&bodyFilterNeverSent, "filter-never-sent", false, "Never sent")
	cmd.Flags().StringVar(&bodyFilterNotInListIds, "filter-not-in-list-ids", "", "Not in list ids")
	cmd.Flags().IntVar(&bodyFilterNotOpenedCampaignId, "filter-not-opened-campaign-id", 0, "Contacts the campaign sent to who did not open it. Bulk actions only — not available on GET /contacts")
	cmd.Flags().StringVar(&bodyFilterNotTagIds, "filter-not-tag-ids", "", "Not tag ids")
	cmd.Flags().IntVar(&bodyFilterOpenedCampaignId, "filter-opened-campaign-id", 0, "Bulk actions only — not available on GET /contacts")
	cmd.Flags().StringVar(&bodyFilterStatus, "filter-status", "", "Status")
	cmd.Flags().StringVar(&bodyFilterTagIds, "filter-tag-ids", "", "Tag ids")
	cmd.Flags().IntVar(&bodyTargetId, "target-id", 0, "The tag id (apply_tag/remove_tag) or list id (add_to_list/remove_from_list)")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
