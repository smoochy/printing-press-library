// Copyright 2026 Cathryn Lavery and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newCampaignsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyExcludedContactTags string
	var bodyExcludedLists string
	var bodyFromEmail string
	var bodyFromName string
	var bodyHtml string
	var bodyLists string
	var bodyPreviewText string
	var bodyScheduledAt string
	var bodySubject string
	var bodyTitle string
	var bodyToContactTags string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Example:     "  sendfox-pp-cli campaigns create --title 'Weekly newsletter' --subject 'A useful idea' --html '<p>Hello reader</p>' --from-name 'Example Newsletter' --from-email reader@example.com --dry-run",
		Short:       "Creates a campaign as a draft. To send it, use the send endpoint or provide scheduled_at.",
		Annotations: map[string]string{"pp:happy-args": "--title=Weekly newsletter;--subject=A useful idea;--html=<p>Hello reader</p>;--from-name=Example Newsletter;--from-email=reader@example.com", "pp:endpoint": "campaigns.create", "pp:method": "POST", "pp:path": "/campaigns", "pp:requires-input": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("from-email") && bodyFromEmail == "" && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "from-email")
				}
				if !cmd.Flags().Changed("from-name") && bodyFromName == "" && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "from-name")
				}
				if !cmd.Flags().Changed("html") && bodyHtml == "" && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "html")
				}
				if !cmd.Flags().Changed("subject") && bodySubject == "" && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "subject")
				}
				if !cmd.Flags().Changed("title") && bodyTitle == "" && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "title")
				}
			}
			path := "/campaigns"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = bodyMap
				if cmd.Flags().Changed("excluded-contact-tags") || bodyExcludedContactTags != "" {
					var parsedExcludedContactTags any
					if err := json.Unmarshal([]byte(bodyExcludedContactTags), &parsedExcludedContactTags); err != nil {
						return fmt.Errorf("parsing --excluded-contact-tags JSON: %w", err)
					}
					asArray, ok := parsedExcludedContactTags.([]any)
					if !ok {
						return fmt.Errorf("--excluded-contact-tags must be a JSON array, got JSON %T", parsedExcludedContactTags)
					}
					bodyMap["excluded_contact_tags"] = asArray
				}
				if cmd.Flags().Changed("excluded-lists") || bodyExcludedLists != "" {
					var parsedExcludedLists any
					if err := json.Unmarshal([]byte(bodyExcludedLists), &parsedExcludedLists); err != nil {
						return fmt.Errorf("parsing --excluded-lists JSON: %w", err)
					}
					asArray, ok := parsedExcludedLists.([]any)
					if !ok {
						return fmt.Errorf("--excluded-lists must be a JSON array, got JSON %T", parsedExcludedLists)
					}
					bodyMap["excluded_lists"] = asArray
				}
				if cmd.Flags().Changed("from-email") || bodyFromEmail != "" {
					bodyMap["from_email"] = bodyFromEmail
				}
				if cmd.Flags().Changed("from-name") || bodyFromName != "" {
					bodyMap["from_name"] = bodyFromName
				}
				if cmd.Flags().Changed("html") || bodyHtml != "" {
					bodyMap["html"] = bodyHtml
				}
				if cmd.Flags().Changed("lists") || bodyLists != "" {
					var parsedLists any
					if err := json.Unmarshal([]byte(bodyLists), &parsedLists); err != nil {
						return fmt.Errorf("parsing --lists JSON: %w", err)
					}
					asArray, ok := parsedLists.([]any)
					if !ok {
						return fmt.Errorf("--lists must be a JSON array, got JSON %T", parsedLists)
					}
					bodyMap["lists"] = asArray
				}
				if cmd.Flags().Changed("preview-text") || bodyPreviewText != "" {
					bodyMap["preview_text"] = bodyPreviewText
				}
				if cmd.Flags().Changed("scheduled-at") || bodyScheduledAt != "" {
					bodyMap["scheduled_at"] = bodyScheduledAt
				}
				if cmd.Flags().Changed("subject") || bodySubject != "" {
					bodyMap["subject"] = bodySubject
				}
				if cmd.Flags().Changed("title") || bodyTitle != "" {
					bodyMap["title"] = bodyTitle
				}
				if cmd.Flags().Changed("to-contact-tags") || bodyToContactTags != "" {
					var parsedToContactTags any
					if err := json.Unmarshal([]byte(bodyToContactTags), &parsedToContactTags); err != nil {
						return fmt.Errorf("parsing --to-contact-tags JSON: %w", err)
					}
					asArray, ok := parsedToContactTags.([]any)
					if !ok {
						return fmt.Errorf("--to-contact-tags must be a JSON array, got JSON %T", parsedToContactTags)
					}
					bodyMap["to_contact_tags"] = asArray
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "campaigns", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "campaigns", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "campaigns", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "campaigns", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "campaigns", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "campaigns",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Mutation-riding reads (POST search, RPC-over-POST lists) return
				// the same single-key collection envelopes as GET reads. Unwrap
				// before filtering so rows nest once under the result key and
				// --select filters rows, not envelope keys; plain created-object
				// responses pass through unwrapSingleKeyArray untouched.
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				var selectErr error
				filtered := unwrapSingleKeyArray(data)
				if flags.selectFields != "" {
					filtered, selectErr = filterFieldsChecked(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"created_at": true, "from_name": true, "id": true, "scheduled_at": true, "sent_at": true, "title": true, "updated_at": true})
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "campaigns", partialFailure.Message))
				}
				return selectErr
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlagsMeta renders the body with live
			// provenance, then the typed partial-failure exit fires unless
			// --allow-partial-failure downgrades it. Without this guard a
			// partial failure would exit 0 for these output modes — the exact
			// silent-swallow regression the surrounding patch is preventing
			// for asJSON / piped output.
			printErr := printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"}, map[string]bool{"created_at": true, "from_name": true, "id": true, "scheduled_at": true, "sent_at": true, "title": true, "updated_at": true})
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "campaigns", partialFailure.Message))
			}
			return printErr
		},
	}
	cmd.Flags().StringVar(&bodyExcludedContactTags, "excluded-contact-tags", "", "Tag IDs whose contacts should be excluded from the send. Exclusion wins over inclusion.")
	cmd.Flags().StringVar(&bodyExcludedLists, "excluded-lists", "", "List IDs whose contacts should be excluded from the send. Exclusion wins over inclusion.")
	cmd.Flags().StringVar(&bodyFromEmail, "from-email", "", "From email")
	cmd.Flags().StringVar(&bodyFromName, "from-name", "", "From name")
	cmd.Flags().StringVar(&bodyHtml, "html", "", "Email body HTML content")
	cmd.Flags().StringVar(&bodyLists, "lists", "", "Array of list IDs to send to")
	cmd.Flags().StringVar(&bodyPreviewText, "preview-text", "", "Inbox preview snippet shown beneath the subject line. Optional.")
	cmd.Flags().StringVar(&bodyScheduledAt, "scheduled-at", "", "Schedule send time (omit for draft). Must include at least one list.")
	cmd.Flags().StringVar(&bodySubject, "subject", "", "Subject")
	cmd.Flags().StringVar(&bodyTitle, "title", "", "Title")
	cmd.Flags().StringVar(&bodyToContactTags, "to-contact-tags", "", "Tag IDs whose contacts should receive the campaign. Can be combined with lists.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
