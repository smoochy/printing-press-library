// Copyright 2026 Kevin Magnan and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newPollsResponsesPollsListCmd(flags *rootFlags) *cobra.Command {
	var flagLimit int
	var flagCursor string
	var flagPage string
	var flagOrderBy string
	var flagDirection string
	var flagExpand string
	var flagPostId string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "polls-list <publicationId> <pollId>",
		Aliases:     []string{"get"},
		Short:       "Retrieve all individual subscriber responses for a specific poll with cursor-based pagination.",
		Example:     "  beehiiv-pp-cli polls responses polls-list pub_00000000-0000-0000-0000-000000000000 poll_00000000-0000-0000-0000-000000000000",
		Annotations: map[string]string{"pp:endpoint": "responses.polls-list", "pp:method": "GET", "pp:path": "/publications/{publicationId}/polls/{pollId}/responses", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <publicationId> <pollId>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <publicationId> <pollId>"))
			}
			if cmd.Flags().Changed("order-by") {
				allowedOrderBy := []string{"created"}
				validOrderBy := false
				for _, v := range allowedOrderBy {
					if flagOrderBy == v {
						validOrderBy = true
						break
					}
				}
				if !validOrderBy {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrderBy, "order-by", allowedOrderBy)
				}
			}
			if cmd.Flags().Changed("direction") {
				allowedDirection := []string{"asc", "desc"}
				validDirection := false
				for _, v := range allowedDirection {
					if flagDirection == v {
						validDirection = true
						break
					}
				}
				if !validDirection {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagDirection, "direction", allowedDirection)
				}
			}
			path := "/publications/{publicationId}/polls/{pollId}/responses"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("publicationId is required\nUsage: %s <%s>", cmd.CommandPath(), "publicationId"))
			}
			path = replacePathParam(path, "publicationId", args[0])
			if len(args) < 2 || args[1] == "" {
				return usageErr(fmt.Errorf("pollId is required\nUsage: %s <%s>", cmd.CommandPath(), "pollId"))
			}
			path = replacePathParam(path, "pollId", args[1])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			if flagExpand != "" {
				path = appendArrayQueryParam(path, "expand[]", flagExpand, "form", true)
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "live", "responses", path, map[string]string{
				"limit":     formatCLIParamValue(flagLimit),
				"cursor":    formatCLIParamValue(flagCursor),
				"page":      formatCLIParamValue(flagPage),
				"order_by":  formatCLIParamValue(flagOrderBy),
				"direction": formatCLIParamValue(flagDirection),
				"post_id":   formatCLIParamValue(flagPostId),
			}, nil, flagAll, "cursor", "cursor", "limit", 0, "next_cursor", "has_more", "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			outputData := collectionItemsForOutput(data, path)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"created_at": true, "extended_feedback": true, "id": true, "poll_choice_id": true, "poll_choice_label": true, "post_id": true, "post_publish_date": true, "post_title": true, "subscription_id": true})
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"created_at": true, "extended_feedback": true, "id": true, "poll_choice_id": true, "poll_choice_label": true, "post_id": true, "post_publish_date": true, "post_title": true, "subscription_id": true})
		},
	}
	cmd.Flags().IntVar(&flagLimit, "limit", 0, "A limit on the number of objects to be returned. The limit can range between 1 and 100, and the default is 10.")
	cmd.Flags().StringVar(&flagCursor, "cursor", "", "**Cursor-based pagination (recommended)**: Use this opaque cursor token to fetch the next page of results.")
	cmd.Flags().StringVar(&flagPage, "page", "", "**Offset-based pagination (deprecated)**: Page number for offset-based pagination.")
	cmd.Flags().StringVar(&flagOrderBy, "order-by", "", "The field that the results are sorted by. Defaults to created. (one of: created)")
	cmd.Flags().StringVar(&flagDirection, "direction", "", "The direction that the results are sorted in. Defaults to asc. `asc` - Ascending, sorts from smallest to largest. (one of: asc, desc)")
	cmd.Flags().StringVar(&flagExpand, "expand", "", "Optionally expand the response to include additional data.")
	cmd.Flags().StringVar(&flagPostId, "post-id", "", "Filter to only return responses collected via the specified post. Accepts a prefixed post ID (e.g. `post_abc123`).")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
