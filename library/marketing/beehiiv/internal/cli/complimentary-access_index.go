// Copyright 2026 Kevin Magnan and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newComplimentaryAccessIndexCmd(flags *rootFlags) *cobra.Command {
	var flagExpand string
	var flagLimit int
	var flagCursor string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "index <publicationId>",
		Aliases:     []string{"get"},
		Short:       "Retrieve complimentary access objects for the publication.",
		Example:     "  beehiiv-pp-cli complimentary-access index pub_00000000-0000-0000-0000-000000000000",
		Annotations: map[string]string{"pp:endpoint": "complimentary-access.index", "pp:method": "GET", "pp:path": "/publications/{publicationId}/complimentary_access", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				// A missing required positional is a usage error in every output
				// mode (matches command_promoted.go.tmpl). Machine callers
				// (--json/--agent) also get a JSON error envelope on stdout;
				// usageErr sets exit 2.
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "missing required argument",
						"usage": fmt.Sprintf("%s%s", cmd.CommandPath(), " <publicationId>"),
					}, flags); printErr != nil {
						return printErr
					}
				}
				return usageErr(fmt.Errorf("missing required argument\nUsage: %s%s", cmd.CommandPath(), " <publicationId>"))
			}
			path := "/publications/{publicationId}/complimentary_access"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("publicationId is required\nUsage: %s <%s>", cmd.CommandPath(), "publicationId"))
			}
			path = replacePathParam(path, "publicationId", args[0])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			if flagExpand != "" {
				path = appendArrayQueryParam(path, "expand[]", flagExpand, "form", true)
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "live", "complimentary-access", path, map[string]string{
				"limit":  formatCLIParamValue(flagLimit),
				"cursor": formatCLIParamValue(flagCursor),
			}, nil, flagAll, "cursor", "cursor", "limit", 0, "next_cursor", "has_more", "data", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			outputData := collectionItemsForOutput(data, path)
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(outputData, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"description": true, "duration": true, "id": true, "stats": true, "tier": true, "tier_id": true, "time_unit": true})
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				wrapped, wrapErr = wrapPlatformStructuredOutput(wrapped, flags, "results", true)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(outputData, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			formatData := data
			if flags.csv || flags.plain {
				formatData = outputData
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), formatData, flags, map[string]any{"source": "live"}, map[string]bool{"description": true, "duration": true, "id": true, "stats": true, "tier": true, "tier_id": true, "time_unit": true})
		},
	}
	cmd.Flags().StringVar(&flagExpand, "expand", "", "Optional expandable objects (tier")
	cmd.Flags().IntVar(&flagLimit, "limit", 0, "Limit on objects returned (1-100")
	cmd.Flags().StringVar(&flagCursor, "cursor", "", "Opaque cursor token from next_cursor in a previous response.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
