## Absorb Manifest

### Absorbed (match or beat everything that exists)
| # | Feature | Best Source | Our Implementation | Added Value |
|---|---------|-----------|-------------------|-------------|
| 1 | List all TLDs with registrations, top-10M presence, views, min price | oneword.domains /tlds page | (generated endpoint) tlds list | `--json`, `--csv`, `--select`, offline after `sync`, `--sort-alpha` |
| 2 | TLD detail with per-registrar prices, cheapest registrar, example sites | oneword.domains /tlds/{tld} | (generated endpoint) tlds get | Six registrars in one call, cheapest highlighted, agent-native JSON |
| 3 | Dictionary autocomplete and category directory (17 categories, 27,627 words) | oneword.domains word search + /api/words | (generated endpoint) words list | `--prefix`, `--contains`, `--category`, paging, offline FTS after `sync` |
| 4 | Word detail with category tags | oneword.domains /api/words/{word} | (generated endpoint) words get | Typed output, `--select` |
| 5 | Single-domain availability with premium flag, price, aftermarket flag, popularity | oneword.domains domain rows | (generated endpoint) domains check | Typed exit codes, `--json`, clear 500 mapping for non-dictionary words |
| 6 | Filtered search of the available-domain database (text, category, price, length, sort, page) | oneword.domains /tlds/{tld} table (lifetime pass) | (generated endpoint) domains search | Session via `auth login --chrome`, all six filters as flags, CSV export the site lacks |
| 7 | Count of matching available domains | oneword.domains /api/domains/count | (generated endpoint) domains count | Public even without a pass; scriptable |
| 8 | Saved domains list | oneword.domains dashboard | (generated endpoint) domains saves | Reachable from scripts and MCP |
| 9 | Aftermarket listings with TLD, registrar, length filters and sort by domain, price, bids, ending | oneword.domains /aftermarket | (generated endpoint) listings list | Every server sort exposed as an enum flag, CSV export, offline mirror |
| 10 | Aftermarket counts and TLD facets | oneword.domains /api/listings/count and /filters | (generated endpoint) listings count, listings filters | Facet counts for agents choosing a TLD |
| 11 | DomainsGPT generation (6 name styles, include-word with position, length bounds, context, exclusions) | DomainsGPT page and API docs | oneword-domains-pp-cli gpt generate | Streamed concatenated JSON parsed into real JSON/NDJSON, `--available-only` in the hand-built wrapper, optional Bearer token |
| 12 | DomainsGPT usage and quota | oneword.domains /api/gpt/usage | (generated endpoint) gpt usage | Numeric-or-string quota normalised |
| 13 | Saved DomainsGPT names | oneword.domains dashboard | (generated endpoint) gpt saves | Scriptable |
| 14 | Bulk availability check of many names | Instant Domain Search bulk (5,000), Namecheap Beast Mode | oneword-domains-pp-cli check --file words.txt | Word list in, availability + price out, CSV/JSON, no browser |
| 15 | Cross-TLD comparison for one word | Instant Domain Search, oneword.domains "N of 93 TLDs" popularity | oneword-domains-pp-cli check smart | One word across every TLD with cheapest registrar, sorted by price |
| 16 | CSV export | Instant Domain Search export | (behavior in oneword-domains-pp-cli tlds list) global `--csv` on every list command | Every command, not only bulk results |
| 17 | MCP server for agents | Instant Domain Search MCP | (behavior in oneword-domains-pp-cli agent-context) every command is mirrored as an MCP tool by the bundled oneword-domains-pp-mcp server | Whole surface, not just availability |
| 18 | Local SQLite mirror with offline search and SQL | domain-goat (printing-press library) | (behavior in oneword-domains-pp-cli sync) generated sync/search/sql over tlds, words, listings | FTS across words and listings, `sql` for ad-hoc joins |
| 19 | Price history and drift for TLD registration prices | domain-goat price history, Porkbun pricing pages | oneword-domains-pp-cli tlds drift | Snapshots per sync, drift report per registrar |
| 20 | Watchlist / alerts | domain-goat watchlists, Instant Domain Search alerts | oneword-domains-pp-cli listings watch | New, changed, ending-soon listings since last run |
| 21 | Domain hacks (word whose ending is a TLD, e.g. instagr.am) | Namecheap Beast Mode "domain hacks" | oneword-domains-pp-cli hacks smart | Splits any word against the 93 real TLDs and checks each candidate with the availability route |
| 22 | Side-by-side comparison of candidate domains | domain-goat compare, Instant Domain Search results grid | oneword-domains-pp-cli compare smart.com smart.io smart.ai | One table: available, premium, price, cheapest registrar, popularity, aftermarket flag |
| 23 | Budget / true-cost filter | domain-goat budget | (behavior in oneword-domains-pp-cli check) `--max-price` drops TLDs whose cheapest registrar exceeds the budget | Price-aware shortlist without opening six registrar tabs |
| 24 | Brandability / popularity signal per name | domain-goat score | (behavior in oneword-domains-pp-cli check) popularity percent derived from tldCount (registered in N of 93 TLDs) | The site shows it as dots; the CLI prints the number |
| 25 | Ending-soon auction window | domain-goat drops timeline / drop-bid-window | (behavior in oneword-domains-pp-cli listings rank) `--ending 48h` window, also `listings watch --ending-within 24h` | Auction deadlines in one flag, UTC-safe |
| 26 | Prefix/suffix and misspelling name variants | Namecheap Beast Mode transforms, domain-goat gen | (behavior in oneword-domains-pp-cli gpt generate) `--type alternate|portmanteau|combination --word X --position prefix|suffix` | Uses DomainsGPT instead of local string mangling, with live availability |
| 27 | Bulk availability via MCP for agents | imprvhub/mcp-domain-availability, FastDomainCheck MCP, domainr-mcp-server | (behavior in oneword-domains-pp-cli agent-context) `check` and `domains check` are read-only MCP tools in the bundled server | Dictionary popularity and six-registrar pricing no other MCP returns |

### Transcendence (only possible with our approach)
| # | Feature | Command | Score | Buildability | How It Works | Evidence | Long Description |
|---|---------|---------|-------|--------------|--------------|----------|------------------|
| 1 | Brainstorm pipeline: generate, keep available, price, flag real words, persist | brainstorm --type portmanteau --word open --position prefix --tld com,ai --context "..." | 9/10 | hand-code | Checks quota via GET /api/gpt/usage, calls POST /gpt/generate once per TLD, keeps available names, joins the local word table (real-word flag) and tld table (min price, cheapest registrar), appends rows to a local generation table | Brief Top Workflow 4 and Build Priority 4; HN ask "is it a real word, how much, where cheapest"; anonymous quota 10 | Use this command to generate DomainsGPT names and keep only the available ones with registration price, cheapest registrar, and a real-word flag, saved locally. Do NOT use this command for one raw DomainsGPT call with the unfiltered stream; use 'gpt generate' instead. Do NOT use it to check words you already have; use 'check' instead. |
| 2 | Shortlist recheck with change diff | recheck --since 7d | 8/10 | hand-code | Reads distinct word/TLD pairs from the local domain_check snapshots, re-calls GET /api/domains/{word}.{tld}, appends a snapshot, diffs available/premium/price/tldCount against the prior one; --data-source local diffs the last two snapshots offline | Brief pain point "stale availability, re-check on demand"; Top Workflow 1 | Use this command to re-run every word/TLD pair you previously checked and report what changed (availability, premium, price, popularity). Do NOT use this command for a first check of new words; use 'check' instead. Do NOT use it for aftermarket auction changes; use 'listings watch' instead. |
| 3 | Auction ranking by word demand, price ratio, and bid pace | listings rank --tld co --ending 48h --sort popularity | 8/10 | hand-code | Joins local listing rows (price, bidCount, endDate, first_seen) with the local tld row (minPrice) and the word's tldCount from cached or live GET /api/domains/{word}.{tld}; computes price_x, taken_of_93, bids_per_day; --max-checks caps live lookups | Top Workflow 3; bundle constant 93 TLDs for popularity; the site sorts listings by eight server keys only | Use this command to rank live auctions by word demand (TLDs taken of 93), price versus fresh registration cost, and bid pace. Do NOT use this command to see which listings are new, changed, or ending since the last run; use 'listings watch' instead. Do NOT use it for the raw server-sorted list; use 'listings list' instead. |
| 4 | Cross-TLD intersection over the lifetime-pass database | domains intersect --tld com,ai --category positive --min-length 4 --max-length 6 | 8/10 | hand-code | Pages GET /api/domains?tld=X&... with the session cookie for each TLD, intersects the word sets, optionally subtracts words still free on --taken-on TLDs, emits per-TLD price/premium; 401 maps to "lifetime pass required" | Beast Mode "TLD comparison in one view"; /api/domains is per-TLD and pass-gated; Top Workflows 2 and 5 | Use this command to find dictionary words available on every listed TLD at once, or free on one TLD but taken on another (lifetime pass). Do NOT use this command to filter the dictionary by category or length without availability; use 'words mine' instead. Do NOT use it to verify a word list you already have; use 'check --file' instead. Do NOT use it to count inventory per TLD; use 'tlds inventory' instead. |
| 5 | Multi-category dictionary mining | words mine --category positive,tech --min-len 4 --max-len 6 | 7/10 | hand-code | One SQLite query over the local word and word_category tables with AND (default) or --any OR semantics, length bounds, prefix, and GLOB filters; offline after sync --resources words | Pain point "too few filters"; /api/words takes one category and no length params | Use this command to filter the local dictionary by several categories at once, length, prefix, or glob pattern. Do NOT use this command to find which words are available on a TLD; use 'domains intersect' (lifetime pass) or pipe its output into 'check --file' instead. |
| 6 | Available inventory per TLD for a filter set | tlds inventory --category positive --min-len 4 --max-len 6 --sort count | 7/10 | hand-code | Calls GET /api/domains/count?tld=X&... for every TLD in the local tld table (live GET /api/tlds when unsynced) and joins min price, cheapest registrar, top10m, totalReg into one sorted table | Top Workflow 2; the count route is public; the site shows one TLD count per page | Use this command to compare how many available one-word domains each TLD still has for a category/length/price filter, with the TLD's cheapest registration price. Do NOT use this command to list the words themselves; use 'domains intersect' instead. Do NOT use it for registrations, top-10M presence, views, or registrar prices alone; use 'tlds list' or 'tlds get' instead. |

### Stubs
None. Every row above ships fully.

### Hand-code commitment
- Transcendence rows tagged hand-code: 6 (brainstorm, recheck, listings rank, domains intersect, words mine, tlds inventory).
- Absorbed rows that are hand-built commands: 5 (check with --file, tlds drift, listings watch, hacks, compare) plus behaviour flags on generated commands (--max-price on check, --ending-within on listings list, popularity percent on check).
- Generated endpoint rows: 13 (spec-emitted).

### Killed candidates
See the brainstorm audit trail (`*-novel-features-brainstorm.md`): gpt history, listings heat, listings history, words hunt, tlds registrars, tlds market, tlds alternatives, words popular, variants, tlds card, words nearest, plan, words recent, gpt styles.

### Sources consulted (ecosystem search, Phase 1.5a)
No tool targets oneword.domains or DomainsGPT (GitHub, npm, PyPI, MCP marketplaces, official Claude plugins). Adjacent tools absorbed from: domain-goat (printing-press library), Instant Domain Search bulk/MCP, Namecheap Beast Mode, domainr-cli / domainr-mcp-server, imprvhub/mcp-domain-availability, bingal/FastDomainCheck-MCP-Server, davila7 domain-name-brainstormer skill.
