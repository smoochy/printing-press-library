version: 2
project_name: dndbeyond-pp-cli
changelog:
  disable: true
builds:
  - id: dndbeyond-pp-cli
    main: ./cmd/dndbeyond-pp-cli
    binary: dndbeyond-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/developer-tools/dndbeyond/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: dndbeyond-pp-mcp
    main: ./cmd/dndbeyond-pp-mcp
    binary: dndbeyond-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: dndbeyond-pp-cli
    repository:
      owner: Matthew Martin
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Read public D&D Beyond rules pages from the terminal."
    install: |
      bin.install "dndbeyond-pp-cli"
      bin.install "dndbeyond-pp-mcp"
