version: 2
project_name: is-agentic-pp-cli
changelog:
  disable: true
builds:
  - id: is-agentic-pp-cli
    main: ./cmd/is-agentic-pp-cli
    binary: is-agentic-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/developer-tools/is-agentic/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: is-agentic-pp-mcp
    main: ./cmd/is-agentic-pp-mcp
    binary: is-agentic-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: is-agentic-pp-cli
    repository:
      owner: somsamantray
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Turn Is Agentic reports into durable, scriptable readiness evidence."
    install: |
      bin.install "is-agentic-pp-cli"
      bin.install "is-agentic-pp-mcp"
