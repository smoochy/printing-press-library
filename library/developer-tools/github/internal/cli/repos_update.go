// Copyright 2026 Brandon Nye and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newReposUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAllowAutoMerge bool
	var bodyAllowForking bool
	var bodyAllowMergeCommit bool
	var bodyAllowRebaseMerge bool
	var bodyAllowSquashMerge bool
	var bodyAllowUpdateBranch bool
	var bodyArchived bool
	var bodyDefaultBranch string
	var bodyDeleteBranchOnMerge bool
	var bodyDescription string
	var bodyHasIssues bool
	var bodyHasProjects bool
	var bodyHasPullRequests bool
	var bodyHasWiki bool
	var bodyHomepage string
	var bodyIsTemplate bool
	var bodyMergeCommitMessage string
	var bodyMergeCommitTitle string
	var bodyName string
	var bodyPrivate bool
	var bodyPullRequestCreationPolicy string
	var bodySecurityAndAnalysisAdvancedSecurityStatus string
	var bodySecurityAndAnalysisCodeSecurityStatus string
	var bodySecurityAndAnalysisSecretScanningStatus string
	var bodySecurityAndAnalysisSecretScanningAiDetectionStatus string
	var bodySecurityAndAnalysisSecretScanningDelegatedAlertDismissalStatus string
	var bodySecurityAndAnalysisSecretScanningDelegatedBypassStatus string
	var bodySecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewers string
	var bodySecurityAndAnalysisSecretScanningNonProviderPatternsStatus string
	var bodySecurityAndAnalysisSecretScanningPushProtectionStatus string
	var bodySquashMergeCommitMessage string
	var bodySquashMergeCommitTitle string
	var bodyUseSquashPrTitleAsDefault bool
	var bodyVisibility string
	var bodyWebCommitSignoffRequired bool
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update <owner> <repo>",
		Short:       "**Note**: To edit a repository's topics, use the [Replace all repository topics](https://docs.github.",
		Example:     "  github-pp-cli repos update example-value example-value",
		Annotations: map[string]string{"pp:endpoint": "repos.update", "pp:method": "PATCH", "pp:path": "/repos/{owner}/{repo}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/repos/{owner}/{repo}"
			path = replacePathParam(path, "owner", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("repo is required\nUsage: %s <%s>", cmd.CommandPath(), "repo"))
			}
			path = replacePathParam(path, "repo", args[1])
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("allow-auto-merge") {
					body["allow_auto_merge"] = bodyAllowAutoMerge
				}
				if cmd.Flags().Changed("allow-forking") {
					body["allow_forking"] = bodyAllowForking
				}
				if cmd.Flags().Changed("allow-merge-commit") {
					body["allow_merge_commit"] = bodyAllowMergeCommit
				}
				if cmd.Flags().Changed("allow-rebase-merge") {
					body["allow_rebase_merge"] = bodyAllowRebaseMerge
				}
				if cmd.Flags().Changed("allow-squash-merge") {
					body["allow_squash_merge"] = bodyAllowSquashMerge
				}
				if cmd.Flags().Changed("allow-update-branch") {
					body["allow_update_branch"] = bodyAllowUpdateBranch
				}
				if cmd.Flags().Changed("archived") {
					body["archived"] = bodyArchived
				}
				if bodyDefaultBranch != "" {
					body["default_branch"] = bodyDefaultBranch
				}
				if cmd.Flags().Changed("delete-branch-on-merge") {
					body["delete_branch_on_merge"] = bodyDeleteBranchOnMerge
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if cmd.Flags().Changed("has-issues") {
					body["has_issues"] = bodyHasIssues
				}
				if cmd.Flags().Changed("has-projects") {
					body["has_projects"] = bodyHasProjects
				}
				if cmd.Flags().Changed("has-pull-requests") {
					body["has_pull_requests"] = bodyHasPullRequests
				}
				if cmd.Flags().Changed("has-wiki") {
					body["has_wiki"] = bodyHasWiki
				}
				if bodyHomepage != "" {
					body["homepage"] = bodyHomepage
				}
				if cmd.Flags().Changed("is-template") {
					body["is_template"] = bodyIsTemplate
				}
				if bodyMergeCommitMessage != "" {
					body["merge_commit_message"] = bodyMergeCommitMessage
				}
				if bodyMergeCommitTitle != "" {
					body["merge_commit_title"] = bodyMergeCommitTitle
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if cmd.Flags().Changed("private") {
					body["private"] = bodyPrivate
				}
				if bodyPullRequestCreationPolicy != "" {
					body["pull_request_creation_policy"] = bodyPullRequestCreationPolicy
				}
				{
					nestedSecurityAndAnalysis := map[string]any{}
					{
						nestedSecurityAndAnalysisAdvancedSecurity := map[string]any{}
						if bodySecurityAndAnalysisAdvancedSecurityStatus != "" {
							nestedSecurityAndAnalysisAdvancedSecurity["status"] = bodySecurityAndAnalysisAdvancedSecurityStatus
						}
						if len(nestedSecurityAndAnalysisAdvancedSecurity) > 0 {
							nestedSecurityAndAnalysis["advanced_security"] = nestedSecurityAndAnalysisAdvancedSecurity
						}
					}
					{
						nestedSecurityAndAnalysisCodeSecurity := map[string]any{}
						if bodySecurityAndAnalysisCodeSecurityStatus != "" {
							nestedSecurityAndAnalysisCodeSecurity["status"] = bodySecurityAndAnalysisCodeSecurityStatus
						}
						if len(nestedSecurityAndAnalysisCodeSecurity) > 0 {
							nestedSecurityAndAnalysis["code_security"] = nestedSecurityAndAnalysisCodeSecurity
						}
					}
					{
						nestedSecurityAndAnalysisSecretScanning := map[string]any{}
						if bodySecurityAndAnalysisSecretScanningStatus != "" {
							nestedSecurityAndAnalysisSecretScanning["status"] = bodySecurityAndAnalysisSecretScanningStatus
						}
						if len(nestedSecurityAndAnalysisSecretScanning) > 0 {
							nestedSecurityAndAnalysis["secret_scanning"] = nestedSecurityAndAnalysisSecretScanning
						}
					}
					{
						nestedSecurityAndAnalysisSecretScanningAiDetection := map[string]any{}
						if bodySecurityAndAnalysisSecretScanningAiDetectionStatus != "" {
							nestedSecurityAndAnalysisSecretScanningAiDetection["status"] = bodySecurityAndAnalysisSecretScanningAiDetectionStatus
						}
						if len(nestedSecurityAndAnalysisSecretScanningAiDetection) > 0 {
							nestedSecurityAndAnalysis["secret_scanning_ai_detection"] = nestedSecurityAndAnalysisSecretScanningAiDetection
						}
					}
					{
						nestedSecurityAndAnalysisSecretScanningDelegatedAlertDismissal := map[string]any{}
						if bodySecurityAndAnalysisSecretScanningDelegatedAlertDismissalStatus != "" {
							nestedSecurityAndAnalysisSecretScanningDelegatedAlertDismissal["status"] = bodySecurityAndAnalysisSecretScanningDelegatedAlertDismissalStatus
						}
						if len(nestedSecurityAndAnalysisSecretScanningDelegatedAlertDismissal) > 0 {
							nestedSecurityAndAnalysis["secret_scanning_delegated_alert_dismissal"] = nestedSecurityAndAnalysisSecretScanningDelegatedAlertDismissal
						}
					}
					{
						nestedSecurityAndAnalysisSecretScanningDelegatedBypass := map[string]any{}
						if bodySecurityAndAnalysisSecretScanningDelegatedBypassStatus != "" {
							nestedSecurityAndAnalysisSecretScanningDelegatedBypass["status"] = bodySecurityAndAnalysisSecretScanningDelegatedBypassStatus
						}
						if len(nestedSecurityAndAnalysisSecretScanningDelegatedBypass) > 0 {
							nestedSecurityAndAnalysis["secret_scanning_delegated_bypass"] = nestedSecurityAndAnalysisSecretScanningDelegatedBypass
						}
					}
					{
						nestedSecurityAndAnalysisSecretScanningDelegatedBypassOptions := map[string]any{}
						if bodySecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewers != "" {
							var parsedSecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewers any
							if err := json.Unmarshal([]byte(bodySecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewers), &parsedSecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewers); err != nil {
								return fmt.Errorf("parsing --security-and-analysis-secret-scanning-delegated-bypass-options-reviewers JSON: %w", err)
							}
							nestedSecurityAndAnalysisSecretScanningDelegatedBypassOptions["reviewers"] = parsedSecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewers
						}
						if len(nestedSecurityAndAnalysisSecretScanningDelegatedBypassOptions) > 0 {
							nestedSecurityAndAnalysis["secret_scanning_delegated_bypass_options"] = nestedSecurityAndAnalysisSecretScanningDelegatedBypassOptions
						}
					}
					{
						nestedSecurityAndAnalysisSecretScanningNonProviderPatterns := map[string]any{}
						if bodySecurityAndAnalysisSecretScanningNonProviderPatternsStatus != "" {
							nestedSecurityAndAnalysisSecretScanningNonProviderPatterns["status"] = bodySecurityAndAnalysisSecretScanningNonProviderPatternsStatus
						}
						if len(nestedSecurityAndAnalysisSecretScanningNonProviderPatterns) > 0 {
							nestedSecurityAndAnalysis["secret_scanning_non_provider_patterns"] = nestedSecurityAndAnalysisSecretScanningNonProviderPatterns
						}
					}
					{
						nestedSecurityAndAnalysisSecretScanningPushProtection := map[string]any{}
						if bodySecurityAndAnalysisSecretScanningPushProtectionStatus != "" {
							nestedSecurityAndAnalysisSecretScanningPushProtection["status"] = bodySecurityAndAnalysisSecretScanningPushProtectionStatus
						}
						if len(nestedSecurityAndAnalysisSecretScanningPushProtection) > 0 {
							nestedSecurityAndAnalysis["secret_scanning_push_protection"] = nestedSecurityAndAnalysisSecretScanningPushProtection
						}
					}
					if len(nestedSecurityAndAnalysis) > 0 {
						body["security_and_analysis"] = nestedSecurityAndAnalysis
					}
				}
				if bodySquashMergeCommitMessage != "" {
					body["squash_merge_commit_message"] = bodySquashMergeCommitMessage
				}
				if bodySquashMergeCommitTitle != "" {
					body["squash_merge_commit_title"] = bodySquashMergeCommitTitle
				}
				if cmd.Flags().Changed("use-squash-pr-title-as-default") {
					body["use_squash_pr_title_as_default"] = bodyUseSquashPrTitleAsDefault
				}
				if bodyVisibility != "" {
					body["visibility"] = bodyVisibility
				}
				if cmd.Flags().Changed("web-commit-signoff-required") {
					body["web_commit_signoff_required"] = bodyWebCommitSignoffRequired
				}
			}
			data, statusCode, err := c.PatchWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "repos", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "repos", data, "topics")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "repos", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "repos", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "repos", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "patch",
					"resource": "repos",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "repos", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "repos", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyAllowAutoMerge, "allow-auto-merge", false, "Either `true` to allow auto-merge on pull requests, or `false` to disallow auto-merge.")
	cmd.Flags().BoolVar(&bodyAllowForking, "allow-forking", false, "Either `true` to allow private forks, or `false` to prevent private forks.")
	cmd.Flags().BoolVar(&bodyAllowMergeCommit, "allow-merge-commit", true, "Either `true` to allow merging pull requests with a merge commit")
	cmd.Flags().BoolVar(&bodyAllowRebaseMerge, "allow-rebase-merge", true, "Either `true` to allow rebase-merging pull requests, or `false` to prevent rebase-merging.")
	cmd.Flags().BoolVar(&bodyAllowSquashMerge, "allow-squash-merge", true, "Either `true` to allow squash-merging pull requests, or `false` to prevent squash-merging.")
	cmd.Flags().BoolVar(&bodyAllowUpdateBranch, "allow-update-branch", false, "Either `true` to always allow a pull request head branch that is behind its base branch to be updated even if it is not")
	cmd.Flags().BoolVar(&bodyArchived, "archived", false, "Whether to archive this repository. `false` will unarchive a previously archived repository.")
	cmd.Flags().StringVar(&bodyDefaultBranch, "default-branch", "", "Updates the default branch for this repository.")
	cmd.Flags().BoolVar(&bodyDeleteBranchOnMerge, "delete-branch-on-merge", false, "Either `true` to allow automatically deleting head branches when pull requests are merged")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A short description of the repository.")
	cmd.Flags().BoolVar(&bodyHasIssues, "has-issues", true, "Either `true` to enable issues for this repository or `false` to disable them.")
	cmd.Flags().BoolVar(&bodyHasProjects, "has-projects", true, "Either `true` to enable projects for this repository or `false` to disable them.")
	cmd.Flags().BoolVar(&bodyHasPullRequests, "has-pull-requests", true, "Either `true` to allow pull requests for this repository or `false` to prevent pull requests.")
	cmd.Flags().BoolVar(&bodyHasWiki, "has-wiki", true, "Either `true` to enable the wiki for this repository or `false` to disable it.")
	cmd.Flags().StringVar(&bodyHomepage, "homepage", "", "A URL with more information about the repository.")
	cmd.Flags().BoolVar(&bodyIsTemplate, "is-template", false, "Either `true` to make this repo available as a template repository or `false` to prevent it.")
	cmd.Flags().StringVar(&bodyMergeCommitMessage, "merge-commit-message", "", "The default value for a merge commit message. - `PR_TITLE` - default to the pull request's title.")
	cmd.Flags().StringVar(&bodyMergeCommitTitle, "merge-commit-title", "", "Required when using `merge_commit_message`. The default value for a merge commit title.")
	cmd.Flags().StringVar(&bodyName, "name", "", "The name of the repository.")
	cmd.Flags().BoolVar(&bodyPrivate, "private", false, "Either `true` to make the repository private or `false` to make it public. Default: `false`.")
	cmd.Flags().StringVar(&bodyPullRequestCreationPolicy, "pull-request-creation-policy", "", "The policy that controls who can create pull requests for this repository: `all` or `collaborators_only`.")
	cmd.Flags().StringVar(&bodySecurityAndAnalysisAdvancedSecurityStatus, "security-and-analysis-advanced-security-status", "", "Can be `enabled` or `disabled`.")
	cmd.Flags().StringVar(&bodySecurityAndAnalysisCodeSecurityStatus, "security-and-analysis-code-security-status", "", "Can be `enabled` or `disabled`.")
	cmd.Flags().StringVar(&bodySecurityAndAnalysisSecretScanningStatus, "security-and-analysis-secret-scanning-status", "", "Can be `enabled` or `disabled`.")
	cmd.Flags().StringVar(&bodySecurityAndAnalysisSecretScanningAiDetectionStatus, "security-and-analysis-secret-scanning-ai-detection-status", "", "Can be `enabled` or `disabled`.")
	cmd.Flags().StringVar(&bodySecurityAndAnalysisSecretScanningDelegatedAlertDismissalStatus, "security-and-analysis-secret-scanning-delegated-alert-dismissal-status", "", "Can be `enabled` or `disabled`.")
	cmd.Flags().StringVar(&bodySecurityAndAnalysisSecretScanningDelegatedBypassStatus, "security-and-analysis-secret-scanning-delegated-bypass-status", "", "Can be `enabled` or `disabled`.")
	cmd.Flags().StringVar(&bodySecurityAndAnalysisSecretScanningDelegatedBypassOptionsReviewers, "security-and-analysis-secret-scanning-delegated-bypass-options-reviewers", "", "The bypass reviewers for secret scanning delegated bypass.")
	cmd.Flags().StringVar(&bodySecurityAndAnalysisSecretScanningNonProviderPatternsStatus, "security-and-analysis-secret-scanning-non-provider-patterns-status", "", "Can be `enabled` or `disabled`.")
	cmd.Flags().StringVar(&bodySecurityAndAnalysisSecretScanningPushProtectionStatus, "security-and-analysis-secret-scanning-push-protection-status", "", "Can be `enabled` or `disabled`.")
	cmd.Flags().StringVar(&bodySquashMergeCommitMessage, "squash-merge-commit-message", "", "The default value for a squash merge commit message: - `PR_BODY` - default to the pull request's body.")
	cmd.Flags().StringVar(&bodySquashMergeCommitTitle, "squash-merge-commit-title", "", "Required when using `squash_merge_commit_message`.")
	cmd.Flags().BoolVar(&bodyUseSquashPrTitleAsDefault, "use-squash-pr-title-as-default", false, "Either `true` to allow squash-merge commits to use pull request title, or `false` to use commit message.")
	cmd.Flags().StringVar(&bodyVisibility, "visibility", "", "The visibility of the repository.")
	cmd.Flags().BoolVar(&bodyWebCommitSignoffRequired, "web-commit-signoff-required", false, "Either `true` to require contributors to sign off on web-based commits")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
