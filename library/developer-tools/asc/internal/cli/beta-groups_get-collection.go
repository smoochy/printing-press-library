// Copyright 2026 justinwfu and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newBetaGroupsGetCollectionCmd(flags *rootFlags) *cobra.Command {
	var flagFilterName string
	var flagFilterIsInternalGroup string
	var flagFilterPublicLinkEnabled string
	var flagFilterPublicLinkLimitEnabled string
	var flagFilterPublicLink string
	var flagFilterApp string
	var flagFilterBuilds string
	var flagFilterId string
	var flagSort string
	var flagFieldsBetaGroups string
	var flagFieldsApps string
	var flagFieldsBuilds string
	var flagFieldsBetaTesters string
	var flagFieldsBetaRecruitmentCriteria string
	var flagLimit int
	var flagLimitBetaTesters int
	var flagLimitBuilds int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "get-collection",
		Aliases:     []string{"list"},
		Short:       "Get collection",
		Example:     "  asc-pp-cli beta-groups get-collection",
		Annotations: map[string]string{"pp:endpoint": "beta-groups.get-collection", "pp:method": "GET", "pp:path": "/v1/betaGroups", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			path := "/v1/betaGroups"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "beta-groups", path, map[string]string{
				"filter[name]":                    formatCLIParamValue(flagFilterName),
				"filter[isInternalGroup]":         formatCLIParamValue(flagFilterIsInternalGroup),
				"filter[publicLinkEnabled]":       formatCLIParamValue(flagFilterPublicLinkEnabled),
				"filter[publicLinkLimitEnabled]":  formatCLIParamValue(flagFilterPublicLinkLimitEnabled),
				"filter[publicLink]":              formatCLIParamValue(flagFilterPublicLink),
				"filter[app]":                     formatCLIParamValue(flagFilterApp),
				"filter[builds]":                  formatCLIParamValue(flagFilterBuilds),
				"filter[id]":                      formatCLIParamValue(flagFilterId),
				"sort":                            formatCLIParamValue(flagSort),
				"fields[betaGroups]":              formatCLIParamValue(flagFieldsBetaGroups),
				"fields[apps]":                    formatCLIParamValue(flagFieldsApps),
				"fields[builds]":                  formatCLIParamValue(flagFieldsBuilds),
				"fields[betaTesters]":             formatCLIParamValue(flagFieldsBetaTesters),
				"fields[betaRecruitmentCriteria]": formatCLIParamValue(flagFieldsBetaRecruitmentCriteria),
				"limit":                           formatCLIParamValue(flagLimit),
				"limit[betaTesters]":              formatCLIParamValue(flagLimitBetaTesters),
				"limit[builds]":                   formatCLIParamValue(flagLimitBuilds),
			}, nil, flagAll, "", "offset", "limit", 100, "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagFilterName, "filter-name", "", "filter by attribute 'name'")
	cmd.Flags().StringVar(&flagFilterIsInternalGroup, "filter-is-internal-group", "", "filter by attribute 'isInternalGroup'")
	cmd.Flags().StringVar(&flagFilterPublicLinkEnabled, "filter-public-link-enabled", "", "filter by attribute 'publicLinkEnabled'")
	cmd.Flags().StringVar(&flagFilterPublicLinkLimitEnabled, "filter-public-link-limit-enabled", "", "filter by attribute 'publicLinkLimitEnabled'")
	cmd.Flags().StringVar(&flagFilterPublicLink, "filter-public-link", "", "filter by attribute 'publicLink'")
	cmd.Flags().StringVar(&flagFilterApp, "filter-app", "", "filter by id(s) of related 'app'")
	cmd.Flags().StringVar(&flagFilterBuilds, "filter-builds", "", "filter by id(s) of related 'builds'")
	cmd.Flags().StringVar(&flagFilterId, "filter-id", "", "filter by id(s)")
	cmd.Flags().StringVar(&flagSort, "sort", "", "comma-separated list of sort expressions; resources will be sorted as specified")
	cmd.Flags().StringVar(&flagFieldsBetaGroups, "fields-beta-groups", "", "the fields to include for returned resources of type betaGroups")
	cmd.Flags().StringVar(&flagFieldsApps, "fields-apps", "", "the fields to include for returned resources of type apps")
	cmd.Flags().StringVar(&flagFieldsBuilds, "fields-builds", "", "the fields to include for returned resources of type builds")
	cmd.Flags().StringVar(&flagFieldsBetaTesters, "fields-beta-testers", "", "the fields to include for returned resources of type betaTesters")
	cmd.Flags().StringVar(&flagFieldsBetaRecruitmentCriteria, "fields-beta-recruitment-criteria", "", "the fields to include for returned resources of type betaRecruitmentCriteria")
	cmd.Flags().IntVar(&flagLimit, "limit", 0, "maximum resources per page")
	cmd.Flags().IntVar(&flagLimitBetaTesters, "limit-beta-testers", 0, "maximum number of related betaTesters returned (when they are included)")
	cmd.Flags().IntVar(&flagLimitBuilds, "limit-builds", 0, "maximum number of related builds returned (when they are included)")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
