// Copyright 2026 RyanGravetteIDLA and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAdminDirectoryGroupsListCmd(flags *rootFlags) *cobra.Command {
	var flagCustomer string
	var flagDomain string
	var flagMaxResults int
	var flagOrderBy string
	var flagPageToken string
	var flagQuery string
	var flagSortOrder string
	var flagUserKey string
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "directory-groups-list",
		Aliases:     []string{"list"},
		Short:       "Retrieves all groups of a domain or of a user given a userKey (paginated).",
		Example:     "  workspace-admin-pp-cli admin directory-groups-list",
		Annotations: map[string]string{"pp:endpoint": "admin.directory-groups-list", "pp:method": "GET", "pp:path": "/admin/directory/v1/groups", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("order-by") {
				allowedOrderBy := []string{"email"}
				validOrderBy := false
				for _, v := range allowedOrderBy {
					if flagOrderBy == v {
						validOrderBy = true
						break
					}
				}
				if !validOrderBy {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagOrderBy, "order-by", allowedOrderBy)
				}
			}
			if cmd.Flags().Changed("sort-order") {
				allowedSortOrder := []string{"ASCENDING", "DESCENDING"}
				validSortOrder := false
				for _, v := range allowedSortOrder {
					if flagSortOrder == v {
						validSortOrder = true
						break
					}
				}
				if !validSortOrder {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagSortOrder, "sort-order", allowedSortOrder)
				}
			}
			path := "/admin/directory/v1/groups"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "admin", path, map[string]string{
				"customer":   formatCLIParamValue(flagCustomer),
				"domain":     formatCLIParamValue(flagDomain),
				"maxResults": formatCLIParamValue(flagMaxResults),
				"orderBy":    formatCLIParamValue(flagOrderBy),
				"pageToken":  formatCLIParamValue(flagPageToken),
				"query":      formatCLIParamValue(flagQuery),
				"sortOrder":  formatCLIParamValue(flagSortOrder),
				"userKey":    formatCLIParamValue(flagUserKey),
			}, nil, flagAll, "pageToken", "page_token", "maxResults", "nextPageToken", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&flagCustomer, "customer", "my_customer", // PATCH(workspace-admin-required-list-params): Google Directory list requires customer; default the standard alias, user override still wins
		"The unique ID for the customer's Google Workspace account.")
	cmd.Flags().StringVar(&flagDomain, "domain", "", "The domain name. Use this field to get groups from only one domain.")
	cmd.Flags().IntVar(&flagMaxResults, "max-results", 0, "Maximum number of results to return. Max allowed value is 200.")
	cmd.Flags().StringVar(&flagOrderBy, "order-by", "", "Column to use for sorting results (one of: email)")
	cmd.Flags().StringVar(&flagPageToken, "page-token", "", "Token to specify next page in the list")
	cmd.Flags().StringVar(&flagQuery, "query", "", "Query string search. Should be of the form ''. Complete documentation is at https: //developers.google.")
	cmd.Flags().StringVar(&flagSortOrder, "sort-order", "", "Whether to return results in ascending or descending order. Only of use when orderBy is also used (one of: ASCENDING, DESCENDING)")
	cmd.Flags().StringVar(&flagUserKey, "user-key", "", "Email or immutable ID of the user if only those groups are to be listed, the given user is a member of.")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
