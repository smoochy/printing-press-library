// Copyright 2026 RyanGravetteIDLA and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newChangesWatchCmd(flags *rootFlags) *cobra.Command {
	var flagPageToken string
	var flagDriveId string
	var flagIncludeCorpusRemovals bool
	var flagIncludeItemsFromAllDrives bool
	var flagIncludeLabels string
	var flagIncludePermissionsForView string
	var flagIncludeRemoved bool
	var flagIncludeTeamDriveItems bool
	var flagPageSize int
	var flagRestrictToMyDrive bool
	var flagSpaces string
	var flagSupportsAllDrives bool
	var flagSupportsTeamDrives bool
	var flagTeamDriveId string
	var bodyAddress string
	var bodyExpiration string
	var bodyId string
	var bodyKind string
	var bodyParams string
	var bodyPayload bool
	var bodyResourceId string
	var bodyResourceUri string
	var bodyToken string
	var bodyType string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "watch",
		Aliases:     []string{"create"},
		Short:       "Subscribes to changes for a user. To use this method, you must include the pageToken query parameter.",
		Example:     "  workspace-admin-pp-cli changes watch --page-token your-token-here",
		Annotations: map[string]string{"pp:endpoint": "changes.watch", "pp:method": "POST", "pp:path": "https://www.googleapis.com/drive/v3/changes/watch"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !cmd.Flags().Changed("page-token") && !flags.dryRun {
				return fmt.Errorf("required flag \"%s\" not set", "page-token")
			}
			if !stdinBody {
			}
			path := "https://www.googleapis.com/drive/v3/changes/watch"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			if flagPageToken != "" {
				params["pageToken"] = formatCLIParamValue(flagPageToken)
			}
			if flagDriveId != "" {
				params["driveId"] = formatCLIParamValue(flagDriveId)
			}
			if flagIncludeCorpusRemovals != false {
				params["includeCorpusRemovals"] = formatCLIParamValue(flagIncludeCorpusRemovals)
			}
			if flagIncludeItemsFromAllDrives != false {
				params["includeItemsFromAllDrives"] = formatCLIParamValue(flagIncludeItemsFromAllDrives)
			}
			if flagIncludeLabels != "" {
				params["includeLabels"] = formatCLIParamValue(flagIncludeLabels)
			}
			if flagIncludePermissionsForView != "" {
				params["includePermissionsForView"] = formatCLIParamValue(flagIncludePermissionsForView)
			}
			if flagIncludeRemoved != false {
				params["includeRemoved"] = formatCLIParamValue(flagIncludeRemoved)
			}
			if flagIncludeTeamDriveItems != false {
				params["includeTeamDriveItems"] = formatCLIParamValue(flagIncludeTeamDriveItems)
			}
			if flagPageSize != 0 {
				params["pageSize"] = formatCLIParamValue(flagPageSize)
			}
			if flagRestrictToMyDrive != false {
				params["restrictToMyDrive"] = formatCLIParamValue(flagRestrictToMyDrive)
			}
			if flagSpaces != "" {
				params["spaces"] = formatCLIParamValue(flagSpaces)
			}
			if flagSupportsAllDrives != false {
				params["supportsAllDrives"] = formatCLIParamValue(flagSupportsAllDrives)
			}
			if flagSupportsTeamDrives != false {
				params["supportsTeamDrives"] = formatCLIParamValue(flagSupportsTeamDrives)
			}
			if flagTeamDriveId != "" {
				params["teamDriveId"] = formatCLIParamValue(flagTeamDriveId)
			}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAddress != "" {
					body["address"] = bodyAddress
				}
				if bodyExpiration != "" {
					body["expiration"] = bodyExpiration
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyKind != "" {
					body["kind"] = bodyKind
				}
				if bodyParams != "" {
					var parsedParams any
					if err := json.Unmarshal([]byte(bodyParams), &parsedParams); err != nil {
						return fmt.Errorf("parsing --params JSON: %w", err)
					}
					body["params"] = parsedParams
				}
				if cmd.Flags().Changed("payload") {
					body["payload"] = bodyPayload
				}
				if bodyResourceId != "" {
					body["resourceId"] = bodyResourceId
				}
				if bodyResourceUri != "" {
					body["resourceUri"] = bodyResourceUri
				}
				if bodyToken != "" {
					body["token"] = bodyToken
				}
				if bodyType != "" {
					body["type"] = bodyType
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "changes", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "changes", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "changes", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "changes", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "changes", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "changes",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "changes", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "changes", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagPageToken, "page-token", "", "The token for continuing a previous list request on the next page.")
	cmd.Flags().StringVar(&flagDriveId, "drive-id", "", "The shared drive from which changes are returned.")
	cmd.Flags().BoolVar(&flagIncludeCorpusRemovals, "include-corpus-removals", false, "Whether changes should include the file resource if the file is still accessible by the user at the time of the request")
	cmd.Flags().BoolVar(&flagIncludeItemsFromAllDrives, "include-items-from-all-drives", false, "Whether both My Drive and shared drive items should be included in results.")
	cmd.Flags().StringVar(&flagIncludeLabels, "include-labels", "", "A comma-separated list of IDs of labels to include in the labelInfo part of the response.")
	cmd.Flags().StringVar(&flagIncludePermissionsForView, "include-permissions-for-view", "", "Specifies which additional view's permissions to include in the response. Only 'published' is supported.")
	cmd.Flags().BoolVar(&flagIncludeRemoved, "include-removed", false, "Whether to include changes indicating that items have been removed from the list of changes")
	cmd.Flags().BoolVar(&flagIncludeTeamDriveItems, "include-team-drive-items", false, "Deprecated use includeItemsFromAllDrives instead.")
	cmd.Flags().IntVar(&flagPageSize, "page-size", 0, "The maximum number of changes to return per page.")
	cmd.Flags().BoolVar(&flagRestrictToMyDrive, "restrict-to-my-drive", false, "Whether to restrict the results to changes inside the My Drive hierarchy.")
	cmd.Flags().StringVar(&flagSpaces, "spaces", "", "A comma-separated list of spaces to query within the corpora. Supported values are 'drive' and 'appDataFolder'.")
	cmd.Flags().BoolVar(&flagSupportsAllDrives, "supports-all-drives", false, "Whether the requesting application supports both My Drives and shared drives.")
	cmd.Flags().BoolVar(&flagSupportsTeamDrives, "supports-team-drives", false, "Deprecated use supportsAllDrives instead.")
	cmd.Flags().StringVar(&flagTeamDriveId, "team-drive-id", "", "Deprecated use driveId instead.")
	cmd.Flags().StringVar(&bodyAddress, "address", "", "The address where notifications are delivered for this channel.")
	cmd.Flags().StringVar(&bodyExpiration, "expiration", "", "Date and time of notification channel expiration, expressed as a Unix timestamp, in milliseconds. Optional.")
	cmd.Flags().StringVar(&bodyId, "id", "", "A UUID or similar unique string that identifies this channel.")
	cmd.Flags().StringVar(&bodyKind, "kind", "api#channel", "Identifies this as a notification channel used to watch for changes to a resource, which is 'api#channel'.")
	cmd.Flags().StringVar(&bodyParams, "params", "", "Additional parameters controlling delivery channel behavior. Optional.")
	cmd.Flags().BoolVar(&bodyPayload, "payload", false, "A Boolean value to indicate whether payload is wanted. Optional.")
	cmd.Flags().StringVar(&bodyResourceId, "resource-id", "", "An opaque ID that identifies the resource being watched on this channel. Stable across different API versions.")
	cmd.Flags().StringVar(&bodyResourceUri, "resource-uri", "", "A version-specific identifier for the watched resource.")
	cmd.Flags().StringVar(&bodyToken, "token", "", "An arbitrary string delivered to the target address with each notification delivered over this channel. Optional.")
	cmd.Flags().StringVar(&bodyType, "type", "", "The type of delivery mechanism used for this channel. Valid values are 'web_hook' (or 'webhook').")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
