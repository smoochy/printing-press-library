// Copyright 2026 RyanGravetteIDLA and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newAdminDirectoryCustomersUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyAlternateEmail string
	var bodyCustomerCreationTime string
	var bodyCustomerDomain string
	var bodyEtag string
	var bodyId string
	var bodyKind string
	var bodyLanguage string
	var bodyPhoneNumber string
	var bodyPostalAddressAddressLine1 string
	var bodyPostalAddressAddressLine2 string
	var bodyPostalAddressAddressLine3 string
	var bodyPostalAddressContactName string
	var bodyPostalAddressCountryCode string
	var bodyPostalAddressLocality string
	var bodyPostalAddressOrganizationName string
	var bodyPostalAddressPostalCode string
	var bodyPostalAddressRegion string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "directory-customers-update <customerKey>",
		Short:       "Updates a customer.",
		Example:     "  workspace-admin-pp-cli admin directory-customers-update your-token-here",
		Annotations: map[string]string{"pp:endpoint": "admin.directory-customers-update", "pp:method": "PUT", "pp:path": "/admin/directory/v1/customers/{customerKey}"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			path := "/admin/directory/v1/customers/{customerKey}"
			if len(args) < 1 || args[0] == "" {
				return usageErr(fmt.Errorf("customerKey is required\nUsage: %s <%s>", cmd.CommandPath(), "customerKey"))
			}
			path = replacePathParam(path, "customerKey", args[0])
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAlternateEmail != "" {
					body["alternateEmail"] = bodyAlternateEmail
				}
				if bodyCustomerCreationTime != "" {
					body["customerCreationTime"] = bodyCustomerCreationTime
				}
				if bodyCustomerDomain != "" {
					body["customerDomain"] = bodyCustomerDomain
				}
				if bodyEtag != "" {
					body["etag"] = bodyEtag
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyKind != "" {
					body["kind"] = bodyKind
				}
				if bodyLanguage != "" {
					body["language"] = bodyLanguage
				}
				if bodyPhoneNumber != "" {
					body["phoneNumber"] = bodyPhoneNumber
				}
				{
					nestedPostalAddress := map[string]any{}
					if bodyPostalAddressAddressLine1 != "" {
						nestedPostalAddress["addressLine1"] = bodyPostalAddressAddressLine1
					}
					if bodyPostalAddressAddressLine2 != "" {
						nestedPostalAddress["addressLine2"] = bodyPostalAddressAddressLine2
					}
					if bodyPostalAddressAddressLine3 != "" {
						nestedPostalAddress["addressLine3"] = bodyPostalAddressAddressLine3
					}
					if bodyPostalAddressContactName != "" {
						nestedPostalAddress["contactName"] = bodyPostalAddressContactName
					}
					if bodyPostalAddressCountryCode != "" {
						nestedPostalAddress["countryCode"] = bodyPostalAddressCountryCode
					}
					if bodyPostalAddressLocality != "" {
						nestedPostalAddress["locality"] = bodyPostalAddressLocality
					}
					if bodyPostalAddressOrganizationName != "" {
						nestedPostalAddress["organizationName"] = bodyPostalAddressOrganizationName
					}
					if bodyPostalAddressPostalCode != "" {
						nestedPostalAddress["postalCode"] = bodyPostalAddressPostalCode
					}
					if bodyPostalAddressRegion != "" {
						nestedPostalAddress["region"] = bodyPostalAddressRegion
					}
					if len(nestedPostalAddress) > 0 {
						body["postalAddress"] = nestedPostalAddress
					}
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "admin", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "admin", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "admin",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "admin", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAlternateEmail, "alternate-email", "", "The customer's secondary contact email address. This email address cannot be on the same domain as the `customerDomain`")
	cmd.Flags().StringVar(&bodyCustomerCreationTime, "customer-creation-time", "", "The customer's creation time (Readonly)")
	cmd.Flags().StringVar(&bodyCustomerDomain, "customer-domain", "", "The customer's primary domain name string. Do not include the `www` prefix when creating a new customer.")
	cmd.Flags().StringVar(&bodyEtag, "etag", "", "ETag of the resource.")
	cmd.Flags().StringVar(&bodyId, "id", "", "The unique ID for the customer's Google Workspace account. (Readonly)")
	cmd.Flags().StringVar(&bodyKind, "kind", "admin#directory#customer", "Identifies the resource as a customer. Value: `admin#directory#customer`")
	cmd.Flags().StringVar(&bodyLanguage, "language", "", "The customer's ISO 639-2 language code.")
	cmd.Flags().StringVar(&bodyPhoneNumber, "phone-number", "", "The customer's contact phone number in [E.164](https://en.wikipedia.org/wiki/E.164) format.")
	cmd.Flags().StringVar(&bodyPostalAddressAddressLine1, "postal-address-address-line1", "", "A customer's physical address. The address can be composed of one to three lines.")
	cmd.Flags().StringVar(&bodyPostalAddressAddressLine2, "postal-address-address-line2", "", "Address line 2 of the address.")
	cmd.Flags().StringVar(&bodyPostalAddressAddressLine3, "postal-address-address-line3", "", "Address line 3 of the address.")
	cmd.Flags().StringVar(&bodyPostalAddressContactName, "postal-address-contact-name", "", "The customer contact's name.")
	cmd.Flags().StringVar(&bodyPostalAddressCountryCode, "postal-address-country-code", "", "This is a required property. For `countryCode` information see the [ISO 3166 country code elements](https://www.iso.")
	cmd.Flags().StringVar(&bodyPostalAddressLocality, "postal-address-locality", "", "Name of the locality. An example of a locality value is the city of `San Francisco`.")
	cmd.Flags().StringVar(&bodyPostalAddressOrganizationName, "postal-address-organization-name", "", "The company or company division name.")
	cmd.Flags().StringVar(&bodyPostalAddressPostalCode, "postal-address-postal-code", "", "The postal code. A postalCode example is a postal zip code such as `10009`.")
	cmd.Flags().StringVar(&bodyPostalAddressRegion, "postal-address-region", "", "Name of the region. An example of a region value is `NY` for the state of New York.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
