{
  "api_name": "granola",
  "base_url": "https://public-api.granola.ai",
  "description": "API for accessing meeting data from Granola",
  "mcp_ready": "full",
  "http_transport": "standard",
  "auth": {
    "type": "bearer_token",
    "header": "Authorization",
    "env_vars": [
      "GRANOLA_API_KEY",
      "GRANOLA_AUDIT_API_KEY",
      "GRANOLA_WEBHOOK_SECRET"
    ],
    "env_var_specs": [
      {
        "name": "GRANOLA_API_KEY",
        "kind": "per_call",
        "required": false,
        "sensitive": true,
        "description": "Granola public API key, created in Granola desktop under Settings → Connectors → API keys. Creating one requires a Business or Enterprise Granola workspace; personal and free workspaces cannot issue keys. Needed to fetch new meeting data via `sync-api`, because current Granola desktop builds keep the local cache encryption key in an entitlement-gated macOS keychain group that no third-party binary can read. Reading data already synced into the local store needs no key at all."
      },
      {
        "name": "GRANOLA_AUDIT_API_KEY",
        "kind": "per_call",
        "required": false,
        "sensitive": true,
        "description": "Dedicated Granola audit-log API key."
      },
      {
        "name": "GRANOLA_WEBHOOK_SECRET",
        "kind": "per_call",
        "required": false,
        "sensitive": true,
        "description": "Webhook signing secret used only for offline delivery verification."
      }
    ]
  },
  "required_headers": [],
  "tools": [
    {
      "name": "folders_list",
      "description": "List folders. Optional: cursor, page_size (default: 10). Returns the ListFoldersOutput.",
      "method": "GET",
      "path": "/v1/folders",
      "params": [
        {
          "name": "cursor",
          "type": "string",
          "location": "query",
          "description": "Cursor"
        },
        {
          "name": "page_size",
          "type": "int",
          "location": "query",
          "description": "Page size (1-30; default 10)"
        }
      ]
    },
    {
      "name": "notes_get",
      "description": "Get note. Required: note_id. Optional: include. Returns the Note.",
      "method": "GET",
      "path": "/v1/notes/{note_id}",
      "params": [
        {
          "name": "note_id",
          "type": "string",
          "location": "path",
          "description": "Note id",
          "required": true
        },
        {
          "name": "include",
          "type": "string",
          "location": "query",
          "description": "Include"
        }
      ]
    },
    {
      "name": "notes_list",
      "description": "List notes. Optional: created_before, created_after, updated_after, folder_id, cursor, page_size. Returns the ListNotesOutput.",
      "method": "GET",
      "path": "/v1/notes",
      "params": [
        {
          "name": "created_before",
          "type": "string",
          "location": "query",
          "description": "Created before"
        },
        {
          "name": "created_after",
          "type": "string",
          "location": "query",
          "description": "Created after"
        },
        {
          "name": "updated_after",
          "type": "string",
          "location": "query",
          "description": "Updated after"
        },
        {
          "name": "folder_id",
          "type": "string",
          "location": "query",
          "description": "Only notes in this folder"
        },
        {
          "name": "cursor",
          "type": "string",
          "location": "query",
          "description": "Cursor"
        },
        {
          "name": "page_size",
          "type": "int",
          "location": "query",
          "description": "Page size (1-30; default 10)"
        }
      ]
    }
  ]
}
