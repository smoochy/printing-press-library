// Copyright 2026 and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/project-management/redmine/internal/cliutil"
	"github.com/spf13/cobra"
)

func newProjectsJsonCreateProjectCmd(flags *rootFlags) *cobra.Command {
	var flagXRedmineSwitchUser string
	var bodyProjectCustomFieldValues string
	var bodyProjectCustomFields string
	var bodyProjectDefaultAssignedToId int
	var bodyProjectDefaultIssueQueryId int
	var bodyProjectDefaultVersionId int
	var bodyProjectDescription string
	var bodyProjectEnabledModuleNames string
	var bodyProjectHomepage string
	var bodyProjectIdentifier string
	var bodyProjectInheritMembers bool
	var bodyProjectIsPublic bool
	var bodyProjectIssueCustomFieldIds string
	var bodyProjectName string
	var bodyProjectParentId int
	var bodyProjectTrackerIds string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create-project",
		Aliases:     []string{"create"},
		Short:       "Create project",
		Example:     "  redmine-pp-cli projects-json create-project",
		Annotations: map[string]string{"pp:endpoint": "projects-json.create-project", "pp:method": "POST", "pp:path": "/projects.json"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			// Machine callers (--json/--agent, which sets asJSON) get a usage
			// error + exit 2 instead of silent exit-0 help, so an incomplete
			// invocation is never mistaken for success.
			if !hasChangedLocalFlags(cmd) && len(args) == 0 && !flags.dryRun {
				if flags.asJSON {
					if printErr := printJSONFiltered(cmd.OutOrStdout(), map[string]any{
						"error": "requires input",
						"usage": cmd.CommandPath() + " --help",
					}, flags); printErr != nil {
						return printErr
					}
					return usageErr(fmt.Errorf("%q requires input; run %q for usage", cmd.CommandPath(), cmd.CommandPath()+" --help"))
				}
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("project-identifier") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "project-identifier")
				}
				if !cmd.Flags().Changed("project-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "project-name")
				}
			}
			path := "/projects.json"
			c, err := flags.newClient()
			if err != nil {
				return err
			}
			headerOverrides := map[string]string{}

			if cmd.Flags().Changed("x-redmine-switch-user") || flagXRedmineSwitchUser != "" {
				headerOverrides["X-Redmine-Switch-User"] = formatCLIParamValue(flagXRedmineSwitchUser)
			}

			params := map[string]string{}
			var body any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				bodyMap := map[string]any{}
				body = map[string]any{"project": bodyMap}
				if cmd.Flags().Changed("project-custom-field-values") || bodyProjectCustomFieldValues != "" {
					var parsedProjectCustomFieldValues any
					if err := json.Unmarshal([]byte(bodyProjectCustomFieldValues), &parsedProjectCustomFieldValues); err != nil {
						return fmt.Errorf("parsing --project-custom-field-values JSON: %w", err)
					}
					asMap, ok := parsedProjectCustomFieldValues.(map[string]any)
					if !ok {
						return fmt.Errorf("--project-custom-field-values must be a JSON object, got JSON %T", parsedProjectCustomFieldValues)
					}
					bodyMap["custom_field_values"] = asMap
				}
				if cmd.Flags().Changed("project-custom-fields") || bodyProjectCustomFields != "" {
					var parsedProjectCustomFields any
					if err := json.Unmarshal([]byte(bodyProjectCustomFields), &parsedProjectCustomFields); err != nil {
						return fmt.Errorf("parsing --project-custom-fields JSON: %w", err)
					}
					asArray, ok := parsedProjectCustomFields.([]any)
					if !ok {
						return fmt.Errorf("--project-custom-fields must be a JSON array, got JSON %T", parsedProjectCustomFields)
					}
					bodyMap["custom_fields"] = asArray
				}
				if cmd.Flags().Changed("project-default-assigned-to-id") || bodyProjectDefaultAssignedToId != 0 {
					bodyMap["default_assigned_to_id"] = bodyProjectDefaultAssignedToId
				}
				if cmd.Flags().Changed("project-default-issue-query-id") || bodyProjectDefaultIssueQueryId != 0 {
					bodyMap["default_issue_query_id"] = bodyProjectDefaultIssueQueryId
				}
				if cmd.Flags().Changed("project-default-version-id") || bodyProjectDefaultVersionId != 0 {
					bodyMap["default_version_id"] = bodyProjectDefaultVersionId
				}
				if cmd.Flags().Changed("project-description") || bodyProjectDescription != "" {
					bodyMap["description"] = bodyProjectDescription
				}
				if cmd.Flags().Changed("project-enabled-module-names") {
					parsedProjectEnabledModuleNames, parseErr := cliutil.ParseStringList(bodyProjectEnabledModuleNames)
					if parseErr != nil {
						return fmt.Errorf("parsing --project-enabled-module-names list: %w", parseErr)
					}
					bodyMap["enabled_module_names"] = parsedProjectEnabledModuleNames
				}
				if cmd.Flags().Changed("project-homepage") || bodyProjectHomepage != "" {
					bodyMap["homepage"] = bodyProjectHomepage
				}
				if cmd.Flags().Changed("project-identifier") || bodyProjectIdentifier != "" {
					bodyMap["identifier"] = bodyProjectIdentifier
				}
				if cmd.Flags().Changed("project-inherit-members") {
					bodyMap["inherit_members"] = bodyProjectInheritMembers
				}
				if cmd.Flags().Changed("project-is-public") {
					bodyMap["is_public"] = bodyProjectIsPublic
				}
				if cmd.Flags().Changed("project-issue-custom-field-ids") || bodyProjectIssueCustomFieldIds != "" {
					var parsedProjectIssueCustomFieldIds any
					if err := json.Unmarshal([]byte(bodyProjectIssueCustomFieldIds), &parsedProjectIssueCustomFieldIds); err != nil {
						return fmt.Errorf("parsing --project-issue-custom-field-ids JSON: %w", err)
					}
					asArray, ok := parsedProjectIssueCustomFieldIds.([]any)
					if !ok {
						return fmt.Errorf("--project-issue-custom-field-ids must be a JSON array, got JSON %T", parsedProjectIssueCustomFieldIds)
					}
					bodyMap["issue_custom_field_ids"] = asArray
				}
				if cmd.Flags().Changed("project-name") || bodyProjectName != "" {
					bodyMap["name"] = bodyProjectName
				}
				if cmd.Flags().Changed("project-parent-id") || bodyProjectParentId != 0 {
					bodyMap["parent_id"] = bodyProjectParentId
				}
				if cmd.Flags().Changed("project-tracker-ids") || bodyProjectTrackerIds != "" {
					var parsedProjectTrackerIds any
					if err := json.Unmarshal([]byte(bodyProjectTrackerIds), &parsedProjectTrackerIds); err != nil {
						return fmt.Errorf("parsing --project-tracker-ids JSON: %w", err)
					}
					asArray, ok := parsedProjectTrackerIds.([]any)
					if !ok {
						return fmt.Errorf("--project-tracker-ids must be a JSON array, got JSON %T", parsedProjectTrackerIds)
					}
					bodyMap["tracker_ids"] = asArray
				}
			}
			data, statusCode, err := c.PostWithParamsAndHeaders(cmd.Context(), path, params, body, headerOverrides)
			if err != nil {
				return classifyAPIError(cmd.OutOrStdout(), err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "projects-json", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "projects-json", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects-json", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects-json", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects-json", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "projects-json",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if flags.agent {
					envelope["meta"] = map[string]any{"source": "live"}
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered, map[string]bool{"project": true})
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						if flags.agent {
							envelope["results"] = parsed
						} else {
							envelope["data"] = parsed
						}
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				resultKey := "data"
				if flags.agent {
					resultKey = "results"
				}
				structured, err := wrapPlatformStructuredOutput(json.RawMessage(envelopeJSON), flags, resultKey, true)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), structured, true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects-json", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "projects-json", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&flagXRedmineSwitchUser, "x-redmine-switch-user", "", "X redmine switch user")
	cmd.Flags().StringVar(&bodyProjectCustomFieldValues, "project-custom-field-values", "", "Custom field values")
	cmd.Flags().StringVar(&bodyProjectCustomFields, "project-custom-fields", "", "Custom fields")
	cmd.Flags().IntVar(&bodyProjectDefaultAssignedToId, "project-default-assigned-to-id", 0, "Default assignee for new issues in this project. Set to null to clear.")
	cmd.Flags().IntVar(&bodyProjectDefaultIssueQueryId, "project-default-issue-query-id", 0, "Default saved query for the project's issue list page. Set to null to clear.")
	cmd.Flags().IntVar(&bodyProjectDefaultVersionId, "project-default-version-id", 0, "Default target version for new issues in this project. Set to null to clear.")
	cmd.Flags().StringVar(&bodyProjectDescription, "project-description", "", "Description")
	cmd.Flags().StringVar(&bodyProjectEnabledModuleNames, "project-enabled-module-names", "", "Enabled module names for this project.")
	cmd.Flags().StringVar(&bodyProjectHomepage, "project-homepage", "", "Homepage")
	cmd.Flags().StringVar(&bodyProjectIdentifier, "project-identifier", "", "Identifier")
	cmd.Flags().BoolVar(&bodyProjectInheritMembers, "project-inherit-members", false, "Inherit members from the parent project. Only effective for subprojects (parent_id is set).")
	cmd.Flags().BoolVar(&bodyProjectIsPublic, "project-is-public", false, "Is public")
	cmd.Flags().StringVar(&bodyProjectIssueCustomFieldIds, "project-issue-custom-field-ids", "", "IDs of issue custom fields enabled for this project.")
	cmd.Flags().StringVar(&bodyProjectName, "project-name", "", "Name")
	cmd.Flags().IntVar(&bodyProjectParentId, "project-parent-id", 0, "Parent id")
	cmd.Flags().StringVar(&bodyProjectTrackerIds, "project-tracker-ids", "", "IDs of trackers available in this project.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
