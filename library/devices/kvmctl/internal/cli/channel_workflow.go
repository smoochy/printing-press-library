// Copyright 2026 keithah and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/mvanhorn/printing-press-library/library/devices/kvmctl/internal/store"
	"github.com/spf13/cobra"
)

func newWorkflowCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:         "workflow",
		Short:       "Compound workflows that combine multiple API operations",
		Annotations: map[string]string{"mcp:read-only": "true", "pp:parent-group": "true"},
		RunE:        parentNoSubcommandRunE(flags),
	}
	cmd.AddCommand(newWorkflowStatusCmd(flags))

	return cmd
}

func newWorkflowStatusCmd(flags *rootFlags) *cobra.Command {
	var dbPath string

	cmd := &cobra.Command{
		Use:         "status",
		Short:       "Show local archive status and sync state for all resources",
		Annotations: map[string]string{"mcp:read-only": "true"},
		Example: `  # Show archive status
  kvmctl-pp-cli workflow status

  # Show status as JSON
  kvmctl-pp-cli workflow status --json`,
		RunE: func(cmd *cobra.Command, args []string) error {
			if dbPath == "" {
				dbPath = defaultDBPath("kvmctl-pp-cli")
			}

			status := map[string]int{}
			if _, err := os.Stat(dbPath); err == nil {
				s, err := store.OpenReadOnlyContext(cmd.Context(), dbPath)
				if err != nil {
					return fmt.Errorf("opening store read-only: %w", err)
				}
				defer s.Close()

				schemaVersion, err := s.SchemaVersion()
				if err != nil {
					return fmt.Errorf("checking store schema read-only: %w", err)
				}
				if schemaVersion < store.StoreSchemaVersion {

					return fmt.Errorf("local store schema version %d requires migration to %d; this read-only command cannot migrate it; run the command that writes this store", schemaVersion, store.StoreSchemaVersion)

				}
				if schemaVersion > store.StoreSchemaVersion {
					return fmt.Errorf("local store schema version %d is newer than supported version %d; upgrade the CLI binary", schemaVersion, store.StoreSchemaVersion)
				}

				status, err = s.Status()
				if err != nil {
					return err
				}
			} else if !os.IsNotExist(err) {
				return fmt.Errorf("checking store path: %w", err)
			}

			if flags.asJSON {
				enc := json.NewEncoder(cmd.OutOrStdout())
				enc.SetIndent("", "  ")
				return enc.Encode(status)
			}

			if len(status) == 0 {
				fmt.Fprintln(cmd.OutOrStdout(), "No archived data. Add a site-specific sync command to populate the store.")
				return nil
			}

			fmt.Fprintln(cmd.OutOrStdout(), "Archive Status:")
			total := 0
			for resource, count := range status {
				fmt.Fprintf(cmd.OutOrStdout(), "  %-30s %d items\n", resource, count)
				total += count
			}
			fmt.Fprintf(cmd.OutOrStdout(), "\n  Total: %d items\n", total)
			fmt.Fprintf(cmd.OutOrStdout(), "  Store: %s\n", dbPath)
			return nil
		},
	}

	cmd.Flags().StringVar(&dbPath, "db", "", "SQLite database file path (default: resolved data directory data.db)")

	return cmd
}

// defaultDBPath is defined in helpers.go
