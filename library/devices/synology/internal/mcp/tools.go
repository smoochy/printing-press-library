// Copyright 2026 smoochy and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/base64"
	"encoding/json"
	"fmt"
	"math"
	"os"
	"path/filepath"
	"strconv"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"github.com/mvanhorn/printing-press-library/library/devices/synology/internal/cli"
	"github.com/mvanhorn/printing-press-library/library/devices/synology/internal/client"
	"github.com/mvanhorn/printing-press-library/library/devices/synology/internal/cliutil"
	"github.com/mvanhorn/printing-press-library/library/devices/synology/internal/config"
	"github.com/mvanhorn/printing-press-library/library/devices/synology/internal/mcp/bound"
	"github.com/mvanhorn/printing-press-library/library/devices/synology/internal/mcp/cobratree"
	"github.com/mvanhorn/printing-press-library/library/devices/synology/internal/store"
)

const (
	// MCP hosts can fan out tool calls faster than a human CLI session.
	// Keep them on the same polite-client limiter path instead of disabling
	// pacing with rate=0; users can still tune human CLI calls with --rate-limit.
	defaultMCPRateLimit = 2
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool("files_copy_start",
			mcplib.WithDescription("Start a background copy or move and return its task id. Required: path, dest_folder_path. Optional: overwrite, remove_src."),
			mcplib.WithString("path", mcplib.Required(), mcplib.Description("JSON array of absolute source paths")),
			mcplib.WithString("dest_folder_path", mcplib.Required(), mcplib.Description("Absolute destination folder path")),
			mcplib.WithBoolean("overwrite", mcplib.Description("Overwrite an existing destination entry")),
			mcplib.WithBoolean("remove_src", mcplib.Description("Remove the source after a successful copy, turning the copy into a move")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.CopyMove&method=start&version=3", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "path", WireName: "path", Location: "query"}, {PublicName: "dest_folder_path", WireName: "dest_folder_path", Location: "query"}, {PublicName: "overwrite", WireName: "overwrite", Location: "query"}, {PublicName: "remove_src", WireName: "remove_src", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_copy_status",
			mcplib.WithDescription("Report the progress of a running copy or move. Required: taskid."),
			mcplib.WithString("taskid", mcplib.Required(), mcplib.Description("Task id returned by copy_start")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.CopyMove&method=status&version=3", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "taskid", WireName: "taskid", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_copy_stop",
			mcplib.WithDescription("Stop a running copy or move. Required: taskid."),
			mcplib.WithString("taskid", mcplib.Required(), mcplib.Description("Task id returned by copy_start")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.CopyMove&method=stop&version=3", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "taskid", WireName: "taskid", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_delete_start",
			mcplib.WithDescription("Start a background delete and return its task id. Required: path. Optional: recursive."),
			mcplib.WithString("path", mcplib.Required(), mcplib.Description("JSON array of absolute paths to delete")),
			mcplib.WithBoolean("recursive", mcplib.Description("Delete folder contents as well as the folder itself")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.Delete&method=start&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "path", WireName: "path", Location: "query"}, {PublicName: "recursive", WireName: "recursive", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_delete_status",
			mcplib.WithDescription("Report the progress of a running delete. Required: taskid."),
			mcplib.WithString("taskid", mcplib.Required(), mcplib.Description("Task id returned by delete_start")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.Delete&method=status&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "taskid", WireName: "taskid", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_delete_stop",
			mcplib.WithDescription("Stop a running background delete task before it finishes. Required: taskid (from files_delete_start). Returns no body on success; use files_delete_stop only to abort, not to check progress - poll files_delete_status for that."),
			mcplib.WithString("taskid", mcplib.Required(), mcplib.Description("Task id returned by delete_start")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.Delete&method=stop&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "taskid", WireName: "taskid", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_download",
			mcplib.WithDescription("Download a file or folder. Required: path. Optional: mode."),
			mcplib.WithString("path", mcplib.Required(), mcplib.Description("JSON array of absolute paths to download")),
			mcplib.WithString("mode", mcplib.Description("Transfer mode, open to stream inline or download to attach")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.Download&method=download&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "path", WireName: "path", Location: "query"}, {PublicName: "mode", WireName: "mode", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_info",
			mcplib.WithDescription("Report File Station's server-side capabilities and limits: supported protocols, max upload size, and enabled features. No parameters. Call this once before scripting file operations to confirm a feature (like search or thumbnails) is actually available on this NAS."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.Info&method=get&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_list",
			mcplib.WithDescription("List the contents of one folder. Required: folder_path. Optional: additional, filetype, pattern (plus 4 more)."),
			mcplib.WithString("folder_path", mcplib.Required(), mcplib.Description("Absolute folder path, for example /volume1/media")),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields to include, for example ['real_path','size','time','type']")),
			mcplib.WithString("filetype", mcplib.Description("Restrict the listing to file, dir or all")),
			mcplib.WithString("pattern", mcplib.Description("Glob the entry name must match")),
			mcplib.WithString("sort_by", mcplib.Description("Field to sort by")),
			mcplib.WithString("sort_direction", mcplib.Description("Sort direction, asc or desc")),
			mcplib.WithNumber("offset", mcplib.Description("Index of the first entry to return")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of entries to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.List&method=list&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "folder_path", WireName: "folder_path", Location: "query"}, {PublicName: "additional", WireName: "additional", Location: "query"}, {PublicName: "filetype", WireName: "filetype", Location: "query"}, {PublicName: "pattern", WireName: "pattern", Location: "query"}, {PublicName: "sort_by", WireName: "sort_by", Location: "query"}, {PublicName: "sort_direction", WireName: "sort_direction", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_mkdir",
			mcplib.WithDescription("Create a folder. Required: folder_path, name. Optional: force_parent."),
			mcplib.WithString("folder_path", mcplib.Required(), mcplib.Description("JSON array of parent folder paths")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("JSON array of folder names to create")),
			mcplib.WithBoolean("force_parent", mcplib.Description("Create missing parent folders instead of failing")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.CreateFolder&method=create&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "folder_path", WireName: "folder_path", Location: "query"}, {PublicName: "name", WireName: "name", Location: "query"}, {PublicName: "force_parent", WireName: "force_parent", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_rename",
			mcplib.WithDescription("Rename a file or folder. Required: path, name."),
			mcplib.WithString("path", mcplib.Required(), mcplib.Description("JSON array of absolute paths to rename")),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("JSON array of new names")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.Rename&method=rename&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "path", WireName: "path", Location: "query"}, {PublicName: "name", WireName: "name", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_search_results",
			mcplib.WithDescription("Read the results of a running or finished search. Required: taskid. Optional: additional, offset, limit."),
			mcplib.WithString("taskid", mcplib.Required(), mcplib.Description("Task id returned by search_start")),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields to include")),
			mcplib.WithNumber("offset", mcplib.Description("Index of the first result to return")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of results to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.Search&method=list&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "taskid", WireName: "taskid", Location: "query"}, {PublicName: "additional", WireName: "additional", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_search_start",
			mcplib.WithDescription("Start a background search and return its task id. Required: folder_path. Optional: pattern, extension, recursive (plus 1 more)."),
			mcplib.WithString("folder_path", mcplib.Required(), mcplib.Description("Absolute folder path to search under")),
			mcplib.WithString("pattern", mcplib.Description("Glob the file name must match")),
			mcplib.WithString("extension", mcplib.Description("File extension filter, for example mkv")),
			mcplib.WithBoolean("recursive", mcplib.Description("Descend into subfolders")),
			mcplib.WithString("filetype", mcplib.Description("Restrict results to file, dir or all")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.Search&method=start&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "folder_path", WireName: "folder_path", Location: "query"}, {PublicName: "pattern", WireName: "pattern", Location: "query"}, {PublicName: "extension", WireName: "extension", Location: "query"}, {PublicName: "recursive", WireName: "recursive", Location: "query"}, {PublicName: "filetype", WireName: "filetype", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_search_stop",
			mcplib.WithDescription("Stop a running search and release its task. Required: taskid."),
			mcplib.WithString("taskid", mcplib.Required(), mcplib.Description("Task id returned by search_start")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.Search&method=stop&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "taskid", WireName: "taskid", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_shares",
			mcplib.WithDescription("List the shared folders File Station can browse. Optional: additional, sort_by, sort_direction (plus 2 more)."),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields to include, for example ['real_path','size','time','owner']")),
			mcplib.WithString("sort_by", mcplib.Description("Field to sort by, for example name or size")),
			mcplib.WithString("sort_direction", mcplib.Description("Sort direction, asc or desc")),
			mcplib.WithNumber("offset", mcplib.Description("Index of the first share to return")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of shares to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.List&method=list_share&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "additional", WireName: "additional", Location: "query"}, {PublicName: "sort_by", WireName: "sort_by", Location: "query"}, {PublicName: "sort_direction", WireName: "sort_direction", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("files_stat",
			mcplib.WithDescription("Show metadata for one file or folder. Required: path. Optional: additional."),
			mcplib.WithString("path", mcplib.Required(), mcplib.Description("JSON array of absolute paths, for example ['/volume1/media/movie.mkv']")),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields to include")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.FileStation.List&method=getinfo&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "path", WireName: "path", Location: "query"}, {PublicName: "additional", WireName: "additional", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("folder_get",
			mcplib.WithDescription("Show one shared folder in detail. Required: name. Optional: additional."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Share name")),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields to include")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.Share&method=get&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "query"}, {PublicName: "additional", WireName: "additional", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("folder_list",
			mcplib.WithDescription("List every shared folder on the NAS with name, path, and space usage. Optional: additional (extra fields like nfs_privilege or encryption), limit, offset for pagination. Use folder_list to discover folder names, then folder_get for one folder's full detail or folder_permissions for its ACL."),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields to include, for example ['nfs_privilege','encryption']")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of shares to return")),
			mcplib.WithNumber("offset", mcplib.Description("Index of the first share to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.Share&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "additional", WireName: "additional", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("folder_permissions",
			mcplib.WithDescription("List the accounts and groups that hold permissions on one shared folder. Required: name. Optional: user_group_type, limit, offset."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Share name")),
			mcplib.WithString("user_group_type", mcplib.Description("Which principal set to report, for example local_user or local_group")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of entries to return")),
			mcplib.WithNumber("offset", mcplib.Description("Index of the first entry to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.Share.Permission&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "query"}, {PublicName: "user_group_type", WireName: "user_group_type", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("group_list",
			mcplib.WithDescription("List local DSM user groups with name and description. Optional: additional (extra fields), limit, offset for pagination. Use group_list to find a group name, then group_members to see which accounts belong to it."),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields to include")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of groups to return")),
			mcplib.WithNumber("offset", mcplib.Description("Index of the first group to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.Group&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "additional", WireName: "additional", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("group_members",
			mcplib.WithDescription("List the accounts that belong to one group. Required: group. Optional: limit, offset."),
			mcplib.WithString("group", mcplib.Required(), mcplib.Description("Group name")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of members to return")),
			mcplib.WithNumber("offset", mcplib.Description("Index of the first member to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.Group.Member&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "group", WireName: "group", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("log_list",
			mcplib.WithDescription("Read the DSM system log with filtering by level, date range and keyword. Optional: logtype, level, keyword (plus 6 more)."),
			mcplib.WithString("logtype", mcplib.Description("Log category to read, for example system or connection")),
			mcplib.WithString("level", mcplib.Description("Minimum severity to include, for example err, warn or info")),
			mcplib.WithString("keyword", mcplib.Description("Substring the log message must contain")),
			mcplib.WithNumber("date_from", mcplib.Description("Unix timestamp of the earliest entry to include")),
			mcplib.WithNumber("date_to", mcplib.Description("Unix timestamp of the latest entry to include")),
			mcplib.WithString("target", mcplib.Description("Which log store to read")),
			mcplib.WithString("dir", mcplib.Description("Sort direction, ASC or DESC")),
			mcplib.WithNumber("start", mcplib.Description("Index of the first entry to return")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of entries to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.SyslogClient.Log&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "logtype", WireName: "logtype", Location: "query"}, {PublicName: "level", WireName: "level", Location: "query"}, {PublicName: "keyword", WireName: "keyword", Location: "query"}, {PublicName: "date_from", WireName: "date_from", Location: "query"}, {PublicName: "date_to", WireName: "date_to", Location: "query"}, {PublicName: "target", WireName: "target", Location: "query"}, {PublicName: "dir", WireName: "dir", Location: "query"}, {PublicName: "start", WireName: "start", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("nfs_status",
			mcplib.WithDescription("Show whether the NFS service is enabled and which protocol versions it offers."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.FileServ.NFS&method=get&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("package_list",
			mcplib.WithDescription("List installed packages with their version and running state. Optional: additional."),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields to include, for example ['status','version']")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.Package&method=list&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "additional", WireName: "additional", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("session_apis",
			mcplib.WithDescription("Enumerate every API namespace the NAS exposes, with its version range and CGI path. (public)"),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/query.cgi?api=SYNO.API.Info&method=query&version=1&query=all", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("session_login",
			mcplib.WithDescription("Log in to DSM and obtain a session id plus a SynoToken. Required: account, passwd. Optional: otp_code, device_id, device_name. (public)"),
			mcplib.WithString("account", mcplib.Required(), mcplib.Description("DSM account name")),
			mcplib.WithString("passwd", mcplib.Required(), mcplib.Description("DSM account password")),
			mcplib.WithString("otp_code", mcplib.Description("Six-digit one-time password, required on the first login of a two-factor account")),
			mcplib.WithString("device_id", mcplib.Description("Trusted-device token issued by a previous two-factor login, used instead of otp_code")),
			mcplib.WithString("device_name", mcplib.Description("Label DSM records for the trusted device")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.API.Auth&method=login&version=7&format=sid&enable_syno_token=yes", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "account", WireName: "account", Location: "query"}, {PublicName: "passwd", WireName: "passwd", Location: "query"}, {PublicName: "otp_code", WireName: "otp_code", Location: "query"}, {PublicName: "device_id", WireName: "device_id", Location: "query"}, {PublicName: "device_name", WireName: "device_name", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("session_logout",
			mcplib.WithDescription("Invalidate the current DSM session id, ending the authenticated session immediately. No parameters, no response body. Call this to explicitly end a session rather than letting it expire; a new session_login is required afterward for further calls."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.API.Auth&method=logout&version=6", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("storage_disks",
			mcplib.WithDescription("List physical disks with model, serial number, temperature and health status. Optional: limit, offset."),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of disks to return")),
			mcplib.WithNumber("offset", mcplib.Description("Index of the first disk to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.Storage.Disk&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("storage_esata",
			mcplib.WithDescription("List eSATA storage devices physically attached to the NAS, with device name and status. Optional: additional (extra fields). Returns an empty list on units with no eSATA port or nothing plugged in."),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields to include")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.ExternalDevice.Storage.eSATA&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "additional", WireName: "additional", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("storage_luns",
			mcplib.WithDescription("List iSCSI LUNs (block-storage targets) configured on this NAS, with name, size, and backing volume. No parameters. Returns an empty list on units with iSCSI Manager not configured."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.ISCSI.LUN&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("storage_overview",
			mcplib.WithDescription("Show the complete storage picture DSM Storage Manager renders, covering disks, pools and volumes in one call."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Storage.CGI.Storage&method=load_info&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("storage_pools",
			mcplib.WithDescription("List storage pools with their RAID level and member disks. Optional: limit, offset."),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of pools to return")),
			mcplib.WithNumber("offset", mcplib.Description("Index of the first pool to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.Storage.Pool&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("storage_smart",
			mcplib.WithDescription("Show the full SMART attribute table for one disk. Required: device."),
			mcplib.WithString("device", mcplib.Required(), mcplib.Description("Disk device name, for example sata1")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Storage.CGI.Smart&method=get_smart_info&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "device", WireName: "device", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("storage_smart_schedule",
			mcplib.WithDescription("List the configured SMART self-test schedule (test type and recurrence) applied to the NAS's physical disks. No parameters. Use storage_smart for a specific disk's actual SMART attribute results."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Storage.CGI.Smart.Scheduler&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("storage_usb",
			mcplib.WithDescription("List USB storage devices physically attached to the NAS, with device name and status. Optional: additional (extra fields like dev_type or status). Returns an empty list on units with nothing plugged into a USB port."),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields to include, for example ['dev_type','status']")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.ExternalDevice.Storage.USB&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "additional", WireName: "additional", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("storage_volumes",
			mcplib.WithDescription("List volumes with size, used space and status. Optional: limit, offset."),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of volumes to return")),
			mcplib.WithNumber("offset", mcplib.Description("Index of the first volume to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.Storage.Volume&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("system_dsm",
			mcplib.WithDescription("Show DSM product identity as the web UI reports it."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.DSM.Info&method=getinfo&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("system_health",
			mcplib.WithDescription("Show the aggregate system health verdict DSM shows on its dashboard."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.System.SystemHealth&method=get&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("system_info",
			mcplib.WithDescription("Show model, serial number, firmware version, uptime and temperature. Optional: type."),
			mcplib.WithString("type", mcplib.Description("Restrict the report to one section, for example storage")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.System&method=info&version=3", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "type", WireName: "type", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("system_network",
			mcplib.WithDescription("Show hostname, DNS, gateway and every network interface."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.Network&method=get&version=2", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("system_services",
			mcplib.WithDescription("List DSM services and whether each one is enabled. Optional: offset, limit."),
			mcplib.WithNumber("offset", mcplib.Description("Index of the first service to return")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of services to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.Service&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "offset", WireName: "offset", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("system_ups",
			mcplib.WithDescription("Show the state of an attached uninterruptible power supply."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.ExternalDevice.UPS&method=get&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("system_utilization",
			mcplib.WithDescription("Show current CPU, memory, disk and network utilization."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.System.Utilization&method=get&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("user_get",
			mcplib.WithDescription("Show one local user account in detail. Required: name. Optional: additional."),
			mcplib.WithString("name", mcplib.Required(), mcplib.Description("Account name")),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields to include")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.User&method=get&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "name", WireName: "name", Location: "query"}, {PublicName: "additional", WireName: "additional", Location: "query"}}, []string{}),
	)
	s.AddTool(
		mcplib.NewTool("user_list",
			mcplib.WithDescription("List local user accounts. Optional: type, additional, limit (plus 1 more)."),
			mcplib.WithString("type", mcplib.Description("Account source, for example local")),
			mcplib.WithString("additional", mcplib.Description("JSON array of extra fields to include, for example ['email','expired','description']")),
			mcplib.WithNumber("limit", mcplib.Description("Maximum number of accounts to return")),
			mcplib.WithNumber("offset", mcplib.Description("Index of the first account to return")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/webapi/entry.cgi?api=SYNO.Core.User&method=list&version=1", true, false, nil, mcpPageConfig{}, []mcpParamBinding{{PublicName: "type", WireName: "type", Location: "query"}, {PublicName: "additional", WireName: "additional", Location: "query"}, {PublicName: "limit", WireName: "limit", Location: "query"}, {PublicName: "offset", WireName: "offset", Location: "query"}}, []string{}),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Synced records live in resources(resource_type, id, data); filter by resource_type and use json_extract on data, e.g. SELECT json_extract(data,'$.name') FROM resources WHERE resource_type='files'.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

type mcpPageConfig struct {
	CursorParam    string
	NextCursorPath string
}

func formatMCPParamValue(v any) string {
	switch tv := v.(type) {
	case string:
		return tv
	case bool:
		return strconv.FormatBool(tv)
	case float64:
		if math.IsNaN(tv) || math.IsInf(tv, 0) {
			return strconv.FormatFloat(tv, 'f', -1, 64)
		}
		if math.Trunc(tv) == tv && math.Abs(tv) < 1e15 {
			return strconv.FormatInt(int64(tv), 10)
		}
		return strconv.FormatFloat(tv, 'f', -1, 64)
	case float32:
		f := float64(tv)
		if math.IsNaN(f) || math.IsInf(f, 0) {
			return strconv.FormatFloat(f, 'f', -1, 32)
		}
		if math.Trunc(f) == f && math.Abs(f) < 1e15 {
			return strconv.FormatInt(int64(f), 10)
		}
		return strconv.FormatFloat(f, 'f', -1, 32)
	default:
		// Composite values (a native []any / map[string]any from an array or
		// object param) reach this path when bound to a query or path slot;
		// JSON-encode them so the wire value is valid JSON rather than Go's
		// "[a b c]" / "map[...]" rendering. Body params never come through
		// here — they are stored natively in bodyArgs and marshalled there.
		if b, err := json.Marshal(v); err == nil {
			return string(b)
		}
		return fmt.Sprintf("%v", v)
	}
}

func mcpPathValue(v any) string {
	return cliutil.EscapePathParam(formatMCPParamValue(v))
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, readOnly bool, binaryResponse bool, headerOverrides map[string]string, pageConfig mcpPageConfig, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcpToolError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		mcpCursor := ""
		if pageConfig.CursorParam != "" {
			knownArgs["cursor"] = true
			if v, ok := args["cursor"]; ok {
				s, ok := v.(string)
				if !ok {
					return mcpToolError("cursor must be an opaque string returned by a previous MCP response"), nil
				}
				mcpCursor = s
				upstreamCursor, err := bound.UpstreamCursor(s)
				if err != nil {
					return mcpToolError(err.Error()), nil
				}
				if upstreamCursor != "" {
					params[pageConfig.CursorParam] = upstreamCursor
				}
			}
		}
		var headers map[string]string
		if len(headerOverrides) > 0 {
			headers = make(map[string]string, len(headerOverrides)+1)
			for k, v := range headerOverrides {
				headers[k] = v
			}
		}
		if binaryResponse {
			if headers == nil {
				headers = map[string]string{}
			}
			headers[client.BinaryResponseHeader] = "true"
		}
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, mcpPathValue(v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = formatMCPParamValue(v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, mcpPathValue(v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = formatMCPParamValue(v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			if len(headers) > 0 {
				data, err = c.GetWithHeaders(ctx, path, params, headers)
				break
			}
			data, err = c.Get(ctx, path, params)
		case "POST":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PostQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PostWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PostQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PostWithParams(ctx, path, params, bodyArgs)
			}
		case "PUT":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PutQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PutWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PutQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PutWithParams(ctx, path, params, bodyArgs)
			}
		case "PATCH":
			if len(headers) > 0 {
				if readOnly {
					data, _, err = c.PatchQueryWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				} else {
					data, _, err = c.PatchWithParamsAndHeaders(ctx, path, params, bodyArgs, headers)
				}
				break
			}
			if readOnly {
				data, _, err = c.PatchQueryWithParams(ctx, path, params, bodyArgs)
			} else {
				data, _, err = c.PatchWithParams(ctx, path, params, bodyArgs)
			}
		case "DELETE":
			if len(headers) > 0 {
				data, _, err = c.DeleteWithParamsAndHeaders(ctx, path, params, headers)
				break
			}
			data, _, err = c.DeleteWithParams(ctx, path, params)
		default:
			return mcpToolError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcpToolError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Run 'synology-pp-cli auth setup' for credential setup steps." +
					"\n      Run 'synology-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcpToolError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API credentials." +
					"\n      Run 'synology-pp-cli auth setup' for credential setup steps." +
					"\n      Run 'synology-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcpToolError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource. Check that they have the required permissions and match the API's expected auth scheme." +
					"\n      Run 'synology-pp-cli auth setup' for credential setup steps." +
					"\n      Run 'synology-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcpToolError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcpToolError("rate limited: " + msg), nil
			default:
				return mcpToolError(msg), nil
			}
		}

		if binaryResponse {
			encoded := base64.StdEncoding.EncodeToString(data)
			out, err := json.Marshal(map[string]any{
				"content_encoding": "base64",
				"data_base64":      encoded,
				"byte_count":       len(data),
			})
			if err != nil {
				return mcpToolError(fmt.Sprintf("encoding binary result: %v", err)), nil
			}
			if len(out) > bound.MaxBytes {
				return mcpToolError(fmt.Sprintf("binary response is too large for MCP text output: %d response bytes encode to %d base64 bytes and %d MCP result bytes, exceeding the %d byte budget. Use the companion CLI command with --output <file> to save the payload locally.", len(data), len(encoded), len(out), bound.MaxBytes)), nil
			}
			return mcplib.NewToolResultText(string(out)), nil
		}
		if pageConfig.CursorParam != "" {
			return mcpToolPageResultText(method, data, pageConfig, mcpCursor), nil
		}
		return mcpToolResultText(method, data), nil
	}
}

func mcpToolResultText(method string, data json.RawMessage) *mcplib.CallToolResult {
	return mcplib.NewToolResultText(bound.EndpointResponse(method, data))
}

// mcpToolError keeps provider-controlled typed endpoint errors within the MCP
// text-result budget just like successful endpoint results.
func mcpToolError(message string) *mcplib.CallToolResult {
	return mcplib.NewToolResultError(bound.Text(message))
}

func mcpToolPageResultText(method string, data json.RawMessage, pageConfig mcpPageConfig, cursor string) *mcplib.CallToolResult {
	return mcplib.NewToolResultText(bound.EndpointPageResponse(method, data, bound.PageOptions{
		Cursor:         cursor,
		CursorParam:    pageConfig.CursorParam,
		NextCursorPath: pageConfig.NextCursorPath,
	}))
}

func newMCPClient() (*client.Client, error) {
	cfg, err := newMCPConfig()
	if err != nil {
		return nil, err
	}
	return newMCPClientFromConfig(cfg), nil
}

func newMCPConfig() (*config.Config, error) {
	cfg, err := config.Load("")
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	return cfg, nil
}

func newMCPClientFromConfig(cfg *config.Config) *client.Client {
	c := client.New(cfg, 60*time.Second, defaultMCPRateLimit)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c
}

func mcpDBPath() (string, error) {
	dir, err := cliutil.DataDir()
	if err != nil {
		return "", err
	}
	return filepath.Join(dir, "data.db"), nil
}

type mcpStoreStatusKind string

const (
	mcpStoreStatusEmpty mcpStoreStatusKind = "empty"
	mcpStoreStatusReady mcpStoreStatusKind = "ready"
)

func openMCPReadOnlyStore(path string) (*store.Store, *mcplib.CallToolResult) {
	if _, err := os.Stat(path); err != nil {
		if os.IsNotExist(err) {
			return nil, mcplib.NewToolResultError(mcpMissingStoreMessage(path))
		}
		return nil, mcplib.NewToolResultError(fmt.Sprintf("checking local data store %s: %v", path, err))
	}
	db, err := store.OpenReadOnly(path)
	if err != nil {
		return nil, mcplib.NewToolResultError(fmt.Sprintf("opening local data store %s: %v. Run synology-pp-cli sync to refresh the store, or use live endpoint MCP tools for unsynced data.", path, err))
	}
	return db, nil
}

func mcpMissingStoreMessage(path string) string {
	return fmt.Sprintf("No local data store found at %s. Run synology-pp-cli sync before using MCP search/sql, or use live endpoint MCP tools for unsynced data.", path)
}

func mcpStoreStatus(db *store.Store) (mcpStoreStatusKind, error) {
	status, err := db.Status()
	if err != nil {
		return "", err
	}
	if len(status) == 0 {
		return mcpStoreStatusEmpty, nil
	}
	return mcpStoreStatusReady, nil
}

func mcpEmptyStoreNextStep() string {
	return "Run synology-pp-cli sync to populate the local SQLite store before using MCP search/sql."
}

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	path, err := mcpDBPath()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("resolving database: %v", err)), nil
	}
	db, toolErr := openMCPReadOnlyStore(path)
	if toolErr != nil {
		return toolErr, nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}
	storeStatus, err := mcpStoreStatus(db)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading store status: %v", err)), nil
	}

	return toolResultJSON(mcpSearchEnvelope(results, storeStatus))
}

func mcpSearchEnvelope(results []json.RawMessage, storeStatus mcpStoreStatusKind) map[string]any {
	if results == nil {
		results = []json.RawMessage{}
	}
	out := map[string]any{
		"count":        len(results),
		"results":      results,
		"store_status": storeStatus,
		"resumable":    false,
	}
	if len(results) == 0 {
		if storeStatus == mcpStoreStatusEmpty {
			out["next_step"] = mcpEmptyStoreNextStep()
		} else {
			out["next_step"] = "No local search matches. Try a broader query, a lower-specificity FTS expression, or sync again if data may be stale."
		}
	}
	return out
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate rejects multi-statement input, then applies an allowlist (SELECT or
// WITH only) AFTER stripping the leading whitespace, line comments, block
// comments, and semicolons that SQLite itself ignores before parsing. A naive
// HasPrefix check on a keyword blocklist is bypassable by prefixing the
// dangerous statement with "/* x */" or "-- x\n"; a naive leading-keyword
// allowlist is bypassable by appending "; ATTACH DATABASE ...". Combined with
// the empirical fact that modernc.org/sqlite's mode=ro does NOT block VACUUM
// INTO (writes a snapshot to a new file) or ATTACH DATABASE (opens a separate
// writable handle), either bypass produces silent exfiltration to an
// attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	stripped := stripLeadingSQLNoise(query)
	if hasTrailingSQLStatement(stripped) {
		return fmt.Errorf("only a single SELECT or WITH statement is allowed")
	}
	upper := strings.ToUpper(stripped)
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

// hasTrailingSQLStatement reports whether query contains a statement
// terminator followed by more executable SQL. A trailing semicolon is allowed;
// a second statement is not. Semicolons inside string literals, quoted
// identifiers, bracket identifiers, and comments are ignored to match SQLite's
// parser shape closely enough for this security gate.
func hasTrailingSQLStatement(query string) bool {
	inSingle := false
	inDouble := false
	inBacktick := false
	inBracket := false
	inLineComment := false
	inBlockComment := false

	for i := 0; i < len(query); i++ {
		ch := query[i]
		next := byte(0)
		if i+1 < len(query) {
			next = query[i+1]
		}

		switch {
		case inLineComment:
			if ch == '\n' {
				inLineComment = false
			}
			continue
		case inBlockComment:
			if ch == '*' && next == '/' {
				inBlockComment = false
				i++
			}
			continue
		case inSingle:
			if ch == '\'' {
				if next == '\'' {
					i++
					continue
				}
				inSingle = false
			}
			continue
		case inDouble:
			if ch == '"' {
				if next == '"' {
					i++
					continue
				}
				inDouble = false
			}
			continue
		case inBacktick:
			if ch == '`' {
				if next == '`' {
					i++
					continue
				}
				inBacktick = false
			}
			continue
		case inBracket:
			if ch == ']' {
				inBracket = false
			}
			continue
		}

		switch {
		case ch == '-' && next == '-':
			inLineComment = true
			i++
		case ch == '/' && next == '*':
			inBlockComment = true
			i++
		case ch == '\'':
			inSingle = true
		case ch == '"':
			inDouble = true
		case ch == '`':
			inBacktick = true
		case ch == '[':
			inBracket = true
		case ch == ';':
			if stripLeadingSQLNoise(query[i+1:]) != "" {
				return true
			}
			return false
		}
	}
	return false
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	path, err := mcpDBPath()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("resolving database: %v", err)), nil
	}
	db, toolErr := openMCPReadOnlyStore(path)
	if toolErr != nil {
		return toolErr, nil
	}
	defer db.Close()

	rows, err := db.DB().QueryContext(ctx, query)
	if err != nil {
		return mcplib.NewToolResultError(mcpSQLQueryError(err)), nil
	}
	defer rows.Close()

	cols, err := rows.Columns()
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading columns: %v", err)), nil
	}
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		if err := rows.Scan(ptrs...); err != nil {
			return mcplib.NewToolResultError(fmt.Sprintf("scanning row: %v", err)), nil
		}
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}
	// rows.Next() stops on a mid-iteration error without failing the loop, so
	// skipping rows.Err() would return a truncated result set as success.
	if err := rows.Err(); err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading rows: %v", err)), nil
	}
	storeStatus, err := mcpStoreStatus(db)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("reading store status: %v", err)), nil
	}

	return toolResultJSON(mcpSQLEnvelope(results, cols, storeStatus))
}

func mcpSQLEnvelope(rows []map[string]any, columns []string, storeStatus mcpStoreStatusKind) map[string]any {
	if rows == nil {
		rows = []map[string]any{}
	}
	out := map[string]any{
		"count":        len(rows),
		"columns":      columns,
		"rows":         rows,
		"store_status": storeStatus,
		"resumable":    false,
	}
	if len(rows) == 0 {
		if storeStatus == mcpStoreStatusEmpty {
			out["next_step"] = mcpEmptyStoreNextStep()
		} else {
			out["next_step"] = "The read-only SQL query returned no rows. Check resource_type filters, json_extract paths, or run sync again if data may be stale."
		}
	}
	return out
}

func mcpSQLQueryError(err error) string {
	msg := err.Error()
	if strings.Contains(strings.ToLower(msg), "no such table") {
		return fmt.Sprintf("query failed: %v. Synced records live in resources(resource_type, id, data), not one SQL table per resource. Filter by resource_type, for example resource_type='files', and read JSON fields with json_extract(data,'$.field').", err)
	}
	return fmt.Sprintf("query failed: %v", err)
}

// toolResultJSON renders v as the indented JSON body of an MCP text result,
// surfacing a marshal failure as a tool error instead of empty content.
func toolResultJSON(v any) (*mcplib.CallToolResult, error) {
	text, err := bound.JSON(v)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("encoding result: %v", err)), nil
	}
	return mcplib.NewToolResultText(text), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	paths := map[string]string{}
	if dir, err := cliutil.ConfigDir(); err == nil {
		paths["config_dir"] = dir
	}
	if dir, err := cliutil.DataDir(); err == nil {
		paths["data_dir"] = dir
	}
	if dir, err := cliutil.StateDir(); err == nil {
		paths["state_dir"] = dir
	}
	if dir, err := cliutil.CacheDir(); err == nil {
		paths["cache_dir"] = dir
	}
	ctx := map[string]any{
		"api":         "synology",
		"description": "Query and operate Synology DSM NAS units - system health, storage, SMART, shares, users",
		"archetype":   "developer-platform",
		"tool_count":  46,
		"paths":       paths,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion synology-pp-cli binary.",
		"auth": map[string]any{
			"type": "session_handshake",
		},
		"resources": []map[string]any{
			{
				"name":        "files",
				"description": "File Station browsing, search and file operations",
				"endpoints":   []string{"copy_start", "copy_status", "copy_stop", "delete_start", "delete_status", "delete_stop", "download", "info", "list", "mkdir", "rename", "search_results", "search_start", "search_stop", "shares", "stat"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "folder",
				"description": "Shared folders and their permissions",
				"endpoints":   []string{"get", "list", "permissions"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "group",
				"description": "Local groups",
				"endpoints":   []string{"list", "members"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "log",
				"description": "DSM system log",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "nfs",
				"description": "NFS service state",
				"endpoints":   []string{"status"},
			},
			{
				"name":        "package",
				"description": "Installed DSM packages",
				"endpoints":   []string{"list"},
				"syncable":    true,
				"searchable":  true,
			},
			{
				"name":        "session",
				"description": "DSM session handshake",
				"endpoints":   []string{"apis", "login", "logout"},
				"searchable":  true,
			},
			{
				"name":        "storage",
				"description": "Volumes, pools, disks and SMART data",
				"endpoints":   []string{"disks", "esata", "luns", "overview", "pools", "smart", "smart_schedule", "usb", "volumes"},
				"searchable":  true,
			},
			{
				"name":        "system",
				"description": "DSM system identity, health and load",
				"endpoints":   []string{"dsm", "health", "info", "network", "services", "ups", "utilization"},
				"searchable":  true,
			},
			{
				"name":        "user",
				"description": "Local user accounts",
				"endpoints":   []string{"get", "groups", "list"},
				"syncable":    true,
				"searchable":  true,
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass offset parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
		// Command-mirror capabilities are exposed through MCP by shelling out
		// to the companion CLI binary.
		"command_mirror_capabilities": []map[string]string{
			{"name": "API namespace inventory", "command": "session apis", "description": "List every API namespace this particular NAS exposes, with its version range and CGI path.", "rationale": "DSM's API surface depends on which packages are installed, so it differs per NAS and is not documented anywhere; no competing CLI or MCP server enumerates it.", "via": "mcp-command-mirror"},
			{"name": "SMART attribute table", "command": "storage smart", "description": "Read a drive's raw SMART attribute table; 'storage smart-schedule' reads the scheduled SMART test configuration.", "rationale": "The DSM web UI shows SMART in a dialog and no competing CLI or library exposes it at all; the schedule is only reachable through a namespace the web UI itself calls.", "via": "mcp-command-mirror"},
		},
		"playbook": []map[string]string{
			{"topic": "API namespace inventory", "insight": ""},
			{"topic": "SMART attribute table", "insight": ""},
			{"topic": "Resource discovery", "insight": "Use list commands to discover available resources before attempting operations. Developer platform APIs often have nested resource hierarchies."},
		},
	}
	return toolResultJSON(ctx)
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
