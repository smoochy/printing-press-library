// Copyright 2026 Paul Gradeff and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"io"
	"testing"
)

// TestNovelLocationsHelpWires smoke-tests that the locations command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelLocationsHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"locations", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("locations --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelLocationsBehavior is the placeholder for table-driven tests of
// the locations command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelLocationsBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
func TestNovelLocationsBehavior(t *testing.T) {
	t.Parallel()
	want := map[string]string{
		"Delta": "84624", "Fillmore": "84631", "Hinckley": "84635",
		"Oak City": "84649", "Holden": "84636", "Scipio": "84656",
		"Kanosh": "84637", "Meadow": "84644", "Lynndyl": "84640",
		"Leamington": "84638",
	}
	if len(millardLocations) != len(want) {
		t.Fatalf("len(millardLocations) = %d, want %d", len(millardLocations), len(want))
	}
	gotCities := millardCityNames()
	if len(gotCities) != len(want) {
		t.Fatalf("millardCityNames len = %d, want %d", len(gotCities), len(want))
	}
	seen := map[string]bool{}
	for _, loc := range millardLocations {
		zip, ok := want[loc.City]
		if !ok {
			t.Errorf("unexpected city %q", loc.City)
			continue
		}
		if loc.Zip != zip {
			t.Errorf("%s zip = %q, want %q", loc.City, loc.Zip, zip)
		}
		seen[loc.City] = true
	}
	if len(seen) != len(want) {
		t.Fatalf("unique cities = %d, want %d", len(seen), len(want))
	}
}
