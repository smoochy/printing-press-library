// Copyright 2026 Paul Gradeff and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"io"
	"testing"
)

// TestNovelMillardHelpWires smoke-tests that the millard command
// resolves at runtime and renders --help without error. Catches wiring
// regressions (missing AddCommand, panicking RunE on --help, etc.) before
// review. Always runs — do not delete this test when filling in real cases.
func TestNovelMillardHelpWires(t *testing.T) {
	cmd := RootCmd()
	cmd.SetArgs([]string{"millard", "--help"})
	cmd.SetOut(io.Discard)
	cmd.SetErr(io.Discard)
	if err := cmd.Execute(); err != nil {
		t.Fatalf("millard --help error = %v (novel command not wired correctly?)", err)
	}
}

// TestNovelMillardBehavior is the placeholder for table-driven tests of
// the millard command's actual behavior. Replace the t.Skip with
// real cases — reviewers will flag a shipped t.Skip.
//
// Suggested shape:
//
//	func TestNovelMillardBehavior(t *testing.T) {
//	    cases := []struct {
//	        name  string
//	        input ...
//	        want  ...
//	    }{
//	        // {name: "...", input: ..., want: ...},
//	    }
//	    for _, tc := range cases {
//	        tc := tc
//	        t.Run(tc.name, func(t *testing.T) {
//	            t.Parallel()
//	            // assertions here
//	        })
//	    }
//	}
func TestNovelMillardBehavior(t *testing.T) {
	t.Parallel()
	notices := []pmnNotice{
		{NoticeID: 1, PublicBodyName: "Delta City Council", EntityName: "Delta"},
		{NoticeID: 2, PublicBodyName: "Millard County Planning Commission", EntityName: "Fillmore"},
		{NoticeID: 3, PublicBodyName: "Millard Board of Education", EntityName: "Fillmore"},
		{NoticeID: 4, PublicBodyName: "Library Board", EntityName: "Delta"},
	}
	var kept []int64
	for _, n := range notices {
		if bodyLooksLandUse(n) {
			kept = append(kept, n.NoticeID)
		}
	}
	if len(kept) != 2 || kept[0] != 1 || kept[1] != 2 {
		t.Fatalf("land-use bodies = %v, want [1 2]", kept)
	}
}
