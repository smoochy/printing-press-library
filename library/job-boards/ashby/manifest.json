{
  "manifest_version": "0.3",
  "name": "ashby-pp-mcp",
  "display_name": "Ashby",
  "version": "4.31.1",
  "description": "Unauthenticated, read-only access to currently published postings on a named Ashby job board.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/ashby-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/ashby-pp-mcp",
      "args": [],
      "env": {
        "PRINTING_PRESS_CLIENT_PROFILE": "${user_config.printing_press_client_profile}"
      }
    }
  },
  "user_config": {
    "printing_press_client_profile": {
      "type": "string",
      "title": "Client profile",
      "description": "Binds the MCP server to an existing tenant-gated Printing Press client profile.",
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
