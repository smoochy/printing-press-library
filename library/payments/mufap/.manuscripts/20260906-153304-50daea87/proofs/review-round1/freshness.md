# Review findings for internal/cli/freshness.go

[BLOCKER] Fund identity is silently empty on 4 of the 5 selectable tabs. $HOME/printing-press/.runstate/personal-74f0f171/runs/20260906-153304-50daea87/working/mufap-pp-cli/internal/cli/freshness.go:136 resolves the name column with freshnessColumn("Fund Name"). The resolver (lines 122-134) does exact-match first, then a containment test in the WRONG direction: strings.Contains(lower(header), lower(want)) — line 129. discovery/endpoint-contract.md section H measured that the name column is "Fund Name" ONLY on tab=returns and plain "Fund" on nav, pricing, payout and ter. Header "Fund" is not equal to "Fund Name" and does not CONTAIN "fund name", so fundCol == "" for those tabs. Nothing guards that (only validityCol is checked, line 138), so line 159 evaluates r[""] on a map[string]string and yields "" with no error: every emitted row gets "fund": "" in JSON and a blank FUND column in the table (line 288). The command's own Example advertises the broken path — line 71: `mufap-pp-cli freshness --tab nav --min-lag-days 7 --json`. The house fix already exists in the same package: backfill.go:216-228 (backfillRowKey) probes []string{"Fund Name", "Fund"} in order and its comment states exactly this tab difference. Same latent hole applies to categoryCol (line 137) — an absent Category header also degrades to r[""] rather than an error.

[MAJOR] `--tab payout` is offered but can never succeed, and its failure is misdiagnosed as an upstream page change. freshness.go:299 advertises payout in the --tab help string and the validation loop at lines 85-94 accepts it (MUFAPDailyTabNames includes "payout"), but discovery/endpoint-contract.md section H records that tab=payout has NO "Validity Date" column at all — its date column is "Payout Date" — and explicitly warns that any code normalizing the row's own validity date must special-case this tab. So validityCol resolves to "" (line 135) and line 139 returns `tab payout carries no Validity Date column (headers: ...): page shape changed`, blaming a site change for a permanent, already-measured column difference; it is also an untyped error, not a usageErr, so it exits as an API/runtime failure rather than bad input. If the undated payout view happens to return zero rows the guard at line 138 is skipped entirely and the human branch instead prints "MUFAP returned an empty table, which it also does on weekends and holidays" (line 264) — a second wrong explanation. Either reject payout at flag-validation time with usageErr, or resolve its date column to "Payout Date".

[MINOR] total_funds is a row count, not a fund count, and is presented as universe width. freshness.go:147/158/231 sets TotalFunds = len(obs) = number of parsed rows carrying a date, rendered as "funds:" at line 269. discovery/endpoint-contract.md section G measured that fund rows are not unique per date (49 of 388 name collisions on tab=returns, plus one genuine upstream duplicate row), and section H warns that tab=pricing and tab=ter return 551 rows of current reference data that must NOT be read as a universe width. On those tabs the number printed next to "funds:" overstates the universe. Consider labelling it rows_with_validity_date, or adding a distinct-fund count alongside it.

[MINOR] Positional arguments are silently ignored, unlike every sibling novel command. freshness.go:73-76 uses the house help branch, but no equivalent of coverage.go:125-128 (`if len(args) > 0 { _ = cmd.Usage(); return usageErr(...) }`) follows the dryRunOK block. Because a non-empty args slice also suppresses the help branch, `mufap-pp-cli freshness junk` skips help, ignores "junk", and runs a live fetch with all-default flags — a typo'd flag such as `freshness -limit 5` (parsed by pflag as shorthand, or landing in args) never gets diagnosed.
