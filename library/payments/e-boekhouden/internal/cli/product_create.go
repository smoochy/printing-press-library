// Copyright 2026 markvandeven and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newProductCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyActive bool
	var bodyCode string
	var bodyCostCenterId string
	var bodyDescription string
	var bodyGroupId string
	var bodyInStockManagement bool
	var bodyLedgerId string
	var bodyPriceExcl float64
	var bodyPriceIncl float64
	var bodyPurchasePriceExcl float64
	var bodyUnitId string
	var bodyVat string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create a new product.",
		Example:     "  e-boekhouden-pp-cli product create --code example-value",
		Annotations: map[string]string{"pp:endpoint": "product.create", "pp:method": "POST", "pp:path": "/v1/product"},
		RunE: func(cmd *cobra.Command, args []string) error {
			// Bare invocation of a command with required input prints help
			// instead of pflag's terse "required flag not set" error. Optional-
			// only read commands fall through so a bare call still executes.
			if cmd.Flags().NFlag() == 0 && len(args) == 0 && !flags.dryRun {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("code") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "code")
				}
				if !cmd.Flags().Changed("description") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "description")
				}
				if !cmd.Flags().Changed("ledger-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "ledger-id")
				}
				if !cmd.Flags().Changed("vat") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "vat")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/v1/product"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if cmd.Flags().Changed("active") {
					body["active"] = bodyActive
				}
				if bodyCode != "" {
					body["code"] = bodyCode
				}
				if bodyCostCenterId != "" {
					body["costCenterId"] = bodyCostCenterId
				}
				if bodyDescription != "" {
					body["description"] = bodyDescription
				}
				if bodyGroupId != "" {
					body["groupId"] = bodyGroupId
				}
				if cmd.Flags().Changed("in-stock-management") {
					body["inStockManagement"] = bodyInStockManagement
				}
				if bodyLedgerId != "" {
					body["ledgerId"] = bodyLedgerId
				}
				if bodyPriceExcl != 0.0 {
					body["priceExcl"] = bodyPriceExcl
				}
				if bodyPriceIncl != 0.0 {
					body["priceIncl"] = bodyPriceIncl
				}
				if bodyPurchasePriceExcl != 0.0 {
					body["purchasePriceExcl"] = bodyPurchasePriceExcl
				}
				if bodyUnitId != "" {
					body["unitId"] = bodyUnitId
				}
				if bodyVat != "" {
					body["vat"] = bodyVat
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "product", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "product", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "product", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "product", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "product", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "product",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "product", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "product", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().BoolVar(&bodyActive, "active", true, "Whether the product is active.")
	cmd.Flags().StringVar(&bodyCode, "code", "", "The code of the product. Error codes ART_004 Product Code is missing. ART_007 Product Code is too long.")
	cmd.Flags().StringVar(&bodyCostCenterId, "cost-center-id", "", "The cost center ID of the product. Error codes ART_010 Product Cost center is inactive.")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "The description of the product. Error codes ART_005 Product Description is missing.")
	cmd.Flags().StringVar(&bodyGroupId, "group-id", "", "The group ID of the product. Error codes ART_012 Product Group could not be found.")
	cmd.Flags().BoolVar(&bodyInStockManagement, "in-stock-management", false, "Whether the product will be Included in automatic inventory management.")
	cmd.Flags().StringVar(&bodyLedgerId, "ledger-id", "", "The ledger ID of the product. Error codes ART_006 Product Ledger ID is missing.")
	cmd.Flags().Float64Var(&bodyPriceExcl, "price-excl", 0.0, "The price (excluding VAT) of the product. This field can not be combined with `priceIncl`.")
	cmd.Flags().Float64Var(&bodyPriceIncl, "price-incl", 0.0, "The price (including VAT) of the product. This field can not be combined with `priceExcl`.")
	cmd.Flags().Float64Var(&bodyPurchasePriceExcl, "purchase-price-excl", 0.0, "The purchase price (excluding VAT) of the product.")
	cmd.Flags().StringVar(&bodyUnitId, "unit-id", "", "The unit ID of the product. Error codes ART_011 Product Unit could not be found.")
	cmd.Flags().StringVar(&bodyVat, "vat", "", "The VAT code of the product. Only supports VAT codes of type 'Sale'.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
