# Shared review brief — splitwise reprint (2026-09-04)

## What is under review
A REPRINT of the published `splitwise-pp-cli` (Splitwise REST API v3.0) under cli-printing-press 4.31.7.
Repo (scratch git, review the full range): `/Users/vinnypasceri/printing-press/.runstate/printing-press-workshop-e51dd43c/runs/20260904-131606-b6e44bce/working/splitwise-pp-cli`
- `5be959c` = fresh generator output (baseline; generated files carry `// Generated by CLI Printing Press … DO NOT EDIT`).
- Hand-authored diff to review: `git diff 5be959c..HEAD` — concentrated in `internal/cli/`: 16 ported novel commands (activity, audit, balances(+--by-group), debts, fairness, fairness nudge, forecast, ledger(+--friend), net, normalize, recurring, report, settle-up, split, spend, resolve), 2 new commands (`brief`, `reconcile`), shared hand files (`splitwise_data.go`, `splitwise_output.go`, `splitwise_novel.go`, `splitwise_compat.go`, `splitwise_balances_bygroup.go`) and their tests. Any later commits are shipcheck/review fixes.
- Prior implementation these were ported FROM (read-only reference, for "did the port preserve behavior"): `/Users/vinnypasceri/printing-press/library/splitwise/internal/cli/splitwise_*.go` (+ that tree's patched `helpers.go`).

## What it must do (the REAL contracts — read these, not the intent)
- Generated helper contracts: `internal/cli/helpers.go` (`dryRunOK`, `writeDryRun`, `printJSONFiltered`, `wantsHumanTable`, `usageErr` → exit 2, `boundCtx`, `defaultDBPath`), `internal/cli/sync_hint.go` (`hintIfUnsynced`/`hintIfStale`), `internal/cli/root.go` (`registerNovelCommand`, `addNovelCommandIfAbsent`, root flags incl. `--data-source`, `--max-age`, `--dry-run`, output flags), `internal/store/store.go` (resources table: `resource_type, id, data`; `List`, `OpenWithContext`; WAL single-writer), `internal/client/client.go` (Get/Post, auth header, timeout, rate limiting), `internal/cliutil/` (`IsAnyHarness`, `IsDogfoodEnv`, `ParseDurationLoose`).
- Local store resource types in THIS tree: `get-groups`, `get-friends`, `get-expenses`, `get-current-user`, `get-notifications`, `get-categories`, `get-currencies`.
- Splitwise API truths (from the prior run's patches, `~/printing-press/library/splitwise/.printing-press-patches/*.json`): offset pagination with NO has_more/cursor (advance until a short page); large integer IDs must never be rendered in scientific notation; multi-word names must be rejoined from positionals; ambiguous name matches must ERROR, never silently pick (load-bearing for `settle-up --record` and `fairness nudge --send`, which WRITE to the user's real Splitwise account); `create_expense` shares must sum exactly to the cost; `payment:true` expenses are settlements.
- Skill rules the code must satisfy: `/Users/vinnypasceri/.claude/plugins/cache/cli-printing-press/cli-printing-press/4.31.7/skills/printing-press/SKILL.md` §"Agent Build Checklist" (13 principles: non-interactive, `--json` valid + `--select`, verify-friendly RunE — no `cobra.MinimumNArgs`/`MarkFlagRequired`, dry-run before any IO, `writeDryRun` envelope, missing-mirror guard, empty lists as `[]`, `pp:data-source` directive, `mcp:read-only` only on true read-only commands, side-effect commands print-by-default + refuse under `cliutil.IsAnyHarness()`, scan caps for scan-and-filter, sql.Null* scans, drain-first SQLite, no `store.Upsert` inside an open write tx).
- The approved feature manifest (scope of record): `/Users/vinnypasceri/printing-press/.runstate/printing-press-workshop-e51dd43c/runs/20260904-131606-b6e44bce/research/2026-09-03-215539-feat-splitwise-pp-cli-absorb-manifest.md` (Transcendence table incl. the Long-description scope redirects) and `…/research.json` (`novel_features`).
- Data sensitivity: the local store and every output carry PERSONAL FINANCIAL DATA — friend names, amounts, who owes whom, expense descriptions, comment text. The MCP server exposes these commands as tools. Nothing may log or emit the API key; nothing should print more PII than the command's purpose needs; tests must use synthetic fixtures (no real names/ids).

## How to verify a finding
Quote the authoritative line that establishes the rule the finding depends on (file:line in the tree above). A finding whose cited line exists but whose rule is inverted is wrong. Where a plan/manifest contradicts the code, say so as a claim-vs-reality correction. Classify Blocker / Major / Minor / Nit with a concrete failure scenario and a concrete fix. Say explicitly where something is correct that a shallow review would flag.

Build/test: `cd <repo> && go build ./... && go vet ./... && go test -count=1 ./internal/cli/`. Binary: `./splitwise-pp-cli` (rebuild with `go build -o ./splitwise-pp-cli ./cmd/splitwise-pp-cli`). A real synced store exists at `~/.local/share/splitwise-pp-cli/data.db` — READ-ONLY use, never run `--record`, `--send`, `sync`, or any mutating endpoint against it or the live API.

## What is DIFFERENT this round (2026-09-04, second reprint: 4.31.6 → 4.31.7)
The hand-authored files were ported by name from the 4.31.6 reprint that was already peer-reviewed three rounds yesterday (dispositions: /Users/vinnypasceri/Projects/printing-press-workshop/docs/agent-reviews/2026-09-04-splitwise-reprint-peer-reviews.md — read it so you do not re-raise closed findings; re-raise only if the fix regressed). Focus on TWO things:
1. **The hand delta since yesterday** (small): `/Users/vinnypasceri/printing-press/.runstate/printing-press-workshop-e51dd43c/runs/20260904-131606-b6e44bce/pipeline/hand-delta-vs-4.31.6.diff` — emitStructured keeps a JSON fallback under --csv for multi-array envelopes (the 4.31.7 printCSV now flattens any object into one row), and pp:happy-args verifier fixtures on fairness nudge / ledger / settle-up / split (+ one pp:data-source directive removed in split.go). Commits 3f92465, bfce052, b81bb3b on top of baseline 5be959c.
2. **The generator framework delta 4.31.6 → 4.31.7 as it affects THIS CLI's contracts**: `/Users/vinnypasceri/printing-press/.runstate/printing-press-workshop-e51dd43c/runs/20260904-131606-b6e44bce/pipeline/framework-delta-4.31.6-to-4.31.7.diff` (generated files only; ~3100 lines). Areas: internal/cli/helpers.go (CSV/plain/tabular output, dry-run printing, output bounding), data_source.go, sync.go + internal/store/store.go (pagination cursor-from-URL, composite ID overrides, upsert batching), internal/mcp/{tools,intents,bound}.go, internal/client/client.go, cmd/splitwise-pp-mcp/http_auth_test.go (new MCP HTTP auth test). The hand commands consume these helpers; the question is whether any hand file's assumption about a helper is now false, and whether the framework delta introduces a defect that ships in this artifact.
Yesterday's live matrix was 165/165; a fresh live matrix is running concurrently — do not run sync/--record/--send yourself.
Note: three of the four fixture commands (ledger, fairness nudge, split) report not-found on stderr with EMPTY stdout under --json, while settle-up emits a JSON error object; treat as a candidate Minor interface inconsistency, verify against helpers.go notFoundErr.
