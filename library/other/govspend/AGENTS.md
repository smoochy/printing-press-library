# Govspend Agent Notes

`govspend-pp-cli` is a read-only public-sector research CLI. Keep commands non-interactive and do not commit API key values, customer names, or sensitive procurement research notes.

Use `--dry-run` when showing request shape. Use `--agent` and `--select` for compact machine-readable output.
