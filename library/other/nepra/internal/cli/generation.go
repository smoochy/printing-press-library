// Copyright 2026 qazmataz and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"github.com/spf13/cobra"
)

func newGenerationCmd(flags *rootFlags) *cobra.Command {
	cmd := &cobra.Command{
		Use:         "generation",
		Short:       "Plant-level installed and dependable capacity with monthly generation and utilisation, per fiscal year",
		Annotations: map[string]string{"mcp:read-only": "true", "pp:parent-group": "true", "pp:api-resource": "true", "pp:typed-exit-codes": "0,2"},
		RunE:        parentNoSubcommandRunE(flags),
	}

	cmd.AddCommand(newGenerationIndexCmd(flags))
	cmd.AddCommand(newGenerationYearCmd(flags))
	// PATCHED (hand-authored, see .printing-press-patches/
	// nepra-generation-grains.json). The two grain children are ALSO attached
	// from a registerNovelCommand hook in the hand-authored
	// internal/cli/nepra_generation_grains.go, which is what survives a
	// regen. Both paths are idempotent: the hook uses addNovelCommandIfAbsent,
	// which no-ops when a non-scaffold child of that name already exists, so
	// the tree carries exactly one of each either way.
	//
	// They are listed HERE as well because verify-skill builds the command
	// tree by following literal AddCommand calls and cannot see through
	// root.Find. Without these two lines it reports
	// "nepra-pp-cli generation plants: command path not found in
	// internal/cli/*.go" — a false finding about a command that resolves.
	cmd.AddCommand(newGenerationPlantsCmd(flags))
	cmd.AddCommand(newGenerationMonthlyCmd(flags))
	return cmd
}
