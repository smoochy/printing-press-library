# Phase 4.95 local code review — zimmo-pp-cli (2026-09-23)
Review path: direct subagent dispatch (compound-engineering:ce-correctness-reviewer, correctness + security brief).
Autofix: 20 findings, 20 fixed in-place in 1 round (P1 whole-command --timeout on batch commands → batchFlags; comps mixed sale/rent median → usage error; walkSearch cross-page dedupe + unique-count completeness + partial result on late-page error; enrich geocode-only writes no longer bump last_seen, GEOCODE_FAILED marker, geo precision preserved on upsert, final gone-writes on a fresh context; motivated re-listing limited to earlier for-sale listings within 3 years; --csv formula neutralisation on every hand-written command; days_on_market recomputed on read; yield <code> requires a for-sale listing; watch save resets the seen-set on changed criteria; seen-set deletions per code + 30-day prune for incomplete searches; photos filename allowlist, redirect pinning, temp-file + size error; sibling DSN rejects ?/#/% and sets busy_timeout; UUID lookups by id column, shortlist stores the code; titleCase rune-safe; '+' in search= base64; FTS index pairs; termSafe C1/bidi; published_at NULL + price clears; ctx-aware backoff; prices --place-id skips a paramless geo call).
Tests added for dedupe, min-days filter, rental exclusion, safety helpers, accented titleCase, base64 '+'.
Template-shape retro candidates: generic writeCSVRow in helpers.go has no formula neutralisation (all printed CLIs); helpers.go:606 "Run the list command" troubleshooting hint for CLIs without list; README Output Formats examples use "mock-value" when the first endpoint lacks an example; boundCtx applies --timeout to whole batch commands.
Out-of-scope retro candidates: none.
Convergence: findings cleared at round 1 (shipcheck 7/7 after fixes).
