{
  "manifest_version": "0.3",
  "name": "pbs-pp-mcp",
  "display_name": "Pakistan Bureau of Statistics",
  "version": "4.31.7",
  "description": "The only machine-readable Pakistani price panel — every urban centre and essential item the Bureau prices weekly, accrued locally because the Bureau itself does not keep the series.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/pbs-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/pbs-pp-mcp",
      "args": [],
      "env": {
        "PBS_BASE_URL": "${user_config.pbs_base_url}",
        "PBS_USER_AGENT": "${user_config.pbs_user_agent}"
      }
    }
  },
  "user_config": {
    "pbs_base_url": {
      "type": "string",
      "title": "PBS_BASE_URL",
      "description": "Optional. Sets PBS_BASE_URL for the Pakistan Bureau of Statistics MCP server. Per-instance, non-secret setting; not a credential."
    },
    "pbs_user_agent": {
      "type": "string",
      "title": "PBS_USER_AGENT",
      "description": "Optional. Sets PBS_USER_AGENT for the Pakistan Bureau of Statistics MCP server. Per-instance, non-secret setting; not a credential."
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
